# 未绑定控制器截图候选归档

本目录保存迁移到 v2 截图槽位之前的 92 张控制器图片候选：46 张结构图候选和 46 张最小闭环结果图候选。

这些文件缺少当前冻结模型来源和当前 G5 packet 的绑定，因此只能用于历史追溯，不能复制回 `Docs/报告/图/控制器/`，不能作为报告正文、图审通过或仿真通过证据。

原始路径、归档路径、SHA-256、文件尺寸和候选角色见 `UNBOUND_SCREENSHOT_ARCHIVE_MANIFEST.json`。`pre_v2_*` 清单仅记录旧 v1 槽位状态，不恢复任何当前证据关系。
