# 报告图片目录

本目录只集中保存准备插入报告正文的图片副本。原始仿真结果、指标、日志和截图仍以 `Results/` 下的权威证据为准。

## 当前已整理

| 分类 | 当前数量 | 状态 | 用途 |
|---|---:|---|---|
| `APP/` | 3 | 可用 | 在线建模验证、实时联合仿真、生成代码部署三个工作台 |
| `QGC/` | 1 | 可用但需注明界面状态 | 飞行控制台、UE场景和Factory地图综合画面 |
| `控制器/` | 130 | 65条路线有模型图和结果图，部分待复核 | 按控制器类别和权威ID归档 |
| `FUEL/` | 6 | 可用 | 点云、探索轨迹、误差、高度及Gazebo/RViz联合画面 |
| `三机编队/` | 4 | 可用 | OpenBlocks整机模型、规划轨迹、原生动画和闭环曲线 |
| `手绘架构/` | 20 + 1总览 | 可用，已完成清单归位 | 系统架构、控制器族、安全/故障、规划、定位、代码生成和实验平台流程图 |
| `rviz/` | 3 | 分类完成 | FAST-LIO八字、Diff地图转换、FUEL点云地图 |
| `不采用/` | 2 | 不用于正文 | Sysplorer模型升级日志和升级弹窗 |

## 仍需补截

### P0：报告正文与录制视频必须补

1. `MWORKS建模/`：整机顶层图形模型完整窗口，能够看清四电机、电调、飞控、MID360、机载计算机和主要连接。
2. `MWORKS建模/`：单电机、电调、桨叶和效率因子子系统局部放大图。
3. `MWORKS结果与动画/`：单机起飞、悬停或8字任务的原生三维动画代表帧。
4. `MWORKS结果与动画/`：Official PID整机悬停和8字的参考/实际轨迹及误差曲线，至少2张。
5. `MWORKS结果与动画/`：风扰注入前后姿态、位置和控制量曲线，1张。
6. `MWORKS结果与动画/`：单电机效率下降、FDI确认、FTC重构和恢复/降落曲线，1至2张。
7. `MWORKS结果与动画/`：Safety Filter或安全状态机介入前后曲线，1张。
8. `代码生成与部署/`：MWORKS `GenerateModelCode`成功窗口或生成产物目录，1张。
9. `代码生成与部署/`：MIL/SIL输出叠加与差值图，1至2张；应由数据生成，不使用终端文字冒充。
10. `Gazebo部署/`：PX4、MAVROS、px4ctrl和Gazebo同次运行的清晰画面，1张。
11. `FAST-LIO/`：`rviz/8字.png` 已覆盖当前ROS1/Sunray/MID360点云与八字轨迹；正式入正文前建议裁掉顶部Codex标题栏和底部聊天文字，或补一张干净截图。
12. `Diff-Planner/`：`rviz/diff.png` 只覆盖点云地图与Grid3D栅格转换，仍需补单机RViz规划、单机Gazebo飞行、三机RViz规划、三机Gazebo飞行，共4张。
13. `QGC/`：连接、模式、位置姿态和任务状态均清晰的最终运行截图，1张。现有综合图可作界面展示，但不能单独证明飞行闭环。
14. `QGC/`或后续独立显示目录：无警告遮挡的UE飞行画面，1张。

按每个可变项取最低数量计算，系统与运行证据当前至少还缺17张；若风扰/故障、MIL/SIL分别拆图，则约需19张。加上控制器去重补截的12张，最低待补总量为29张。若把`rviz/8字.png`重新截成干净版本，则为30张。

## 已有RViz图片边界

- `rviz/8字.png`：可证明当前ROS1 FAST-LIO点云和八字轨迹，内容有效但建议裁剪。
- `rviz/diff.png`：可证明点云到Grid3D栅格地图的转换，不能证明Diff规划器完成单机或三机飞行。
- `rviz/fuel.png`：可作为FUEL探索轨迹与点云地图的简洁代表图；`FUEL/`中另有6张更完整证据。

### P0：控制器截图复核

- `mu_synthesis`和`neural_smc`没有模型图和结果图，属于明确实现阻塞，不补伪截图。
- 当前65条已实现路线均已归档模型图和结果图。
- 字节级检查发现4组模型图复用、5组结果图复用。若报告要求“每条路线一张独立截图”，至少还需补7张模型图和5张结果图。
- 除字节级重复外，还必须人工核对窗口标题、结果树、曲线名称和控制器ID。窗口复用导致的错误标题不能仅靠文件名修正。

### P1：有时间再补

1. Official PID阶跃、螺旋任务的独立结果图。
2. 改进PID、SMC、INDI、NMPC的同坐标性能对比图。
3. 代表性失败案例，如翻滚、发散、饱和或求解超时。
4. Gazebo与MWORKS同场景的趋势对比图。
5. QGC故障页、安全状态页和证据页的局部截图。

## 文件命名

统一采用：

```text
序号_对象_场景_证据类型.png
```

例如：

```text
01_OfficialPID_悬停_结果曲线.png
02_PID_INDI_八字_结果曲线.png
03_FTC_电机1效率下降_结果曲线.png
```

不要在本目录中放入激活登录页、升级弹窗、空白窗口、被通知遮挡的截图或无法追溯来源的论文图片。
