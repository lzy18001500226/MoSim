# 答辩PPT大纲 v4.0

## 第十五届中国软件杯 · MoSim · 总决赛答辩

> 版本：v4.0 / 2026-08-16
> 主线：**打通从MWORKS图形化建模到Gazebo实机部署的完整闭环**
> 口吻：参赛选手面向评委，自然、有节奏，不读数据

---

## 基本参数

| 项目     | 内容                                         |
| -------- | -------------------------------------------- |
| 答辩时长 | 6分钟展示 + 4分钟问答                        |
| 评分     | 功能性60% / 创新性20% / 规范性10% / 完整性10% |
| 核心主线 | MWORKS图形化建模→代码生成→ROS部署→Gazebo验证 |
| 视频策略 | 6段视频插入关键页（V1~V6），不单设演示页     |
| 联合仿真 | 截图+剪辑视频，MATLAB联合仿真风格包装        |
| 部署环境 | WSL2 Ubuntu 20.04，完整ROS运行基线         |

---

## 赛题覆盖检查表

| 赛题要求                          | 对应页码         |
| --------------------------------- | ---------------- |
| 基于Sysblock设计控制算法          | P10–15（第三幕） |
| PID改进（FOPID/AWFF/INDI等）      | P11              |
| 现代控制（滑模/MPC）              | P13–14           |
| 智能算法（模糊/神经网络复合）     | P12              |
| 编队控制（多机协同）              | P31–33（第七幕） |
| Syslab定量对比分析                | P22–23（第四幕） |
| 阶跃响应/8字/螺旋轨迹跟踪        | P20              |
| 鲁棒性（参数失配/风扰/电机故障）  | P20+P23          |
| 模块化可扩展设计                  | P07+P37          |
| 代码规范与可复现交付              | P37–38           |

---

## 叙事骨架（6分钟节奏）

```
第一幕  痛点与主张       ~1分20秒   MATLAB断供→工业对标→MoSim开放架构
第二幕  仿真体系基础     ~0分40秒   机体模型·统一接口·实验框架
第三幕  单机控制器建模   ~1分10秒   7族48路·Sysblock全图形化
第四幕  数据说话         ~1分20秒   对比实验·核心降幅·负样本可信
第五幕  代码生成与闭环   ~0分50秒   C99→SIL→ROS→Gazebo打通
第六幕  联合仿真         ~0分20秒   MWORKS↔外部仿真器交互
第七幕  编队控制         ~0分20秒   三机Figure8·ECBF安全层
第八幕  扩展能力         ~0分20秒   规划·感知·工业场景
第九幕  软件工程交付     ~0分20秒   Studio·WSL部署规范·开源
────────────────────────────────
合计                      6分00秒
```

---

## 页面列表（共40页）

| 页  | 类型   | 标题                                               | 所属幕   |
| --- | ------ | -------------------------------------------------- | -------- |
| 01  | 封面   | MoSim                                              | —        |
| 02  | 目录   | 九幕全景                                           | —        |
| 03  | 内容   | MATLAB断供：一个工程师都懂的壁垒                   | 第一幕   |
| 04  | 内容   | 对标：MATLAB UAV Toolbox工业参考方案               | 第一幕   |
| 05  | 内容   | MoSim对标：从零构建开放架构                        | 第一幕   |
| 06  | 内容   | 全链路架构：五层·数据流向·闭环定义                | 第一幕   |
| 07  | 内容   | 云纵150：六自由度MultiBody动力学模型               | 第二幕   |
| 07  | 内容   | 云纵150：六自由度MultiBody动力学模型               | 第二幕   |
| 08  | 内容   | 四种统一输出接口：任意控制器接入同一Plant          | 第二幕   |
| 09  | 内容   | 实验框架：48路·同一机体·同一评价标准              | 第二幕   |
| 10  | 章节   | 第三章：单机控制器·7族46条全图形化建模            | 过渡     |
| 11  | 内容   | 七族算法分类：从PID到学习控制                      | 第三幕   |
| 12  | 内容   | PID族：基线→FOPID·AWFF·INDI·L1（九条路线）       | 第三幕   |
| 13  | 内容   | 智能算法：模糊PID·模糊滑模·神经网络残差补偿        | 第三幕   |
| 14  | 内容   | 滑模族：Super-Twisting SMC + 高阶DFBC              | 第三幕   |
| 15  | 内容   | MPC族：线性MPC·NMPC（十条路线）                   | 第三幕   |
| 16  | 内容   | 几何控制：公式与Sysblock模型一一对应              | 第三幕   |
| 17  | 视频   | **[V1] Sysblock图形化建模实操演示**                | 第三幕   |
| 18  | 章节   | 第四章：七场景实验·数据说话                        | 过渡     |
| 19  | 内容   | ClimbPath 50s筛查：30/48达标·失败有记录            | 第四幕   |
| 20  | 内容   | px4ctrl：全图形化位置/速度外环设计                 | 第四幕   |
| 21  | 内容   | 七场景设计：从名义到极端故障全覆盖                 | 第四幕   |
| 22  | 视频   | **[V2] 七场景可视化对比演示**                      | 第四幕   |
| 23  | 内容   | ★核心数字：step↓29.8%·fig8↓73.3%·wind↓52.1%     | 第四幕   |
| 24  | 内容   | 负样本：参数失配px4ctrl反而更差（可信度锚点）      | 第四幕   |
| 25  | 章节   | 第五章：代码生成·SIL验证·Gazebo全链路闭环         | 过渡     |
| 26  | 内容   | GenerateModelCode：Sysblock→C99（ISO，无Runtime）  | 第五幕   |
| 27  | 内容   | SIL验证：50s·双精度量级·1.148e-13m                | 第五幕   |
| 28  | 内容   | 自研ROS1-MWORKS实时桥：外部C/C++接口·外环+PX4内环·RT0已验证       | 第五幕   |
| 29  | 视频   | **[V3] C99→ROS Bridge→Gazebo 全链路联通验证（rosbag已录制）**      | 第五幕   |
| 30  | 内容   | MWORKS实时联合仿真：RT0 I/O验证·200Hz·P99=5.71ms·外环+内环分工    | 第六幕   |
| 31  | 视频   | **[V4] RT0实时I/O能力验证演示（200Hz·零丢包）**                    | 第六幕   |
| 32  | 内容   | 三机编队架构：Guidance层+单机控制器分层组合        | 第七幕   |
| 33  | 内容   | 三机Figure8：RMSE=2.3e-13m·最近距离=2.08m         | 第七幕   |
| 34  | 视频   | **[V5] 三机编队Gazebo演示** + ECBF安全层介绍       | 第七幕   |
| 35  | 内容   | OpenBlocks：A*+min-snap轨迹规划·单/多机障碍规避    | 第八幕   |
| 36  | 内容   | FAST-LIO感知·UE工业场景·Diff-Planner局部规划       | 第八幕   |
| 37  | 视频   | **[V6] 障碍规避+工业场景地图重建演示**             | 第八幕   |
| 38  | 内容   | MoSim Studio + AI Agent：统一管理界面              | 第九幕   |
| 39  | 内容   | 软件工程交付：WSL2部署规范·开源·二次开发接口       | 第九幕   |
| 40  | 封底   | 完整交付物全景·致谢                                | —        |

---

## 各页详细内容

---

### 第01页：封面

**主标题**：基于国产MWORKS的四旋翼全链路控制验证平台

**副标题（大字）**：MoSim

**第二副标题**：从图形化建模到运行时部署的完整工程链验证

**视觉元素**（三列连接图）：
- 左：Sysblock控制器图形模型截图
- → 中：C99代码片段截图（`px4ctrl_step()`函数）
- → 右：Gazebo Figure8飞行轨迹画面
- 左下：MWORKS logo + 中国软件杯logo（第十五届）
- 右下：学校·队员·指导老师

**台词（约10秒）**：
> 大家好，我们的项目叫MoSim，是一个基于国产软件MWORKS构建的四旋翼全链路仿真验证平台。今天我们用6分钟，把这条从图形化建模到真实部署的完整链路给大家讲清楚。

---

### 第02页：目录

**内容**：九幕结构，左侧编号+标题，右侧对应关键词

| 幕次 | 标题             | 关键词               |
| ---- | ---------------- | -------------------- |
| 一   | 痛点与主张       | MATLAB→MWORKS壁垒    |
| 二   | 仿真体系基础     | 机体·接口·实验框架   |
| 三   | 单机控制器建模   | 7族·46条·Sysblock    |
| 四   | 数据说话         | 七场景·三大降幅      |
| 五   | 代码生成与闭环   | C99·SIL·Gazebo       |
| 六   | 联合仿真         | MWORKS↔外部仿真器   |
| 七   | 编队控制         | 三机Figure8·ECBF     |
| 八   | 扩展能力         | 规划·感知·工业场景   |
| 九   | 软件工程交付     | WSL2·开源·二次开发 |

---

### 第03页：MATLAB断供——一个工程师都懂的壁垒

**标题**：MATLAB断供：一个工程师都懂的壁垒

**核心视觉（左右布局）**：
- 左：对比图——"MATLAB/Simulink技术栈"（带红色禁用符号）vs "MWORKS技术栈"（绿色）
- 右：三个问题点卡片：①代码生成依赖Embedded Coder ②仿真到部署无标准路径 ③国产平台缺完整工程链验证

**台词（约20秒）**：
> 做无人机仿真，大家最熟悉的工具是MATLAB和Simulink。但断供这件事已经不是新闻了——一旦依赖停了，从模型到代码这整条链就断了。这不是危言耸听，是工程师必须提前解决的壁垒。MWORKS是国产的系统建模平台，功能上对标Simulink。我们这个项目想验证的，就是这个平台能不能真正撑起从建模到部署的完整工程链。

---

### 第04页：工业界参考方案——MATLAB UAV Toolbox完整工具链

**标题**：对标：MATLAB UAV Toolbox工业参考方案

**核心视觉（左图右文）**：
- 左：2张图并排
  - 上：**QGC.png**（MATLAB工程窗口与QGroundControl连接配置）
  - 下：**UE.png**（QGroundControl、Unreal Engine与MATLAB联合仿真视图）
- 右：关键要点（3~4条）
  - ① MATLAB官方UAV Package Delivery示例
  - ② QGC地面站 + Unreal Engine + Simulink全链路集成
  - ③ 预置控制器工具箱（成熟，但黑盒封装）
  - ④ 商业工具链的工业标杆

**底部小字免责声明（灰色）**：
> 以上为MATLAB官方示例，非MoSim实测结果

**台词（约18秒）**：
> 在讲我们的方案之前，先看一下工业界已经有什么。这是MATLAB的UAV Toolbox官方示例——QGroundControl地面站、虚幻引擎仿真环境、Simulink控制器，全链路打通。这是一个成熟的商业工具链，我们对标的正是这个工业参考水平。但它有一个问题。

---

### 第05页：MoSim对标方案——从零构建开放架构

**标题**：MoSim对标：MWORKS无现成工具箱 → 从零构建开放架构

**核心视觉（左图右文）**：
- 左：2张图并排
  - 上：**UI.png**（Simulink顶层模型截图）
  - 下：**障碍物感知.png**（MATLAB工具箱障碍感知能力展示）
- 右：MoSim对比要点（4条，突出差异）
  - ① MWORKS **无现成UAV工具箱** → 从零构建
  - ② **48个控制器核心** × 统一Harness接口
  - ③ **纯Sysblock图形建模** → 完全开放，可审查每个模块
  - ④ 对标工业参考，但**更灵活、可二次开发**

**底部标注（强调）**：
> MATLAB：工具箱成熟，但控制器黑盒封装，二次开发受限
> MoSim：无现成工具箱，但48核心统一接口，完全开放可扩展

**台词（约20秒）**：
> MATLAB工具箱的控制器是封装好的黑盒，你不知道里面用的什么算法，也很难做深度定制。MWORKS没有这种现成的UAV工具箱，所以我们从零开始构建了48个控制器核心，全部用Sysblock图形化实现，每个模块都可以打开审查。我们还定义了统一的Harness接口，任何一个控制器都能快速接入同一套测试框架。对标工业参考，但架构更开放。

---

### 第06页：全链路架构——五层数据流向

**标题**：全链路架构：五层·数据流向·闭环定义

**核心视觉（AI生图，用prompt.md PPT-02块）**：
纵向五层：
1. **建模层** — Sysplorer(MultiBody·6DOF) + Sysblock(控制器图形化·46路) + Syslab(数据分析·Julia)
2. **生成层** — GenerateModelCode → C99(ISO标准，无Runtime依赖)
3. **验证层** — SIL夹具(CFunction·RMSE 1.148e-13m) + FormalRunner(ClimbPath 50s)
4. **MWORKS扩展层** — 三机编队TriangleFigure8 + OpenBlocks轨迹(A*冻结参数) + ECBF安全层（全为Modelica模型，纯MWORKS内）
5. **WSL2运行层** — 自研ROS Bridge + PX4 SITL + Gazebo Classic + FAST-LIO（Ubuntu 20.04 / WSL2）

右侧纵向标注：←MWORKS主线（层1–3） / ←MWORKS内扩展（层4） / ←WSL2链路（层5）
左侧括号：「纯MWORKS内部」覆盖层1–4；「WSL2运行层」覆盖层5

**台词（约15秒）**：
> 整个MoSim分五层。上面四层——建模、代码生成、验证、编队与规划扩展——全都在MWORKS内部完成。最底下的WSL2运行层是独立的部署环境，通过我们自研的ROS Bridge和PX4接入Gazebo。每一层之间有清晰的接口边界。

---

### 第07页：云纵150——六自由度MultiBody动力学模型

**标题**：云纵150：Sysplorer六自由度MultiBody动力学模型

**核心视觉（左图右文）**：
- 左：MWORKS Sysplorer中云纵150机体三维图形审查截图
- 右：参数分层说明（5层）：
  - 层1 几何：轴距、桨径、臂长
  - 层2 质量/重心：m=1.0kg，g=9.80665
  - 层3 惯性张量：Ixx/Iyy/Izz对角主惯量
  - 层4 气动：升力系数k_f, 扭矩系数k_m
  - 层5 传感器：IMU、气压、GPS模型

**台词（约15秒）**：
> 机体模型是整个仿真的物理基础。我们用Sysplorer的MultiBody模块建了一个六自由度的云纵150模型，参数分五层管理，从几何到气动到传感器，每一层都可以单独调整替换，不影响控制器接口。

---

### 第08页：四种统一输出接口——任意控制器接入同一Plant

**标题**：四种统一输出接口：任意控制器接入同一Plant

**核心视觉（AI生图，接口架构图）**：
四列并排，共享一个Plant(向右汇聚)：
- ATTITUDE_THRUST → Adapter → Plant
- BODY_RATE_THRUST → Adapter → Plant
- WRENCH → Adapter → Plant
- ROTOR_COMMAND → Adapter → Plant

底部注明：46条控制器均通过上述四种接口之一输出，Adapter层自动处理坐标变换(ENU/FLU ↔ NED/FRD)

**台词（约15秒）**：
> 为了让所有控制器都能接到同一个Plant，我们定义了四种标准输出接口。不管是PID还是MPC还是几何控制，只要实现其中一个接口，就能立刻接入统一的仿真评测框架。坐标变换、单位换算，全在Adapter层处理，控制器本身不需要关心。

---

### 第09页：实验框架——48路·同一机体·同一评价标准

**标题**：可信实验框架：48路控制器·同一机体·同一Plant·同一评价标准

**核心视觉（左图右文）**：
- 左：实验框架示意图（Profile配置→FormalRunner→对比输出）
- 右：三个保证
  - ① 同一机体：云纵150，参数全局冻结
  - ② 同一Plant：MultiBody物理模型，48路共享
  - ③ 同一评价：ClimbPath 50s统一窗口，RMSE统一计算口径

底部：48=46条算法+Official PID(baseline)+px4ctrl(自研baseline)

**台词（约15秒）**：
> 做对比实验最重要的是公平。我们用同一套Profile配置驱动所有控制器，同一个机体模型，同一套评价时间窗，确保任何两条路线的数字都能直接比较。一共48路控制器，都经过了这套框架。

---

### 第11页：章节页——第三章 单机控制器

**标题（章节过渡页）**：
> 第三章：单机控制器
> 7个算法族 · 46条图形化实现 · MWORKS Sysblock主线

副题：赛题要求基于Sysblock实现PID改进、滑模、MPC、模糊、神经网络——我们全做了

---

### 第11页：七族算法分类总览

**标题**：七个算法族：从PID改进到学习控制，全图形化实现

**核心视觉（分类树/卡片墙）**：
7列卡片，每列一族：

| 族 | 代表算法 | 条数 |
| ---- | ---- | ---- |
| PID族 | PID·FOPID·AWFF·INDI·L1 | 9 |
| 线性鲁棒 | LQR·H∞·极点配置 | 6 |
| 非线性自适应 | MRAC·L1-Adaptive·INDI-Alt | 5 |
| 滑模 | STC·DFBC·TSMC·模糊SMC | 7 |
| 最优预测 | LMPC·NMPC·DMPC | 10 |
| 几何/微分平坦 | SE(3)·平坦轨迹规划 | 6 |
| 学习控制 | 神经残差补偿·模糊PID | 3 |

底部标注：所有控制器均在Sysblock图形化环境中实现

**台词（约15秒）**：
> 一共七个算法族，46条控制器。从最基础的PID改进，到滑模、MPC、几何控制，再到神经网络残差补偿——全部用Sysblock的图形化模块搭建，没有手写一行控制核心代码。

---

### 第12页：PID族——基线与改进

**标题**：PID族：从官方基线到FOPID·AWFF·INDI·L1（九条路线）

**核心视觉（左右对比）**：
- 左：Official PID基线Sysblock截图（赛题提供的原始控制器）
- 右：改进路线树（从Official PID向外分叉）：
  - PID → FOPID（分数阶）
  - PID → AWFF（前馈抗饱和）
  - PID → INDI（增量式非线性动态逆）
  - PID → L1-AC（L1自适应控制）
  - px4ctrl（自研，位置/速度外环）

底部：每条路线均保留与Official PID的A/B对比结果

**台词（约15秒）**：
> 赛题要求以官方PID为基础做改进。我们在保留原始基线的基础上，向外延伸了八条改进路线。从最经典的分数阶PID，到加了前馈和抗饱和，再到INDI这种非线性动态逆——每一条都和官方PID跑了A/B对比。

---

### 第13页：智能算法——模糊PID·模糊滑模·神经网络残差

**标题**：智能算法：模糊PID · 模糊滑模 · 神经网络残差补偿

**核心视觉（三卡片+Sysblock截图）**：
- 卡片1：模糊PID —— 隶属函数图 + Sysblock结构截图
  - 作用点：PID增益在线调整
- 卡片2：模糊滑模 —— 模糊推理替换符号函数（消抖）
  - Sysblock中模糊推理模块截图
- 卡片3：神经网络残差补偿 —— 学习模型不确定性
  - 前馈残差网络结构示意
  - Sysblock中接入方式截图

底部：三种策略均通过统一接口接入评测，不单独设新机体

**台词（约15秒）**：
> 赛题明确要求智能算法的应用。我们做了三个方向：模糊PID在线调整增益，模糊滑模用模糊推理替换符号函数来抑制抖振，还有神经网络残差补偿来处理模型不确定性。三种都在Sysblock里图形化实现，接入同一套评测框架。

---

### 第14页：滑模族——Super-Twisting SMC + 高阶DFBC

**标题**：滑模族：Super-Twisting SMC · 高阶DFBC · 七条路线

**核心视觉（左右布局）**：
- 左：Super-Twisting SMC的Sysblock实现截图（姿态环滑模面+二阶超螺旋律）
- 右：性能对比曲线（MWORKS.Syslab生成）
  - 左图：STC vs PID 阶跃响应对比
  - 右图：STC vs PID 风扰场景RMSE对比

- 底部注明：七条滑模路线，包括Terminal SMC、模糊SMC、离散SMC等

**台词（约12秒）**：
> 滑模控制我们做了七条路线。这里展示的是Super-Twisting，它的好处是避免了传统滑模的高频抖振，同时保留了对扰动的强鲁棒性。曲线是Syslab直接从仿真结果里生成的。

---

### 第15页：MPC族——线性MPC · NMPC（十条路线）

**标题**：最优预测控制族：线性MPC · NMPC · 十条路线

**核心视觉（左图右文）**：
- 左：线性MPC Sysblock结构截图（预测模型→QP求解→控制输出）
- 右：MPC路线谱系卡片：
  - LMPC(线性) × 3预测时域变体
  - NMPC(非线性) × 3求解方法
  - DMPC(分布式) × 3协调策略
  - EMPC(经济) × 1

底部：MPC族在Figure8轨迹跟踪上达到最佳RMSE之一

**台词（约12秒）**：
> MPC族是整个系统里路线最多的，一共十条。核心思路都是滚动时域优化，区别在于预测模型是线性还是非线性，以及求解方式。在Figure8这种高机动轨迹上，MPC族的跟踪精度表现最好。

---

### 第16页：几何控制——公式与Sysblock模型一一对应

**标题**：几何控制：SE(3)流形上的公式与Sysblock模型一一对应

**核心视觉（左公式右模型）**：
- 左：SE(3)几何控制器的姿态误差公式（排版清晰）
  ```
  eR = ½(R_d^T R - R^T R_d)^∨
  eΩ = Ω - R^T R_d Ω_d
  ```
- 右：Sysblock中对应的模块截图（用标注框指出公式→模块的映射关系）

底部说明：Sysblock实现不是黑箱，每个模块都有公式对应，可审查

**台词（约12秒）**：
> 几何控制是在SO(3)或SE(3)流形上直接操作旋转矩阵，理论上可以处理大角度机动。我们把姿态误差公式直接翻译到Sysblock里，左边是公式，右边是对应的模块，一一可对应。这不是黑箱。

---

### 第17页：[V1] Sysblock图形化建模演示视频

**标题**：[视频] Sysblock图形化建模：从数学到可运行模型

**视频说明**：
- **时长**：45~60秒
- **内容构思**：
  1. 打开MWORKS Sysblock界面（工作区截图/录屏）
  2. 展示一个完整控制器的图形化结构（建议：px4ctrl位置外环）
  3. 快进展示从图形模型到连接到Plant的过程
  4. 最后一帧：MWORKS中运行一次仿真，曲线出来

**插入位置说明**：放在第16页，在七族算法展示完毕后，用一段视频让评委直观感受"图形化建模"到底是什么样的

**台词引导（视频前约5秒）**：
> 讲了这么多族，可能评委会想——这个图形化建模到底长什么样？我们来看一段实录。

**台词（视频后约5秒）**：
> 这就是Sysblock里的一个控制器。每个模块、每条连线，都有对应的物理意义，和MATLAB Simulink的操作逻辑是完全一致的。

---

### 第18页：章节页——第四章 七场景实验

**标题（章节过渡页）**：
> 第四章：七场景实验
> 数据说话 · 可信框架 · 核心降幅

副题：ClimbPath筛查 + 七场景设计 + 对比结论

---

### 第19页：ClimbPath 50s筛查——30/48达标

**标题**：ClimbPath 50s筛查：30/48路线达标·失败有记录

**核心视觉（数据可视化，左图右文）**：
- 左：48路控制器筛查结果热力图或条形图（Syslab生成）
  - 绿色：30条通过
  - 红色：18条有记录失败
- 右：筛查标准说明：
  - 终点位置误差 < 阈值
  - 50秒内姿态无发散
  - 各传感器输出无NaN/Inf

底部：失败路线保留记录，不删除，作为性能下界

**台词（约12秒）**：
> 在进入正式七场景对比之前，我们先用一条50秒的ClimbPath把所有48路控制器筛了一遍。结果是30条通过，18条有记录的失败。这18条我们没删，失败就是失败，保留下来是为了让后面的数字更可信。

---

### 第20页：px4ctrl——全图形化位置/速度外环设计

**标题**：px4ctrl：自研全图形化位置/速度外环控制器

**核心视觉（架构图+截图）**：
- 左：px4ctrl分层架构图（Sysblock截图）
  - 外环：位置误差 → 速度指令
  - 中环：速度误差 → 姿态指令（ATTITUDE_THRUST接口）
  - 内环：由PX4飞控原生执行（不替换）
- 右：设计参数表：
  - Kp_pos = Kv_pos = 1.5
  - 采样频率：50Hz
  - 输出类型：ATTITUDE_THRUST

底部：px4ctrl是专门为Gazebo部署层设计的，与46条纯仿真算法独立

**台词（约15秒）**：
> px4ctrl是我们自研的一个控制器，专门用于和PX4飞控对接。它做了两层外环——先把位置误差转成速度指令，再把速度误差转成姿态指令，然后交给PX4内环执行。整个设计过程也是在Sysblock里图形化完成的。

---

### 第21页：七场景设计——从名义到极端故障

**标题**：七场景实验：从名义飞行到极端故障全覆盖

**核心视觉（七格卡片墙）**：

| 场景            | 类型     | 关键参数                  |
| --------------- | -------- | ------------------------- |
| hover           | 名义     | 原地悬停30秒              |
| step_response   | 名义     | 位置阶跃1m，测超调/调节时 |
| figure8         | 名义     | 8字轨迹，测跟踪RMSE       |
| spiral          | 名义     | 螺旋爬升，综合机动         |
| wind_disturbance | 鲁棒性  | 3m/s侧向风扰，测姿态保持  |
| parameter_mismatch | 鲁棒性 | 质量/惯量±30%失配         |
| motor_efficiency_fault | 故障 | 单电机效率下降50%        |

底部：七场景覆盖赛题要求的螺旋爬升、8字、风扰、参数摄动

**台词（约12秒）**：
> 七个场景从易到难，前四个是正常飞行，后三个是鲁棒性测试——风扰、参数失配、电机故障。这基本覆盖了赛题要求的所有测试场景，也包括了我们自己设计的电机效率故障场景。

---

### 第22页：[V2] 七场景可视化对比演示

**标题**：[视频] 七场景MWORKS仿真可视化演示

**视频说明**：
- **时长**：60~90秒
- **内容构思**：
  1. 画中画：左半MWORKS三维仿真视景，右半Syslab数据曲线实时输出
  2. 快剪七个场景：hover→step→figure8→spiral→wind→param_mismatch→motor_fault
  3. 每个场景3~5秒，配字幕标注场景名
  4. 最后定格在数据对比图上（Syslab生成的多控制器对比曲线）

**台词引导（视频前5秒）**：
> 赛题要求用Syslab做定量分析，我们来看一下七个场景的可视化效果。

**台词（视频后5秒）**：
> 每个场景的原始数据、曲线和指标，全部用Syslab生成，可以直接对应到报告里的每一张图。

---

### 第23页：★核心数字——三大降幅

**标题**：★ px4ctrl vs Official PID：三个场景的核心对比数字

**核心视觉（数据高潮页，重点突出）**：

三个大数字并排展示（视觉冲击力）：

```
step_response      figure8          wind_disturbance
   ↓29.8%           ↓73.3%             ↓52.1%
  阶跃RMSE          轨迹RMSE           风扰RMSE
```

下方：统一条件说明框
- 同一机体：云纵150（参数冻结）
- 同一评价窗口：MWORKS FormalRunner，50s ClimbPath通过后进入
- 同一RMSE计算口径：Syslab脚本，结果可复现

底部引出下一页：但有一个场景px4ctrl反而更差 →

**台词（约20秒）**：
> 在三个核心场景上，px4ctrl相比官方PID——阶跃响应RMSE降了29.8%，Figure8轨迹跟踪降了73.3%，风扰场景降了52.1%。这三个数字是在同一套实验框架下跑出来的，任何一个数字背后都有原始的MSR文件和Syslab分析脚本。但我想特别说一下第四个场景。

---

### 第24页：负样本——参数失配场景下px4ctrl反而更差

**标题**：负样本：参数失配场景下px4ctrl反而更差——这很重要

**核心视觉（左右对比）**：
- 左：参数失配场景RMSE对比柱状图
  - Official PID：0.1039m
  - px4ctrl：1.2258m（明显更差，用红色/向上箭头标注）
- 右：原因分析框（简洁）：
  - px4ctrl前馈项对参数误差更敏感
  - 设计权衡，非Bug
  - 说明前三个降幅数字的可信度

**台词（约20秒）**：
> 在参数失配场景里，px4ctrl的RMSE达到了1.2258米，而官方PID只有0.1039米——px4ctrl明显更差。这不是我们想藏的结果，恰恰相反，这个负样本我们专门保留下来。有了这个数字，前面那三个下降的百分比才是可信的，不是挑出来的。这也说明，前馈设计对参数精度有依赖，这本来就是一个工程权衡。

---

### 第25页：章节页——第五章 代码生成与闭环

**标题（章节过渡页）**：
> 第五章：代码生成 · SIL验证 · Gazebo全链路闭环
> 这一章证明：MWORKS生成的代码真的能飞

副题：C99生成 → 代码一致性验证 → 自研ROS层 → Gazebo运行时

---

### 第26页：GenerateModelCode——Sysblock→C99

**标题**：GenerateModelCode：从Sysblock图形模型到ISO C99源码

**核心视觉（左右流程）**：
- 左：Sysblock控制器模型截图（px4ctrl）
- 中：GenerateModelCode操作截图（MWORKS界面）
- 右：生成的C99源码片段截图（`px4ctrl_step()`函数头部）
  - 注明：ISO C99标准，无任何Runtime依赖，可直接编译

底部三个特性卡片：
- ① 无Runtime依赖（不需要MWORKS运行时库）
- ② 符合ISO C99（可移植到任意嵌入式平台）
- ③ 源码与模型绑定（可追溯，可复核）

**台词（约15秒）**：
> MWORKS的GenerateModelCode功能，可以把Sysblock里的图形模型直接生成C99标准的源码。生成出来的代码没有任何Runtime依赖——不需要带着MWORKS的运行库，可以直接用GCC编译，移植到任何平台。这跟Simulink加Embedded Coder的路子是完全一样的。

---

### 第27页：SIL验证——50s·双精度量级一致性

**标题**：SIL验证：代码与模型的数值一致性证明

**核心视觉（数字卡片+对比表）**：

三个核心指标并排展示：

```
位置 RMSE             姿态 RMSE             转速 RMSE
1.148e-13 m         3.207e-12 rad         7.354e-11 rad/s
  双精度量级           双精度量级             双精度量级
```

下方说明：
- 测试方式：将生成的C99函数通过CFunction SIL夹具接入MWORKS，与原Sysblock模型并行运行50秒
- 误差来源：仅浮点运算顺序差异，无逻辑分歧
- 结论：代码生成结果与图形模型数值等价

**台词（约15秒）**：
> 代码生成完之后，我们做了SIL验证——把生成的C99函数接回MWORKS和原模型并行跑了50秒。位置误差是1.148e-13米，姿态误差是3.2e-12弧度，全都在双精度浮点的精度范围内。这证明生成的代码和原来的图形模型是数值等价的。

---

### 第28页：自研ROS通信层——外环控制+内环分工

**标题**：自研ROS1-MWORKS实时桥：外部C/C++接口构建版本化通信层

**核心视觉（横向链路图）**：
```
Gazebo / PX4 / MAVROS
  状态+参考
      ↓
ROS1适配器（时间戳·坐标·有效性校验）
      ↓ [固定版本 UDP StateFrame]
MWORKS 控制器（Sysblock图形化·固定步长）
  仅做位置/平动外环，输出期望姿态+总推力
      ↓ [UDP AttitudeThrustCommand]
ROS1适配器（新鲜度·限幅·唯一发布者仲裁）
      ↓
MAVROS AttitudeTarget
      ↓
PX4内环（姿态率·failsafe·执行器分配）
      ↓
Gazebo
```

下方三个说明卡片：
- ① **MWORKS做外环**：位置/速度外环 → 期望姿态+总推力；PX4保留姿态率内环与安全机制
- ② **外部C/C++接口**：MWORKS 2026a通过外部函数接口接入ROS1，非MATLAB ROS Toolbox式原生支持
- ③ **RT0 I/O已验证**：200Hz输出实测200.02Hz，P99延迟5.71ms，最大9.74ms，零丢包（sim_mode=2）

底部小字注释：px4ctrl回退状态机和影子模式已实现（ros1_rt1_adapter.py / rt1_contract.py）

**台词（约18秒）**：
> 要把MWORKS控制器接入Gazebo，我们自研了这个ROS1通信桥。MWORKS只做位置外环，计算出期望姿态和推力，通过UDP传给ROS适配器，再经MAVROS送给PX4；PX4保留姿态率内环和故障保护。MWORKS本身没有MATLAB那种原生ROS工具箱，我们用的是它的外部C/C++函数接口构建的桥接层。实时I/O能力已通过RT0验证：200Hz，P99延迟5.71毫秒，零丢包。

---

### 第29页：[V3] C99全链路联通验证

**标题**：[视频] C99→ROS Bridge→PX4 SITL→Gazebo 全链路联通验证

**视频说明**：
- **时长**：45~60秒
- **来源**：Results/sunray_ros1/qgc-c99-factory-figure8-live-display-20260807-001/ rosbag回放+RViz显示
- **内容构思**：
  1. 开头5秒：Sysblock控制器截图→C99代码片段截图（静止画面）
  2. 主体40秒：Gazebo+RViz画中画，无人机已起飞（+27.9m高度），rosbag录制中
  3. 结尾：显示DEMO_STATUS.json关键字段：status=connected，rosbag已录制（~229MB），UAV已起飞
  4. 不展示Figure8跟踪RMSE（帧对齐门禁尚未通过）

**台词引导**：
> 这是链路联通的验证——MWORKS生成的C99代码，通过我们的ROS Bridge接入Gazebo，无人机已经起飞了。来看一下。

**台词（视频后）**：
> 链路已经完整打通，rosbag录制了229MB的飞行数据。精确轨迹跟踪的形式化验收我们按分级门禁走，这里展示的是链路连通性证明。

---

### 第30页：联合仿真——MWORKS实时外环+PX4内环架构

**标题**：MWORKS实时联合仿真：外部C/C++接口驱动实时外环控制

**核心视觉（左右分栏架构图）**：
- 左：MWORKS侧（蓝色边框）
  - Sysblock控制器（位置/速度外环，固定步长，sim_mode=2）
  - 外部C函数接口（UDP收发，非阻塞）
  - 注：MWORKS 2026a通过外部C/C++接口接入ROS，无原生ROS工具箱
- 右：WSL2仿真器侧（橙色边框）
  - ROS1 Noetic + MAVROS
  - PX4 SITL（内环·failsafe·执行器分配）
  - Gazebo Classic（动力学·传感器·可视化）

中间：双向UDP数据流箭头
- → 状态帧（位置·速度·姿态，200Hz）
- ← 控制指令（期望姿态+总推力）

底部RT0实测证据框（深绿色边框）：
```
RT0 实时I/O验证（已通过）
输出频率：200.02 Hz（设定200Hz）
P99延迟：5.71 ms   最大延迟：9.74 ms   丢包：0/0
来源：Results/control_platform/mworks_live_full_loop_20260719/rt0_200hz_v3_run6/
```

底部评委口径注释（小字灰色）：
> "完整飞行闭环按RT1影子→RT3悬停→故障回退分级验收；RT0仅证明MWORKS外部I/O与调度能力，不等同于飞行闭环成功。"

**台词（约18秒）**：
> 联合仿真这条路，MWORKS作为实时外环控制器，通过UDP和Gazebo交换数据。MWORKS做外环，输出期望姿态；PX4保留内环和故障保护。我们已经完成了RT0验证：在200Hz下，P99延迟5.71毫秒，没有丢包。这证明了MWORKS的实时I/O能力。完整飞行闭环我们按分级验收，不在这里过度声明。

---

### 第31页：[V4] 联合仿真效果演示

**标题**：[视频] MWORKS实时I/O能力验证演示（RT0）

**视频说明**：
- **时长**：30~40秒
- **内容**（截图+剪辑）：
  1. 左侧：MWORKS Sysblock控制器运行界面（sim_mode=2，固定步长运行）
  2. 右侧：UDP数据收发日志（频率计数、时间戳、丢包统计）
  3. 终帧：rt0_analysis.json关键数字大字展示：200.02Hz / P99=5.71ms / 最大9.74ms / 丢包=0

**台词引导**：
> 来看一下RT0实时I/O验证的实际效果。

**台词（视频后）**：
> 频率、延迟、丢包三个指标全部符合200Hz实时控制的要求。这是我们进入飞行闭环验收的前提门禁，已经通过了。

---

### 第32页：三机编队——Guidance层分层架构

**标题**：三机编队：Guidance层生成参考+单机控制器分层组合

**核心视觉（架构图）**：
```
编队任务目标（三角形阵型）
         ↓
  Guidance层（编队参考生成）
  ├── UAV1参考轨迹
  ├── UAV2参考轨迹
  └── UAV3参考轨迹
         ↓（各自独立）
  单机控制器（px4ctrl×3）
         ↓
  各机Plant（独立仿真）
         ↓
  编队状态采集 → 协同指标计算
```

右侧标注：
- 阵型：等边三角形，边长2.5m
- 轨迹：三机同步Figure8
- ECBF安全层：可插入Guidance与控制器之间

**台词（约15秒）**：
> 编队控制的架构是分层的——上面Guidance层负责根据编队目标给每台无人机生成各自的参考轨迹，下面每台无人机用独立的单机控制器跟踪自己的参考。编队协同是在Guidance层做的，控制器本身不需要知道其他无人机的存在。

---

### 第33页：三机Figure8——协同精度数据

**标题**：三机Figure8协同飞行：RMSE=2.3e-13m · 安全距离=2.08m

**核心视觉（左图右文）**：
- 左：三机Figure8轨迹图（Syslab生成，三条轨迹叠加，颜色区分）
- 右：核心指标表：

| 指标         | 数值          |
| ------------ | ------------- |
| 编队RMSE     | 2.2855e-13 m  |
| 飞行期间最近距离 | 2.0785 m  |
| 阵型类型     | 等边三角形    |
| 飞行轨迹     | Figure8×3     |

底部：编队RMSE达双精度量级——三机轨迹严格同步

**台词（约15秒）**：
> 三台无人机同步飞Figure8，编队的位置同步误差RMSE是2.3e-13米，这已经是双精度浮点的精度范围了，本质上是完美同步。整个飞行过程中，三机之间的最近距离是2.08米，没有碰撞风险。

---

### 第34页：[V5] 三机编队Gazebo演示 + ECBF安全层

**标题**：[视频] 三机编队Figure8演示 · ECBF可插拔安全层

**视频说明**：
- **时长**：30~45秒
- **内容**：Gazebo三机编队Figure8飞行，RViz显示三条轨迹
- **画面补充**：视频后半段展示ECBF架构图（静止）

**ECBF说明卡片（视频结束后显示）**：
- ECBF（指数控制障碍函数）作为安全层，插在Guidance与控制器之间
- 不修改控制器本身，纯叠加约束
- 当前为架构候选，已完成约束公式推导
- 约束形式：h(x) ≥ 0，CBF条件：ḣ + γh ≥ 0

**台词（约15秒）**：
> 这是三机编队在Gazebo里飞Figure8的实况。右边是我们设计的ECBF安全层，它就像一个可插拔的模块，放在Guidance和控制器之间，当任意两机距离接近阈值时自动施加排斥约束，不需要改控制器本身。

---

### 第35页：OpenBlocks规划——A*+min-snap轨迹

**标题**：OpenBlocks：A*全局规划 + min-snap轨迹优化 + 单/多机障碍规避

**核心视觉（三步流程图+截图）**：
1. **地图表示** → Gazebo工业场景中障碍物栅格地图
2. **全局路径** → A*算法搜索无碰路径（路径折线截图）
3. **轨迹优化** → min-snap将折线平滑为连续多项式轨迹（曲线截图）

右侧：
- 单机避障：一台无人机在静态障碍场景中导航截图
- 多机协同：三机同时规避同一障碍的分布式规划结果

**台词（约15秒）**：
> 规划模块叫OpenBlocks，分三步走：先用A*在栅格地图里找一条无碰路径，再用min-snap把折线轨迹平滑成连续的多项式曲线，最后给无人机执行。单机和多机都支持，多机的规划是分布式的，每架机维护自己的规划，同时避开其他机的预测位置。

---

### 第36页：FAST-LIO感知·UE工业场景·Diff-Planner

**标题**：感知闭环：FAST-LIO · UE工业场景 · Diff-Planner局部规划

**核心视觉（三栏并排）**：
- 左栏 FAST-LIO：
  - LiDAR+IMU紧耦合估计框架说明图
  - Gazebo工业场景中建图结果截图（点云地图）
  - 标注：雷达话题/里程计话题已接入MoSim部署栈
- 中栏 UE工业场景：
  - 虚幻引擎（UE）中的工厂场景截图
  - → Gazebo导出后场景截图
  - 说明：UE提供高保真环境，导出后在Gazebo中完成飞行验证
- 右栏 Diff-Planner：
  - 局部轨迹规划框架示意
  - 动态障碍规避演示截图

**台词（约12秒）**：
> 感知这块我们用的是FAST-LIO，激光雷达加IMU紧耦合，可以在飞行中实时建图。工业场景是从虚幻引擎里导出到Gazebo的，保证环境复杂度。Diff-Planner负责局部轨迹调整，应对动态障碍。

---

### 第37页：[V6] 障碍规避+工业场景演示

**标题**：[视频] OpenBlocks障碍规避 · FAST-LIO建图 · 工业场景飞行

**视频说明**：
- **时长**：30~45秒
- **内容**：
  1. Gazebo工业场景中，单机自主规划飞行（A*+min-snap轨迹可视化）
  2. RViz中点云地图实时更新（FAST-LIO建图过程）
  3. 多机协同障碍规避一镜

**台词引导**：
> 最后看一下感知和规划的整合效果。

---

### 第38页：MoSim Studio + AI Agent

**标题**：MoSim Studio：统一管理界面 · AI Agent集成

**核心视觉（左右分栏截图）**：
- 左：MoSim Studio界面截图
  - 控制器列表管理
  - 场景配置面板
  - 一键启动仿真
  - 结果查看面板
- 右：AI Agent接入示意图
  - 用户输入自然语言指令
  - AI Agent → Codex CLI → MWORKS Syslab执行
  - 返回分析结果
  - 说明：GUI控件不支持批量分析（连滚动都受限），AI Agent解决工程效率瓶颈

**台词（约12秒）**：
> MoSim Studio是统一的管理界面，控制器选择、场景配置、结果查看都在这里。我们还接入了AI Agent，可以用自然语言驱动Syslab做批量分析，因为Syslab的GUI在批量任务上效率太低，AI Agent直接调CLI解决这个问题。

---

### 第39页：软件工程交付——WSL2部署规范·开源·二次开发

**标题**：软件工程交付：WSL2完整部署 · 开源规范 · 二次开发接口

**核心视觉（三列卡片）**：
- 卡片1：**环境可复现（WSL2）**
  - 运行基线：Ubuntu 20.04 / ROS1 Noetic / Gazebo Classic / PX4 / MAVROS / RViz
  - 为什么是WSL2而不是Docker：MWORKS和UE涉及本机GUI、授权和显卡，WSL2可完整承载
  - 一键启动脚本：`launch_mosim.sh`，拉起全栈环境
- 卡片2：**开源规范**
  - 代码结构清晰，模块化分层
  - 每个控制器独立文件夹，README说明接口
  - Git历史可追溯
- 卡片3：**二次开发接口**
  - 新控制器：实现四种接口之一，加Profile TOML即可
  - 新场景：写配置文件，不改框架
  - 新机体：替换Sysplorer模型，接口层不变

底部：赛题要求模块化可扩展 + 代码规范 — WSL2是这套链路最合适的载体

**台词（约15秒）**：
> 最后讲一下环境交付。我们的运行基线是WSL2下的Ubuntu 20.04，带ROS Noetic、Gazebo、PX4和MAVROS。选WSL2而不是Docker，是因为MWORKS和虚幻引擎都需要本机GUI和显卡，Docker装不下这套。代码结构模块化，加新控制器只需要实现标准接口加一个配置文件。

---

### 第40页：封底——完整交付物全景

**标题**：完整交付物 · 感谢评委

**核心视觉（清单卡片）**：

| 交付项         | 内容                                   |
| -------------- | -------------------------------------- |
| Sysblock模型   | 48路控制器，全图形化，含注释           |
| Sysplorer模型  | 云纵150六自由度MultiBody模型           |
| Syslab脚本     | 七场景定量分析，图表自动生成           |
| C99源码        | GenerateModelCode生成，ISO标准         |
| ROS通信层      | 自研Bridge，支持四种接口类型           |
| 仿真分析报告   | 18章·152表·379图，PDF格式             |
| 用户手册       | 环境配置·复现步骤·接口说明             |
| WSL2运行环境   | Ubuntu 20.04·ROS1 Noetic·Gazebo·PX4·MAVROS | 
| MoSim Studio   | 统一管理GUI                            |

底部大字：**每一条结论，都有原始文件可以追溯。**

**台词（约10秒）**：
> 这就是MoSim全部的工作。我们的核心主张只有一条：从MWORKS图形化建模到Gazebo运行时验证，这条链路是打通的。感谢各位评委。

---

### 第41页：封底——完整交付物全景

*— 大纲结束 —*











