# 控制器证据审计

状态：`screenshot_slots_prepared`。46 条当前图审范围已经建立原生结构图和最小闭环结果图槽位；当前没有可绑定的 G5 原生截图。本文件不把截图槽位等同于图审、最小闭环仿真、代码生成、运行时或报告性能验收。

## 当前范围

- 项目活动目录保留 48 项：47 个 MWORKS Control Profile 与 1 条 `px4ctrl` 工程/部署基线；本批原生窗口截图范围为 46 条当前 MWORKS 路线。
- 46 个 `01_图形模型.png` 和 `02_最小闭环结果原生窗口.png` 仅保留路径槽位，当前采集数均为 0。任何未来报告副本都必须与其当前 G5 packet、采集 manifest 的路径、SHA-256 和原生尺寸核验一致。
- `pid_awff_linear_eso` 是计划 Profile，`px4ctrl` 的 MWORKS 等效核待实现；两者都不伪造 MWORKS 图形截图。
- 当前目录只接收 Windows MCP 原生整窗/桌面采集的 MWORKS 截图。导出画布、报告副本、历史结果截图、裁切变形图均禁止回写。

## 审核入口

- 当前 46 条槽位、冻结模型入口、截图命名和采集状态：`Docs/报告/审计/控制器原生截图归位/CONTROLLER_SCREENSHOT_REBUILD_MANIFEST.md`。
- 归档的 92 张未绑定候选图、其旧槽位清单和旧归位记录：`Docs/报告/图/归档/控制器未绑定截图候选_20260727/UNBOUND_SCREENSHOT_ARCHIVE_MANIFEST.json`。它们只保留原路径和哈希追溯，不能回写活动目录或作为当前 G5 绑定。
- 历史 130 张导出图片及 3 份旧说明的哈希归档：`Docs/报告/图/归档/控制器旧导出资产_20260722/LEGACY_CONTROLLER_EXPORT_ARCHIVE_MANIFEST.json`。
- 历史 67 条报告资产静态审计和旧活动台账：`Docs/报告/审计/归档/控制器旧导出资产_20260722/`。
- `Results/control_platform/g5_graphical_structure_review_20260722/historical_reviews_pre_normalization_20260722/` 下的 25 份 G5 packet 是规范化前的历史结构审查记录，不是当前报告截图资产，不能复制到 `Docs/报告/图/控制器/`。`reviews/` 仅接收当前冻结模型的新 G5 packet。

下一步是在七个语义控制族中按当前来源的 Phase 1 记录确定实测胜出者，并在 G6 中实际运行最小闭环、采集结果窗口和写入可复核结果包。未取得该类证据前，不得用 46 个截图槽位宣称仿真成功。
