# 控制器证据审计

状态：`report_scope_reconciled_2026-07-31`。本文件区分 48 条目录、47 条
已完成名义运行证据和报告图片资产。46 条当前图审结构候选仍是用户审核的
历史结构图；报告目录中尚无 47 条当前源绑定的最小闭环结果图。本文件不把
结构截图、固定输入探针或单次运行记录等同于统一性能通过。

## 当前范围

- 项目活动目录保留 48 项：47 个 MWORKS Control Profile 与 1 条 `px4ctrl` 工程/部署基线；当前报告运行审计分母为 47：46 条已有 MWORKS 路线加 `px4ctrl`。`pid_awff_linear_eso` 正在运行，终端记录完成前暂不计入。
- 46 个 `01_图形模型.png` 已依据 `控制器未绑定截图候选_20260727` 的 SHA-256 和用户 2026-07-28 视觉审核结果按当前族类归位，用于展示控制器结构；它们不构成当前源仿真或性能证据。报告目录中的 47 个当前结果图槽位仍为空。
- `smc_boundary_layer` 与 `nmpc_outer` 已有隔离 Experimental 整机 FormalRunner 记录；`nmpc_outer` 通过 5 m 终端误差门，`smc_boundary_layer` 完成 50 s 但门失败。两者不进入冻结 G2/G3 分母，但可按各自状态作为整机运行证据引用。
- `px4ctrl` 保留独立 MWORKS 图形化闭环证据。G6 的 46 条结果窗口截图仍需按 `internal_fixed_input_probe` 或明确的 `whole_aircraft_minimum_closure` 类别引用，不能统一改写成整机图。
- 后续新增的当前源截图只接收 Windows MCP 原生整窗/桌面采集；导出画布、报告副本、历史结果截图、裁切变形图均禁止作为当前仿真证据回写。逐项覆盖见 `FORMAL_RUN_SCREENSHOT_COVERAGE_20260731.md`。

## 审核入口

- 当前 46 条结构槽位、冻结模型入口、截图命名和采集状态：`Docs/报告/审计/控制器原生截图归位/CONTROLLER_SCREENSHOT_REBUILD_MANIFEST.md`；它不是 47 条运行证据总账。
- 47 条运行/截图覆盖及证据类别：`Docs/报告/审计/控制器原生截图归位/FORMAL_RUN_SCREENSHOT_COVERAGE_20260731.md`。
- 归档的 92 张候选图、其旧槽位清单和旧归位记录：`Docs/报告/图/归档/控制器未绑定截图候选_20260727/UNBOUND_SCREENSHOT_ARCHIVE_MANIFEST.json`。其中 46 张 `pre_v2_unbound_structure_candidate` 已在用户审核后按当前族类复制到活动目录；46 张历史结果图继续仅用于追溯，不能作为当前 G5 或当前性能绑定。
- 历史 130 张导出图片及 3 份旧说明的哈希归档：`Docs/报告/图/归档/控制器旧导出资产_20260722/LEGACY_CONTROLLER_EXPORT_ARCHIVE_MANIFEST.json`。
- 历史 67 条报告资产静态审计和旧活动台账：`Docs/报告/审计/归档/控制器旧导出资产_20260722/`。
- `Results/control_platform/g5_graphical_structure_review_20260722/historical_reviews_pre_normalization_20260722/` 下的 25 份 G5 packet 是规范化前的历史结构审查记录，不是当前报告截图资产，不能复制到 `Docs/报告/图/控制器/`。`reviews/` 仅接收当前冻结模型的新 G5 packet。

下一步是把已有 `Results/` 原生截图按当前源哈希、运行记录和证据类别逐条绑定到报告资产目录；在此之前，不得把 47 条源截图记录直接当成 47 张报告可用的整机仿真图。
