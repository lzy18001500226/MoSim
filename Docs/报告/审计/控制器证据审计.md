# 控制器证据审计

状态：`native_structure_materialized`。46 条当前图审范围的原生结构截图已从各自 G5 packet 绑定的 Windows 原生窗口采集归位；本文件不把结构截图等同于最小闭环仿真、代码生成、运行时或报告性能验收。

## 当前范围

- 项目方案注册表保留 49 条；本批原生窗口截图范围为 46 条。
- 46 个 `01_图形模型.png` 已写入 `Docs/报告/图/控制器/` 对应路线目录。每个报告副本均与其 G5 packet、采集 manifest 的路径、SHA-256 和原生尺寸核验一致。
- `mu_synthesis`、`neural_smc` 保持实现阻塞；`px4ctrl` 为 ROS1/PX4 运行时基线，不伪造 MWORKS 图形截图。
- 当前目录只接收 Windows MCP 原生整窗/桌面采集的 MWORKS 截图。`01_图形模型.png` 已齐全；`02_最小闭环结果原生窗口.png` 仍为 0，必须由 G6 正式仿真生成。导出画布、报告副本、历史结果截图、裁切变形图均禁止回写。

## 审核入口

- 当前 46 条槽位、冻结模型入口、截图命名和采集状态：`Docs/报告/审计/控制器原生截图归位/CONTROLLER_SCREENSHOT_REBUILD_MANIFEST.md`。
- 46 条报告图与 G5 原始采集之间的逐条哈希归位记录：`Docs/报告/审计/控制器原生截图归位/NATIVE_STRUCTURE_SCREENSHOT_MATERIALIZATION.json`。
- 历史 130 张导出图片及 3 份旧说明的哈希归档：`Docs/报告/图/归档/控制器旧导出资产_20260722/LEGACY_CONTROLLER_EXPORT_ARCHIVE_MANIFEST.json`。
- 历史 67 条报告资产静态审计和旧活动台账：`Docs/报告/审计/归档/控制器旧导出资产_20260722/`。
- `Results/control_platform/g5_graphical_structure_review_20260722/historical_reviews_pre_normalization_20260722/` 下的 25 份 G5 packet 是规范化前的历史结构审查记录，不是当前报告截图资产，不能复制到 `Docs/报告/图/控制器/`。`reviews/` 仅接收当前冻结模型的新 G5 packet。

下一步是按六个名义控制族确定晋级候选，并在 G6 中实际运行最小闭环、采集结果窗口和写入可复核结果包。未取得该类证据前，不得用现有 46 张结构图宣称仿真成功。
