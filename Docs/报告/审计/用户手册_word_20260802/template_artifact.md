# 用户手册 Word 模板审计记录

- **参考模板：** `C:\Users\HP\Desktop\MoSim\Docs\报告\国赛论文模版.docx`
- **SHA-256：** `8be0bed263cd9ab10d9088855992ed4fa6805ecc03756d2d0c9e02673470d91b`
- **区段：** 1 个 A4 纵向区段，页边距为左/右 1.06 in、上/下 1.00 in。
- **保留组件：** 模板的区段属性、页眉、页脚、样式、主题、编号定义和现有包关系；生成后将模板的页眉、页脚及其关系部件按原始字节回填，避免 `python-docx` 无关重写页面家具。
- **正文槽位：** 清除示例正文，但保留 `w:sectPr`；新正文从 Markdown 标题、段落、代码块、表格和图片依次构建。
- **标题层级：** Markdown `#` 作为封面标题；`##`、`###`、`####` 分别呈现为一级、二级、三级正文标题。
- **图表：** 图题和表题保留模板中的 `图表标题` 样式；源手册的“图 8-13 / 表 6-2”编号写为“章节号固定文本 + 章节内 `SEQ 图_8` / `SEQ 表_6` 域”，以便更新域后维持分章编号。图片通过原始文件字节嵌入；输出包显式设置 `w:doNotCompressPictures` 和 `w:updateFields`。
- **表格：** Markdown 表格转换为原生 Word 表格，使用固定列宽、重复表头、单元格边距和自动行高，避免以纯文本模拟表格。
- **渲染状态：** 2026-08-02 检测到本机未安装 LibreOffice/soffice，模板 PNG 渲染不可用；以 OOXML 区段、样式、页眉页脚、图片/字段哈希检查替代。最终文档可在 Word 中打开时更新域并完成视觉排版复核。
