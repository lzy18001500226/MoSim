# 公式与推导：活动运行链与源码映射

> 证据收敛稿，2026-07-31。正文只解释当前 MWORKS 活动运行链；附录中的
> 控制理论和候选模型不代表已经通过全机验证。参数、状态和结果口径以
> `Docs/Design/报告手册交付证据总账_P0_20260731.md` 为准。

## 1. 坐标系、符号与边界

### 1.1 活动模型坐标

MWORKS 活动全机模型使用世界系 ENU 和机体系 FLU 约定：

\[
\mathcal{F}_W=(x_E,y_N,z_U), \qquad
\mathcal{F}_B=(x_F,y_L,z_U).
\]

`FormalAttitudeThrustRunnerBase` 将位置、姿态和参考在同一 MWORKS 约定下
传递。任何 NED/FRD 转换仅发生于外部运行时适配边界，不能把外部 PX4 约定
直接代入 MWORKS 内部符号。常用方向变换可以写为

\[
\mathbf p_{NED}=\begin{bmatrix}y_E\\x_N\\-z_U\end{bmatrix}, \qquad
\mathbf v_{FRD}=\begin{bmatrix}x_F\\-y_L\\-z_U\end{bmatrix},
\]

但该式是接口换算说明，不是活动 MWORKS Plant 内的一次重复动力学计算。

### 1.2 旋翼编号与旋向

活动 Profile 的 MWORKS 旋翼位置为

\[
\mathbf r_1=(0.053745,-0.053740,-0.014052)^T,\quad
\mathbf r_2=(0.053746, 0.053759,-0.014052)^T,
\]
\[
\mathbf r_3=(-0.053761,0.053760,-0.014052)^T,\quad
\mathbf r_4=(-0.053761,-0.053739,-0.014052)^T\;\mathrm{m}.
\]

旋翼指令符号和偏航反扭矩方向均为
\(\{+1,-1,+1,-1\}\)。负号是当前视觉旋翼/分配器约定的一部分，不能在
报告中改写为所有电机正转的理想矩阵。

## 2. 旋翼、电机与故障注入

### 2.1 一阶电机动态与推力

对第 \(i\) 个旋翼，活动模型使用带上升/下降时常数的一阶动态：

\[
\tau_i=
\begin{cases}
\tau_{up}, & |\omega_{cmd,i}|>|\omega_i|\\
\tau_{down}, & \text{otherwise}
\end{cases},\qquad
\dot{\omega}_i=\frac{\omega_{cmd,i}-\omega_i}{\tau_i}.
\]

未缩放推力、故障后推力和偏航反扭矩分别为

\[
T_{0,i}=C_T\omega_i^2,
\qquad T_i=\eta_{f,i}\eta_{T,i}T_{0,i},
\qquad M_{z,i}=\eta_{f,i}d_i\eta_{M,i}C_M\eta_{T,i}T_{0,i}.
\]

其中 \(\eta_{T,i}\) 为静态推力效率，\(\eta_{M,i}\) 为反扭矩效率，
\(d_i\in\{+1,-1\}\) 为偏航方向。调度故障乘子 \(\eta_{f,i}\) 只在指定旋翼且
`time >= fault_start_s` 时取 `fault_rotor_effectiveness`，其他旋翼取 1。

### 2.2 力矩与总作用量

推力在机体系沿正 z 方向；第 \(i\) 个旋翼产生

\[
\boldsymbol\tau_i=
\begin{bmatrix}
r_{y,i}T_i\\-r_{x,i}T_i\\M_{z,i}
\end{bmatrix}, \qquad
T=\sum_{i=1}^4T_i,\qquad
\boldsymbol\tau=\sum_{i=1}^4\boldsymbol\tau_i.
\]

`PhysicalWrenchAdapter` 将 \(\{0,0,T\}\) 与 \(\boldsymbol\tau\) 施加到
MultiBody 机体，并允许质量缩放与主惯量缩放作为参数失配场景输入。

## 3. 实际 X 型控制分配

### 3.1 悬停工作点与线性化斜率

`OfflineWrenchAllocator` 不使用理想化对称矩阵。它在当前 Profile 的带符号
悬停转速 \(\omega_h\) 处线性化：

\[
\Delta T_{rotor}=2C_T\omega_h\Delta\omega,
\]
\[
s_F=4\Delta T_{rotor},\quad
s_\phi=-4\Delta T_{rotor}a_y,\quad
s_\theta=4\Delta T_{rotor}a_x,\quad
s_\psi=-4\Delta T_{rotor}c_M.
\]

其中 \(a_x=|r_{1x}|\)、\(a_y=|r_{1y}|\)，当前 Profile 的悬停转速和几何
决定这些斜率。总推力增量、滚转/俯仰/偏航项为

\[
\Delta\omega_c=\frac{F_z-F_{h}}{s_F},\quad
r=\frac{\tau_x}{s_\phi},\quad
p=\frac{\tau_y}{s_\theta},\quad
y=\frac{\tau_z}{s_\psi}.
\]

### 3.2 当前活动混控器

活动分配器实际执行

\[
\begin{aligned}
\omega_{1,raw}&= \omega_h+\Delta\omega_c-y-p+r,\\
\omega_{2,raw}&=-\omega_h-\Delta\omega_c-y+p+r,\\
\omega_{3,raw}&= \omega_h+\Delta\omega_c-y+p-r,\\
\omega_{4,raw}&=-\omega_h-\Delta\omega_c-y-p-r.
\end{aligned}
\]

之后执行按旋向的限幅：1、3 号限制在 \([0,\omega_{max}]\)，2、4 号限制在
\([-\omega_{max},0]\)。因此该实现既包含几何、旋向，又包含悬停线性化与饱和，
不能以未带符号和限幅的对称分配矩阵冒充活动模型。

## 4. px4ctrl 位置/速度外环

图形类
`MoSimQuadrotorModel.Control.Implementations.Sysblocks.PX4CTRL_Original_OuterLoop_Graphical_Sysblock`
以 0.01 s 运行。令 \(\mathbf e_p=\mathbf p_r-\mathbf p\)、
\(\mathbf e_v=\mathbf v_r-\mathbf v\)，图形模型使用

\[
\mathbf a_d=\mathbf a_r+K_p\mathbf e_p+K_v\mathbf e_v+
\begin{bmatrix}0\\0\\g\end{bmatrix}.
\]

当前图形模块三个轴的增益元件均为 \(K_p=1.5\)、\(K_v=1.5\)，并以
\(g=9.80665\) 和 \(m=1.0\) 形成总推力、归一化推力及按测得偏航旋转的
滚转/俯仰命令。该式描述位置/速度外环；其输出通过 EquationBridge 与共享
姿态环、角速度环和分配器相连。它不是对 PX4/ROS C++ 部署行为的声明。

`Px4CtrlAttitudeThrustAdapter` 将当前姿态转换为四元数后送入 EquationBridge，
并把输出的期望四元数转换为 `attitude_ref`，总推力转换为相对悬停的
`collective_thrust_delta`。为了与共享 Plant 保持符号一致，适配器显式采用
`roll_mea = -attitude_mea[1]`。

## 5. 指标定义

令 \(e_k=\|\mathbf p_{r,k}-\mathbf p_k\|_2\)，样本数为 \(N\)。

\[
\mathrm{RMSE}_p=\sqrt{\frac{1}{N}\sum_{k=1}^N e_k^2},
\qquad e_T=e_N.
\]

对于具有明确稳态参考的分量，超调按指标脚本中的参考幅值归一：

\[
M_p=100\%\cdot\frac{\max_t|x(t)-x_{ss}|}{|x_{ss}-x(0)|}.
\]

当分母很小或轨迹不是一步稳态时，超调数值可很大；因此它必须与场景定义一同
读取，不能脱离场景做横向优劣结论。调节时间 \(t_s\) 是误差首次进入并保持在
脚本定义容差带内的时间；控制能量为

\[
E_u=\sum_{k=1}^{N-1}\|\mathbf u_k\|_2^2\Delta t.
\]

所有这些指标从每条有效记录的 `metrics/METRICS.json` 读取。无效记录的诊断数值
保留为诊断，不参与通过判定。

## 6. SIL 数值一致性误差

对图形基线 \(x^{(g)}\) 和生成 CFunction 闭环 \(x^{(c)}\)，同一时间栅格上定义

\[
\mathrm{RMSE}_{\Delta p}=
\sqrt{\frac1N\sum_{k=1}^N\|\mathbf p_k^{(g)}-\mathbf p_k^{(c)}\|_2^2},
\]
\[
e_{att,max}=\max_{k,j}|\theta_{k,j}^{(g)}-\theta_{k,j}^{(c)}|,
\qquad
e_{rotor,max}=\max_{k,i}|\omega_{k,i}^{(g)}-\omega_{k,i}^{(c)}|.
\]

当前门限为 \(\mathrm{RMSE}_{\Delta p}<10^{-6}\,m\)、
\(e_{att,max}<10^{-8}\,rad\)、\(e_{rotor,max}<10^{-6}\,rad/s\)。50 s SIL
的实际结果和哈希位于
`Results/control_platform/px4ctrl_codegen_sil_v1/logs/CLOSED_LOOP_SIL_RESULT.json`；
它仅证明 MWORKS 内的图形模型到 CFunction 数值等价。

## 7. 公式到代码映射表

| 公式符号 | 物理含义 | Modelica 变量名 | 所在类全名 | 文件相对路径 | 备注 |
|---|---|---|---|---|---|
| \(\mathbf r_i\) | 旋翼中心位置与编号 | `mworks_rotor_center_m[4,3]` | `MoSimQuadrotorModel.Parameters.Sunray150VirtualPx4Classic` | `Models/MoSimQuadrotorModel/Parameters/Sunray150VirtualPx4Classic.mo` | 当前 MWORKS 顺序 1..4；Gazebo 顺序单列，不能混用 |
| \(d_i\) | 指令旋向与偏航反扭矩方向 | `mworks_spin_command_sign`、`mworks_yaw_direction` | 同上 | 同上 | 两个数组均为 `{1,-1,1,-1}` |
| \(T_{0,i}=C_T\omega_i^2\) | 未缩放旋翼推力 | `nominal_thrust[i]`、`lift_coefficient`、`omega[i]` | `MoSimQuadrotorModel.Vehicle.Dynamics.RotorActuatorCore` | `Models/MoSimQuadrotorModel/Vehicle/Dynamics/RotorActuatorCore.mo` | 电机状态为带符号角速度 |
| \(\eta_{f,i}\) | 调度电机效率故障 | `fault_effectiveness[i]`、`fault_start_s`、`fault_rotor_effectiveness` | `MoSimQuadrotorModel.Vehicle.Dynamics.RotorActuatorCore` | 同上 | 指定旋翼在开始时刻后生效一次 |
| \(\boldsymbol\tau_i\) | 推力臂矩与偏航反扭矩 | `rotor_arm_moment[i,1:3]`、`yaw_reaction_moment[i]` | `MoSimQuadrotorModel.Vehicle.Dynamics.RotorActuatorCore` | 同上 | 分量为 `{r_y T,-r_x T,M_z}` |
| \(T,\boldsymbol\tau\) | 作用于刚体的合力/合矩 | `applied_force_body`、`applied_torque_body` | `MoSimQuadrotorModel.Vehicle.Dynamics.PhysicalWrenchAdapter` | `Models/MoSimQuadrotorModel/Vehicle/Dynamics/PhysicalWrenchAdapter.mo` | 机体系 `{0,0,total_thrust}` 与总矩 |
| \(\omega_h\) | 悬停线性化工作点 | `hover_speed`、`hover_collective_thrust_n` | `MoSimQuadrotorModel.Control.Allocation.OfflineWrenchAllocator` | `Models/MoSimQuadrotorModel/Control/Allocation/OfflineWrenchAllocator.mo` | 来自当前 Profile 的视觉旋翼悬停速度 |
| \(s_F,s_\phi,s_\theta,s_\psi\) | 推力/三轴力矩线性化斜率 | `collective_thrust_slope`、`roll_torque_slope`、`pitch_torque_slope`、`yaw_torque_slope` | `MoSimQuadrotorModel.Control.Allocation.OfflineWrenchAllocator` | 同上 | 真实带符号 X 型分配的前提 |
| \(\omega_{raw,1:4}\) | 实际四路分配 | `raw_rotor_command[1:4]` | `MoSimQuadrotorModel.Control.Allocation.OfflineWrenchAllocator` | 同上 | 非理想对称矩阵；包含负转旋翼 |
| \(\mathrm{sat}(\omega_i)\) | 推力/角速度限幅 | `rotor_command[1:4]`、`maximum_speed` | `MoSimQuadrotorModel.Control.Allocation.OfflineWrenchAllocator` | 同上 | 1/3 与 2/4 的限幅区间不同 |
| 速率内环滤波 | 姿态导数估计和限幅 | `body_rate_estimator`、`max_rate_term` | `MoSimQuadrotorModel.Control.Allocation.OfflineBodyRateAllocator` | `Models/MoSimQuadrotorModel/Control/Allocation/OfflineBodyRateAllocator.mo` | 共享内环，不属于 px4ctrl 外环本体 |
| \(\mathbf a_d\) | px4ctrl 位置/速度外环 | `Kp_x/y/z`、`Kv_x/y/z`、`desired_acc_x/y/z` | `MoSimQuadrotorModel.Control.Implementations.Sysblocks.PX4CTRL_Original_OuterLoop_Graphical_Sysblock` | `Models/MoSimQuadrotorModel/Control/Implementations/Sysblocks/PX4CTRL_Original_OuterLoop_Graphical_Sysblock.mo` | 0.01 s 图形 Sysblock |
| 姿态/推力接口 | 外环至统一边界 | `attitude_ref`、`collective_thrust_delta` | `MoSimQuadrotorModel.Control.Adapters.Px4CtrlAttitudeThrustAdapter` | `Models/MoSimQuadrotorModel/Control/Adapters/Px4CtrlAttitudeThrustAdapter.mo` | 含当前 Plant 的滚转符号适配 |
| 采样、滤波、故障 | 全机 Runner 合同 | `controller_sample_period_s`、`velocity_estimator`、`fault_start_s` | `MoSimQuadrotorModel.Experiment.Runners.Base.FormalAttitudeThrustRunnerBase` | `Models/MoSimQuadrotorModel/Experiment/Runners/Base/FormalAttitudeThrustRunnerBase.mo` | 100 Hz 边界、0.05 s 速度导数滤波 |

## 附录 A：理论/候选公式，不代表当前已完成验证

以下项目保留为理论背景或控制器目录候选，不能因写出方程就被描述为已通过当前
全机闭环：

1. LQR、LQI、LQG 的线性二次型和观测器设计；
2. 线性/非线性/鲁棒 MPC、iLQR、MPPI 与约束优化；
3. CBF/QP 安全滤波和分布式编队协议；
4. Fast-LIO、未知环境建图与路径规划；
5. 学习增强、残差网络和强化学习调参。

它们的任何后续结论必须新增对应源码、FormalRunner、原始结果和状态记录，并按
第 5、6 节的指标和证据链单独验收。
