# L1

Status: TYPED STATIC CORE IMPLEMENTED / runtime and fresh MWORKS promotion pending.

Layer: adaptive disturbance/model-uncertainty compensation.

Inputs: tracking error, reference, model estimate, filtered adaptation state,
bandwidth parameters, dt, reset.

Outputs: bounded compensation term or corrected reference/control command.

PX4 dependency: depends on host controller; first release should be a bounded
outer-loop augmentation.

MWORKS/codegen route: fixed adaptation law -> filter and bounds -> generated
C/C++ -> A/B tests under parameter mismatch.

Gazebo/Sunray validation: wind, load and plant-parameter perturbation cases.

Current project evidence: the project-owned
`Models/MoSimQuadrotorModel/Control/Implementations/Sysblocks/AWFF_L1ResidualControllerEquation_Sysblock.mo`
implements bounded low-pass residual compensation and has historical MWORKS
nominal, figure-8, mass, wind and rotor-effectiveness evidence indexed by
`Config/controllers/l1_residual_sysblock/default.yaml`. The nominal, figure-8,
mass and wind quality gates passed; the rotor-effectiveness case remains
`needs_iteration`.

This is evidence for an L1-inspired engineering residual compensator, not a
formal L1 adaptive controller. The bounded residual and AWFF combination is
implemented by `calculate_l1_awff_core` on the typed `ControllerInput` /
`ControllerOutput` contract and is exposed by the C ABI as controller id 7.
The bounded G10-B/D/E static gate is recorded at
`Results/control_platform/g5_wave_c_l1_awff_20260716/RUN_MANIFEST.json`.
It remains non-selectable because it has no current generated-C equivalence
package, ROS1 ExperimentProfile, or paired Factory runtime A/B acceptance.

Forbidden claims: L1 evidence must state its host controller and cannot be
reported as a standalone controller result.

Current reopen gate: restore the MWORKS transport and produce fresh graphical
MIL/code-generation equivalence for the typed core. Then use a single host
controller selected by the board, keep
the same Sunray/Gazebo/PX4/MAVROS runtime baseline, and compare L1 off/on in
one minimal disturbance or parameter-mismatch case before any broader matrix.
The run packet must record host controller, adaptation law, filter bandwidth,
compensation bounds, reset condition, safety filter interaction, and the
no-L1 baseline directory. Passing this gate means "candidate L1 contribution
observed"; it does not mean standalone controller acceptance or MWORKS/codegen
promotion.
