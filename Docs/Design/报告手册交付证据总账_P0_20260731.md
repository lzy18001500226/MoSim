# 报告、手册与交付物证据总账（P0，2026-07-31）

## 1. 用途与边界

本文是 2026-07-31 报告、用户手册、代码生成交付物和发布清单的共同
数字基线。它只整理已存在的源码、配置和结果；不修改 `Models/`、
`Config/`、`Results/`，不替代任何一次原始仿真记录。

结论的优先级为：结果状态 JSON/原始指标 > 当前配置路由表 > 当前源码
> 旧计划、截图和历史草稿。显示层（Studio、QGC、RViz、UE）不是控制器
成功判据。

2026-08-03 增补：`adaptive_mpc` 的结构修复尝试只在项目自有 Adapter 源面
增加了速度参考调理边界；最终 MWORKS reload 在既有结果查看器/MCP 会话中超时，
尚未形成最终 CheckModel、50 s 结果或终端指标。因此本账的 48、30/48、18/48、
0/48 数字不变，证据状态见
`Results/mworks_live_gate/failed18_recovery_20260803/adaptive_mpc/ADAPTIVE_MPC_RECOVERY_20260803.json`。

## 2. 当前计数总账

| 口径 | 当前值 | 直接依据 | 允许的表述 |
|---|---:|---|---|
| 控制器目录条目 | 48 | `Config/control_platform/control_scheme_catalog.json` 的 `schemes` | 设计目录条目数，不表示均已实现或通过 |
| 已实现 MWORKS 控制模块 | 46 | `Config/control_platform/current_model_entry_map.json` 中 `implementation_status=implemented` 的 46 条，且每条 `current_model_file` 存在 | 有 Modelica 实现，不表示性能通过 |
| Studio 可手动打开的正式全机入口 | 48 | `Config/control_platform/model_studio_task_routes_v1.toml` 中 `available=true` 的 48 条；每条 `runner_file` 存在 | 可写配置并打开，不表示自动仿真或性能通过 |
| Formal 目录候选 `.mo` | 53 | `Models/MoSimQuadrotorModel/Experiment/Runners/Formal/` 的 54 个 `.mo` 去掉 `package.mo`，即 53 个 `*FormalRunner.mo` | 文件候选数，不等同 Studio 入口数，也不等同 48 条目录或冻结 G3 的 48 行执行记录 |
| 当前报告运行证据审计分母 | 48 | 46 条已有 MWORKS Profile 的 G6 记录、1 条 `px4ctrl` MWORKS 基线和 1 条 `pid_awff_linear_eso` 50 s 负证据 | 有可追溯运行证据，不表示 48 条性能通过或 48 条整机闭环 |
| `Results/` 源结果窗口覆盖 | 48/48 控制器 | 46 条 G6 结果截图、`px4ctrl` 结果窗口截图和 PID-AWFF-LINEAR-ESO 的 Runner/Result Viewer 截图均可追溯 | 控制器记录均有可追溯结果窗口；G6 多数类别为 `internal_fixed_input_probe` |
| 报告目录控制器结构图 | 48/48 | 48 张结构图均已归位并作为报告结构展示资产管理；采集来源细分仅在截图重建 provenance 清单中保留 | 结构或接口展示，不表示当前源性能、整机仿真或运行时通过 |
| 报告目录当前源绑定的逐控制器结果图 | 0/48 | 尚未完成逐条复制、哈希、运行记录和证据类别绑定 | 尚不能写成 48 张可直接入正文的整机仿真图 |
| G2 同条件 ClimbPath 50 s 通过 | 17/48 | `Results/control_platform/phase2_full_48_climbpath/g3_repair/G3_STATUS.json` 的 `g2_baseline_passed_count` | 第一轮冻结筛查通过 |
| 当前目录 ClimbPath 对账通过 | 30/48 | `G3_CATALOG_48_CURRENT_STATUS.json` 的 `summary.passed_count` | 当前固定目录 48 条名义状态 |
| 当前目录 ClimbPath 对账失败 | 18/48 | 同一文件的 `summary.failed_count` | 已完成失败；另有 0/48 `not_run` |
| 冻结历史 G3 通过 | 28/48 | `G3_STATUS.json` 的 `effective_passed_count` | 历史执行快照与既有 28 条图集来源，不是当前目录状态 |
| 冻结历史 G3 失败 | 20/48 | 同一文件的 `effective_failed_count` | 历史失败分类必须保留 |

`Formal` 文件目录中没有作为 Studio 路由暴露的七个文件为
`FaultCompensationFormalRunner.mo`、`FixedAwffL1IndiFormalRunner.mo`、
`FixedAwffL1ResidualFormalRunner.mo`、`FixedLinearMpcL1IndiFormalRunner.mo`、
`ImprovedPidFormalRunner.mo`、`OfficialPidYawAuthorityMappedFormalRunner.mo`、
`OfficialPidYawCorrectedFormalRunner.mo`。另外，`smc_boundary_layer` 和
`nmpc_outer` 两条可用 Experimental 路由的 Runner 文件位于
`Experiment/Runners/Experimental/`，不计入 `Formal/` 目录的 53 个文件候选。
因此，53 个 Formal 文件候选、2 个 Experimental Runner 文件和 48 条 Studio
路由属于不同的文件系统统计层，不能把差额隐含为性能结论。

路由表本身共 48 条 `[[route]]`，且 48 条均为 `available = true`，每条都有
`runner_file`、Runner 类名和输出边界。`pid_awff_linear_eso` 已登记为 Formal
Runner，并保留其 50 s 终端误差 3412.36 m 的负性能证据；`smc_boundary_layer`
和 `nmpc_outer` 保留 Experimental 身份。`available = true` 只表示路由可以写入
配置并打开，不表示性能通过或已完成七场景验证。

46 条实现的逐项 `current_model_file` 静态清单见
`Docs/Design/控制器实现和Studio入口计数依据_P0_20260731.md`；48 条入口的逐项
Runner 与文件索引见 `RELEASE_CHECKLIST.md` 第 4 节。

与“全部位于 `Models/.../Control/`”的简写不同，当前 46 条映射中的
`fixed_awff_pid`、`fixed_awff_l1_residual`、`fixed_awff_l1_indi`、
`fixed_linear_mpc_l1_indi` 和 `fixed_qp_nmpc_l1_indi_cbf` 的实际
`current_model_file` 位于 `Models/MoSimQuadrotorModel/Experiment/Templates/IntegratedChains/`。
数量仍为 46；正文以实际路径映射而非目录简写为准。

### 2.1 运行与截图分母边界

48 条运行审计分母只表示当前已有的 46 条 MWORKS Profile、1 条
`px4ctrl` 独立基线和 `pid_awff_linear_eso` 的 50 s 运行均有可追溯证据。
PID-AWFF-LINEAR-ESO 的终端误差为 `3412.359226529184 m`，是性能失败的负
证据。当前固定目录的同条件状态以 `G3_CATALOG_48_CURRENT_STATUS.json` 为准：
30 通过、18 完成失败、0 未运行。这个分母不表示每条都达到了
`terminal_position_error < 5 m`，也不替换冻结历史 G3 的 28/20 快照。
`smc_boundary_layer` 与 `nmpc_outer` 的 Experimental 整机 FormalRunner
记录在冻结历史 G3 外单独保留，且已作为当前目录补充记录纳入对账；前者失败，
后者通过 5 m 终端误差门。

截图材料还要再分两层：报告目录中 48 张结构图已经归位，当前正文统一按 48 张
完整结构展示资产表述；原始采集来源差异仅在截图重建 provenance 清单中保留，
不再作为报告覆盖分母。逐控制器当前源绑定的结果图仍为 0/48。`Results/` 中已有的原生
结果窗口截图可以回溯，但 G6 的 46 条结果截图不能因存在就统一改写为整机图，
必须按其 `internal_fixed_input_probe` 或 `whole_aircraft_minimum_closure`
证据类别使用。逐项清单见
`Docs/报告/审计/控制器原生截图归位/FORMAL_RUN_SCREENSHOT_COVERAGE_20260731.md`。

本次 `adaptive_mpc` recovery 的哨兵和后台截图只证明当前既有窗口无明确登录、
授权或错误弹窗；它们不是新的结果窗口、整机仿真图或性能记录，不增加报告目录
当前源绑定结果图的 0/48 覆盖数。

## 3. 冻结历史 G3 有效失败分类

来源：`Results/control_platform/phase2_full_48_climbpath/g3_repair/G3_STATUS.json`。
下表是冻结历史 20 条失败的分类。当前目录的 18 条完成失败由 9 条终端误差超限、
8 条仿真超时和 1 条 `adaptive_mpc` 仿真 API 失败组成，逐条状态见
`G3_CATALOG_48_CURRENT_STATUS.json`。其中 `pole_placement_luenberger` 的历史
CheckModel 授权记录保留在下表，但当前目录已由 2026-08-02 的完整 50 s 重跑
覆盖为终端误差 `402.1409427651827 m` 的性能失败。

| 状态字段 | 数量 | 控制器 ID | 当前处置含义 |
|---|---:|---|---|
| `terminal_position_error_exceeds_5m` | 9 | `awff`、`fault_compensation`、`fopid`、`hinf_hover_wrench`、`indi`、`l_1`、`linear_mpc_rotor`、`mrac`、`official_pid_yaw_corrected` | 仿真完成，但终端位置误差超过 5 m 门限 |
| `simulation_timeout` | 8 | `cascade_pid`、`fuzzy_pid`、`gain_scheduled_pid`、`linear_mpc`、`neural_pid`、`rl_gain_scheduler`、`super_twisting_smc`、`trained_neural_residual` | 仿真执行超时，不能写作“未实现” |
| `simulate_failed` | 2 | `adaptive_mpc`、`improved_pid` | 仿真 API 返回失败 |
| `check_model_failed` | 1 | `pole_placement_luenberger` | 原生模型检查失败 |

## 4. 七场景与灵敏度的实际状态

### 4.1 七场景

`Results/control_platform/seven_scenario_ab_v2/SCENARIO_RMSE_MATRIX.pending_syslab.json`
的 `row_count` 为 14：Official PID 和 px4ctrl 各 7 条场景记录。其实际状态
为 **12 条 `valid`、2 条 `invalid`**，两条无效记录均为
`motor_efficiency_fault`。因此“14 条有效记录”不是当前仓库事实，后续正文
统一写为“14 条总记录，其中 12 条有效，2 条保留为无效负样本”。

`official_pid/spiral/RUN_RECORD.json` 的主记录是 `valid`；其
`native_result_retry_01` 是该主记录关联的原生结果重试工件，不构成第 15 条
场景记录，也不作为独立试验计数。

### 4.2 灵敏度

实际 24 条总账为
`Results/control_platform/sensitivity_analysis_long_v1/SENSITIVITY_LONG_V1_CLOSEOUT.json`：
17 条通过、3 条物理门限失败、4 条执行阻塞。这里的 24 条由风扰、参数失配和
电机效率三组各 8 条组成。

`Results/control_platform/sensitivity_wind_v1/SENSITIVITY_BATCH_STATUS.json`
确实存在，但它仅记录 8 条风扰案例且全部通过。它的 `claim_boundary` 提到
24 条计划网格，不是 24 条执行结果。后续正文不得把该文件误列为 24 条的状态
总账。

## 5. px4ctrl 图形模型、生成 C 与 SIL

| 项目 | 已有证据 | 可得出的结论 | 不可得出的结论 |
|---|---|---|---|
| 图形模型 | `Models/MoSimQuadrotorModel/Control/Implementations/Sysblocks/PX4CTRL_Original_OuterLoop_Graphical_Sysblock.mo` | 位置/速度外环以 0.01 s 图形 Sysblock 实现，并接入共享 MWORKS 内环 | 不等同 PX4/ROS C++ 部署 |
| 生成 C | `src/control/codegen/px4ctrl/` 与 `Results/control_platform/px4ctrl_codegen_sil_v1/generate_model_code_result.json` | MWORKS GenerateModelCode 产物已留存 | 不表示任意平台均无需重编译即可运行 |
| 原生编译 | `Results/control_platform/px4ctrl_codegen_sil_v1/runtime_compile_smoke.json` 与 `native/libpx4ctrl_graphical_generated.so` | 当时 WSL Linux 共享库构建与符号检查通过 | 不表示 Windows DLL 已证明 |
| 50 s 整机 SIL | `Results/control_platform/px4ctrl_codegen_sil_v1/logs/CLOSED_LOOP_SIL_RESULT.json` | 同一 Dassl、50 s、5001 样本下图形基线与 CFunction 生成代码通过门限 | 不表示已接入 Gazebo、PX4、ROS、MAVROS 或实机 |
| C99 源本地 Gazebo 运行 | `Results/sunray_ros1/sunray_ros1_graphical_c99_takeoff_hover_land_20260731_002/` | `graphical_c99` / `graphical_px4ctrl_c99` 后端经项目本地 `src` 工作区接入 MAVROS、PX4 与 Sunray/Gazebo，完成解锁、起飞、悬停、降落与解除解锁 | 仅覆盖 px4ctrl 这一条 C99 路线，不表示全部控制器均已部署或严格性能门已通过 |
| C99 Gazebo 风扰注入 | `Results/sunray_ros1/sunray_ros1_graphical_c99_wind_hover_20260801_002/` | Gazebo 对机体施加 `0.8 N`、`35 deg`、约 `8.10 s` 风扰，81/81 次服务调用确认，任务生命周期完成 | 注入确认与生命周期不等同严格同参抗扰性能对比 |
| C99 Gazebo 电机效率故障恢复 | `Results/sunray_ros1/sunray_ros1_graphical_c99_motor_fault_recovery_20260731_002/` | 转子 1 效率降至 `0.85`，观测到 27 个故障样本，恢复额定效率命令已确认，px4ctrl 始终保持唯一控制权 | 不表示已证明故障容错性能或任意故障幅值下的稳定性 |

该 SIL 文件中的聚合误差为：位置 RMSE `1.1481051588325626e-13 m`，
姿态最大绝对差 `3.2070318622956506e-12 rad`，旋翼指令最大绝对差
`7.354117315117037e-11 rad/s`；分别低于 `1e-6 m`、`1e-8 rad`、
`1e-6 rad/s` 门限。源图形模型哈希为
`86dc0be78490e87ed7bf487406b49531330ee5379d042cf0e44c8a4b718307d6`，
生成 C 源哈希为
`d755e236bdcc9045faa25e4e3055ed7ac3791452960c0d30a41bc0dcada0fb39`。

三次 Gazebo 运行均记录控制器路径为
`src/control/runtime_adapters/px4ctrl`，后端为 `graphical_px4ctrl_c99`；
运行时工作区的 `px4ctrl`、FAST-LIO、Sunray 与 Livox 插件均链接到项目
`src/`。迁移后的静态检查还验证了 9 个迁移组件、17 个活动消费者没有直接
`References/` 源码路径。名义运行使用 FAST-LIO `0.02 m` 体素参数，风扰和
电机效率运行使用 `0.5 m`；因此三项是各自配置下的运行、注入和恢复证据，
不得改写为严格同传感器参数的性能 A/B 对比。

## 6. 当前模型与参数边界

云纵150虚拟参数 Profile 为
`Models/MoSimQuadrotorModel/Parameters/Sunray150VirtualPx4Classic.mo`。
其自身的 `claim_boundary` 明确为虚拟仿真种子，而不是实机系统辨识真值。后续
文字采用以下分层：几何来自实物测量/装配关系；质量来自称重与部件累加；惯量为
CAD 估算和工程经验种子；推力/反扭矩系数为厂商数据和文献典型种子；电机动态为
一阶惯性种子。每项可在后续保留接口的前提下替换。

## 7. 已校验的引用路径

本表中的路径均于 2026-07-31 在仓库根目录静态存在性检查通过。

| 路径 | 用途 |
|---|---|
| `Results/control_platform/phase2_full_48_climbpath/g3_repair/G3_CATALOG_48_CURRENT_STATUS.json` | 当前固定目录 48 条的 30/48、18/48、0/48 对账 |
| `Results/control_platform/phase2_full_48_climbpath/g3_repair/G3_STATUS.json` | 冻结历史 G3 的 48、17/48、28/48、20/48 与失败分类 |
| `Results/control_platform/seven_scenario_ab_v2/SCENARIO_RMSE_MATRIX.pending_syslab.json` | 七场景行数与有效性 |
| `Results/control_platform/sensitivity_analysis_long_v1/SENSITIVITY_LONG_V1_CLOSEOUT.json` | 24 条灵敏度实际总账 |
| `Results/control_platform/sensitivity_wind_v1/SENSITIVITY_BATCH_STATUS.json` | 8 条风扰批次，不作为 24 条总账 |
| `Config/control_platform/seven_scenario_experiment_profiles_v2.json` | 七场景 Profile 合同 |
| `Config/control_platform/controller_route_interface_matrix.json` | 路由输出边界合同 |
| `Config/control_platform/model_studio_task_routes_v1.toml` | Studio 正式任务路由权威源 |
| `src/control/codegen/px4ctrl/` | 可构建的生成 C 交付目录 |
| `Results/sunray_ros1/sunray_ros1_graphical_c99_takeoff_hover_land_20260731_002/RUN_MANIFEST.json` | C99 源本地 Gazebo 名义运行配置、后端与状态源 |
| `Results/sunray_ros1/sunray_ros1_graphical_c99_takeoff_hover_land_20260731_002/PX4CTRL_BASIC_MISSION_METRICS.json` | 起飞、悬停、降落、解除解锁生命周期与原始指标 |
| `Results/sunray_ros1/sunray_ros1_graphical_c99_wind_hover_20260801_002/WIND_INJECTION_EVIDENCE.json` | Gazebo 风扰服务确认与风力参数 |
| `Results/sunray_ros1/sunray_ros1_graphical_c99_motor_fault_recovery_20260731_002/MOTOR_EFFICIENCY_INJECTION_EVIDENCE.json` | 转子效率故障、观测和恢复确认 |
| `Results/static_audits/local_source_activation_20260801/LOCAL_SOURCE_ACTIVATION_FINAL.json` | 本地 `src` 活动源码路径检查 |
| `Results/planning/three_uav_openblocks_gray_completion_20260730/RUN_RECORD.json` | 历史 Linear-MPC 三机 OpenBlocks 记录；不可作为当前源码或 PX4CTRL 证据 |

## 8. 待补证据与不得升级的结论

1. 当前目录的 18 条完成失败和冻结历史 G3 的 20 条失败均不能因目录或图形模块存在而写作通过。
2. 七场景仅覆盖两条代表控制器；它是深度优先对比，不证明其余控制器的鲁棒性。
3. 固定三角队形 Figure8 仅是离线参考接入证据，不证明在线自主避障、可重构
   编队或分布式优化。该记录已通过（`px4ctrl_three_uav_figure8_v1/RUN_RECORD.json`，
   `status=passed`，队形误差 RMSE 2.2855e-13 m），但队形由参考直接给定。
4. 三机 ECBF 安全参考调节为 `blocked`、`accepted=false`
   （`Results/planning/three_uav_openblocks_px4ctrl_ecbf_safety_20260731/metrics/mworks_px4ctrl_ecbf_safety_full_304p84s.json`）。
   八门中三门未过：`safety_intervened`、`tracking_diagnostic`、
   `clearance_proxy_nonnegative_diagnostic`。`intervention_sample_count=0`，
   ECBF 全程零触发，机间距门通过是标称参考自带 1.52 m 间距所致。避障能力属
   **未验证**，不得写作已验证或"效果有限"。同目录 `smoke_15s` 与
   `partial_failure_246s` 无 `status`/`accepted` 字段，为过程工件，不计独立样本。
5. `Results/planning/three_uav_openblocks_gray_completion_20260730/` 保留为历史
   MWORKS Linear-MPC 三机 OpenBlocks 记录：`RUN_RECORD.json` 记为
   `formation_tracking_status=accepted_with_reduced_clearance_margin`、
   `formation_tracking_accepted=true`；306 个样本无 NaN/Inf，最小实际机间距
   `1.0908339 m`、净空下界 `0.0941821 m`。它未保持原始 `0.35 m` 规划余量，且
   整机模型、导航显示和飞行器模型哈希已与当前源码漂移。因此只能作为离线全局 A*
   / EGO 平滑参考由三套 Linear-MPC 整机闭环跟踪的历史接受记录；不得升级为当前
   源码、PX4CTRL、在线重规划、墙体碰撞接触或避障成功。它不覆盖
   `three_uav_openblocks_px4ctrl_completion_20260731/` 的独立 `blocked` 负证据。
6. 生成 C 已完成 MWORKS 内的整机 SIL，并已有一条 `graphical_c99` 源本地
   Gazebo/PX4 运行证据链，覆盖名义起飞-悬停-降落、风扰注入和转子效率故障恢复。
   该记录不扩展为其他控制器部署、严格同参性能对比或完整故障容错结论。
7. 灵敏度只代表已执行网格；4 条执行阻塞和 3 条物理门限失败均须保留。
8. `Docs/Workflows/MoSim_证据总账_20260731.md` 是工作区中未跟踪的历史草稿，
   未纳入本批次发布源，也未修改；其中若保留旧数字或草案措辞，不得引用为当前结论。
