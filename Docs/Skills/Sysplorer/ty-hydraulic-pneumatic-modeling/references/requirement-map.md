# 需求映射

本文件用于把用户自然语言需求快速归类为可执行的液压、热液压、气动任务入口，并标记默认路径与必须确认项。

| 用户说法 | 任务判断 | 默认系统类型 | 推荐路径 | 首轮必须确认 |
|---|---|---|---|---|
| 帮我在 Sysplorer 里搭一个阀控缸系统 | 新建模型 | 液压 | `workflows/build-from-scenario.md` | 缸类型、阀中位机能、压力源、回油路径 |
| 生成泵阀回路并跑位移 / 压力曲线 | 新建模型 + 结果复核 | 液压 | `workflows/build-from-scenario.md` | 泵类型、阀型、负载、目标变量 |
| 做一个有温升或换热的液压系统 | 新建模型 | 热液压 | `workflows/build-from-scenario.md` | 是否需要 `TYThermalHydraulics`、热边界、介质与温度范围 |
| 搭一个气动回路 / 气缸回路 | 新建模型 | 气动 | `workflows/build-from-scenario.md` | 气源、排气边界、气缸类型、阀型 |
| 修一下这个模型，图里没有连线 | 修复模型 | 继承现有模型 | `workflows/repair-existing-model.md` | 是否缺 `Placement` / `Line` / `Diagram` 注解 |
| 导图里全是红色虚线 / 线条乱 | 修复模型 | 继承现有模型 | `workflows/repair-existing-model.md` | connector 放置、端口锚点、连线注解、图层分离 |
| 这个模型能跑，但方向不对 / 没动作 | 修复模型 + 结果复核 | 按模型判断 | `workflows/repair-existing-model.md` | 介质、边界、阀口语义、负载方向、腔体连接方向 |
| 帮我验证一下官方样例 / 企业样例 | 样例验证 | 按样例判断 | `workflows/verify-example.md` | 是否允许只验证样例、是否需要二次改造 |
| 输出压力、流量、位移、温度曲线 | 结果复核 | 按模型判断 | 对应 workflow + `references/validation-rules.md` | 目标变量名、结果时间窗、通过标准 |
| 这个库怎么用 / 有什么示例 / 为什么跑不起来 | 知识增强 + 排障 | 按问题判断 | 先读 `references/manual-knowledge.md` | 具体库名、依赖介质库、Sysplorer 版本、是否真实落地 |

## 任务分类规则

1. 用户要求“新建一个系统”“帮我搭一个回路”“从零建模”时，默认归为新建模型。
2. 用户提供现有模型、报错、导图异常、无连线、变形图元时，默认归为修复模型。
3. 用户明确提到“样例”“例子”“官方案例”“企业案例”并要求真实执行时，归为样例验证。
4. 用户只要求读取、判断或复核结果曲线时，归为结果复核，但若当前没有可用模型，则回退为新建或修复路径。

## 必须标记为待确认的情况

- 4/3 阀未说明中位机能。
- 单作用缸未说明是否弹簧返回。
- 泵未说明定量泵还是变量泵，但该差异会影响性能判断。
- 问题已涉及温度、焓流、换热或热边界，但尚未确认是否切换到热液压库。
- 气动系统未说明气源与排气边界。
- 用户要求“修模型”，但没有提供模型路径、模型名或可操作对象。
