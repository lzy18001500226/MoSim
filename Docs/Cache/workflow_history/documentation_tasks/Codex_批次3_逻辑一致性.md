# Codex 执行指令：批次3 —— 逻辑一致性修复

**目标文件**：`Docs/报告/仿真分析报告_正文骨架.md`
**预计工作量**：1.5小时
**风险等级**：低（格式规范化，不涉及结构调整）

---

## 背景说明

当前文档存在以下逻辑一致性问题：

1. **术语不统一**："官方PID"/"官方 PID"/"Official PID"混用
2. **章节引用陈旧**：部分"第X章"引用未随批次1调整而更新
3. **占位标记不规范**：图占位/表占位/待填/待跑格式不一致

本批次执行全局规范化修复。

---

## 任务1：统一 Official PID 术语

### 1.1 搜索并替换

**规则**：全文统一使用 `Official PID`（首字母大写，无空格变体）

**执行替换**（按顺序，避免重复替换）：

1. `官方PID` → `Official PID`（全局替换）
2. `官方 PID` → `Official PID`（全局替换，注意中间有空格）
3. `official PID` → `Official PID`（全局替换，修正小写）
4. `official pid` → `Official PID`（全局替换，修正全小写）

**例外情况**（不替换）：
- 代码块中的变量名（例如 `official_pid_controller`）保持原样
- 文件路径中的命名（例如 `OfficialPID.mo`）保持原样
- 已经是 `Official PID` 的内容跳过

### 1.2 验证

**搜索**：`官方`（确认无残留中文"官方"）
**搜索**：`official`（确认仅出现在代码/路径中，正文中无小写official）

---

## 任务2：章节交叉引用核对

### 2.1 核对规则

批次1已将章节结构调整为16章，部分"第X章"引用可能遗漏更新。本任务逐一核对。

### 2.2 搜索并验证

**执行全局搜索**（每个搜索词单独执行，逐一人工判断）：

| 搜索词 | 预期含义（批次1后） | 检查要点 |
|--------|-------------------|---------|
| `第五章` | Official PID基线（正确）| 确认不再提及px4ctrl |
| `第六章` | 现代控制（正确）| 无需修改 |
| `第七章` | 智能增强（正确）| 无需修改 |
| `第八章` | px4ctrl工程控制器（新增，正确）| 确认引用准确 |
| `第九章` | 实验设计（原第八章，正确）| 确认引用已更新 |
| `第十章` | 性能对比（原第九章，正确）| 确认引用已更新 |
| `第十一章` | 灵敏度/编队（原第十章，正确）| 确认引用已更新 |
| `第十二章` | 联合仿真（原第十一章，正确）| 确认引用已更新 |
| `第十三章` | 代码生成（原第十二章，正确）| 确认引用已更新 |
| `第十四章` | 部署反馈（原第十三章，正确）| 确认引用已更新 |
| `第十五章` | AI工具链（原第十四章，正确）| 确认引用已更新 |
| `第十六章` | 总结展望（原第十五章，正确）| 确认引用已更新 |

### 2.3 常见错误模式及修复

**错误示例1**：
```
本章结果在第八章进行横向对比。
```
**判断**：如果"第八章"指性能对比，应改为"第十章"；如果指px4ctrl介绍，保持"第八章"。

**错误示例2**：
```
代码生成流程见第十二章。
```
**修复**：`第十二章` → `第十三章`

**错误示例3**：
```
AI Agent工具链在第十四章详述。
```
**修复**：`第十四章` → `第十五章`

### 2.4 特殊检查：小节引用

**搜索**：`§8.`（确认是否遗漏更新为§9.）
**搜索**：`§9.`（确认是否遗漏更新为§10.）
**搜索**：`§10.`（确认是否遗漏更新为§11.）

**规则**：小节引用的章节号必须与小节所在章节一致。例如：
- `§9.1` 必须在第九章中，不能出现在其他章
- 如果某处引用 `§8.2`，需确认是指新第八章（px4ctrl）还是原第八章（现为第九章）

---

## 任务3：占位标记格式统一

### 3.1 图占位统一格式

**标准格式**：

```markdown
> **图占位**
> - 内容：<简要描述图的内容和关键元素>
> - 来源候选：<数据来源路径或生成方法>
> - 关键要素：<图中必须包含的标注、曲线、数值等>

![图X-Y-<标题>](./figures/figX-Y.png)
```

**执行替换**：

1. **搜索**：`> **图占位：`（旧格式，冒号后直接描述）
   **替换为**：
   ```markdown
   > **图占位**
   > - 内容：<提取原描述>
   > - 来源候选：<待补充>
   > - 关键要素：<待补充>
   ```

2. **搜索**：`【图占位】`（方括号格式）
   **替换为**：`> **图占位**`

3. **搜索**：`图占位（`（中文括号格式）
   **替换为**：`> **图占位**`

### 3.2 表占位统一格式

**标准格式**：

```markdown
> **表占位**
> - 内容：<表格描述>
> - 数据来源：<路径或生成方法>

**表X-Y：<标题>**
```

**执行替换**：

1. **搜索**：`【表占位】`
   **替换为**：`> **表占位**`

2. **搜索**：`表占位：`
   **替换为**：`> **表占位**\n> - 内容：`

### 3.3 待填/待跑/待写标记统一格式

**标准格式**：`**【待X】**`（加粗+中文方括号）

**执行替换**：

1. `待填` → `**【待填】**`
2. `待跑` → `**【待跑】**`
3. `待写` → `**【待写】**`
4. `【待补充】` → `**【待填】**`
5. `TODO:` → `**【待写】**`
6. `TBD` → `**【待填】**`

**例外情况**：
- 代码注释中的TODO保持原样
- 已经是 `**【待X】**` 格式的跳过

### 3.4 验证占位标记完整性

**执行搜索**（确认无遗漏格式）：

- `图占位`（确认全部符合标准格式）
- `表占位`（确认全部符合标准格式）
- `待填`（确认全部加粗+方括号）
- `待跑`（确认全部加粗+方括号）
- `待写`（确认全部加粗+方括号）

---

## 任务4：图表编号连续性检查

### 4.1 检查规则

每章的图表编号应连续递增，无跳号、无重复。

### 4.2 执行检查

**搜索模式**（逐章执行）：

- `fig1-`（第一章图编号）→ 确认存在 fig1-1, fig1-2, ... 无跳号
- `fig2-`（第二章图编号）→ 确认连续
- `fig3-`（第三章图编号）→ 确认连续
- ...（逐章检查至 fig16-）

**搜索模式**（表编号）：

- `表1-`（第一章表编号）→ 确认连续
- `表2-`（第二章表编号）→ 确认连续
- ...（逐章检查至 表16-）

### 4.3 常见错误及修复

**错误示例1**：
```
图9-1
图9-3  <-- 缺少 fig9-2
图9-4
```
**修复**：重新编号为 fig9-1, fig9-2, fig9-3

**错误示例2**：
```
图10-5
图10-5  <-- 重复
图10-6
```
**修复**：第二个 fig10-5 改为 fig10-6，后续顺延

---

## 任务5：Markdown语法检查

### 5.1 标题层级检查

**搜索**：`## #`（错误的标题格式，`##` 后多余 `#`）
**搜索**：`###  `（三个井号后有两个空格，应为一个）
**搜索**：`####`（确认四级标题仅在必要时使用，不应出现五级标题 `#####`）

**规则**：
- 章标题：`## 第X章 <标题>`
- 节标题：`### X.Y <标题>`
- 小节标题：`#### X.Y.Z <标题>`（仅在必要时使用）
- 禁止五级标题 `#####`

### 5.2 列表缩进检查

**搜索**：`- **` 后紧跟列表项（确认缩进一致）

**标准格式**：
```markdown
- **第一项**：描述
- **第二项**：描述
  - 子项1
  - 子项2
```

**错误格式**：
```markdown
-**第一项**：描述  <-- 缺少空格
 - **第二项**：描述  <-- 缩进不一致
```

### 5.3 代码块检查

**搜索**：` ``` `（代码块标记）

**规则**：
- 开头必须指定语言：` ```bash `、` ```python `、` ```modelica `
- 结尾必须闭合：` ``` `
- 代码块前后各空一行（Markdown渲染最佳实践）

**执行检查**：
- 搜索 ` ``` `，确认每个开头都有对应的结尾
- 确认每个 ` ``` ` 开头后紧跟语言标识

---

## 任务6：数值单位规范

### 6.1 单位格式统一

**规则**：数值与单位之间加空格（符合国际单位制书写规范）

**执行替换**（常见单位）：

- `5m` → `5 m`
- `10s` → `10 s`
- `0.75N` → `0.75 N`
- `15°` → `15°`（度数符号例外，不加空格）
- `50Hz` → `50 Hz`
- `3kg` → `3 kg`

**例外情况**：
- 代码中的变量名（例如 `error_5m`）保持原样
- 文件名（例如 `ClimbPath50s`）保持原样

### 6.2 数值精度统一

**规则**：
- 位置误差：保留1位小数（例如 `3.1 m`）
- 角度：保留1位小数（例如 `15.2°`）
- 百分比：保留整数或1位小数（例如 `95%` 或 `95.5%`）
- 时间：保留1位小数（例如 `50.0 s`）

**执行检查**（搜索以下模式，确认精度一致）：
- `误差.*m`（位置误差表述）
- `角度.*°`（角度表述）
- `\d+\.\d+%`（百分比表述）

---

## 完成后总验证清单

执行以下搜索，确认无遗漏：

| 检查项 | 搜索词 | 预期结果 |
|--------|--------|---------|
| 术语统一 | `官方` | 0条结果 |
| 术语统一 | `official pid` | 0条结果（除代码块） |
| 章节引用 | `第八章` | 全部指px4ctrl或已更新 |
| 章节引用 | `第九章` | 全部指实验设计或已更新 |
| 占位格式 | `图占位` | 全部符合标准格式 |
| 占位格式 | `【待` | 全部加粗 |
| Markdown | `## #` | 0条结果 |
| 单位格式 | `\dm\b` | 改为 ` m` |
| 单位格式 | `\ds\b` | 改为 ` s` |

---

## 注意事项

1. **批量替换前先备份**：本批次涉及大量全局替换，操作前确认文件已git commit
2. **替换后逐一验证**：不要盲目全局替换，每个替换结果需人工确认上下文合理
3. **不修改代码块内容**：代码、文件路径、变量名中的命名保持原样
4. **不修改数值**：只修改格式（加空格），不修改数值本身
5. **操作完成后不要自动commit**，等待人工review
6. **如遇无法判断的情况，保留原文并在文档末尾列出"待人工确认项"**
