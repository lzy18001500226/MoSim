# MoSim Visible Dispatch Adapter

> Archived legacy workflow. Visible-department dispatch is not active MoSim
> workflow after the single-thread reset.

The full historical document moved to:

```text
Docs/Cache/agent_legacy/legacy_workflows_20260624/mosim_visible_dispatch_adapter.md
```

Use current domain workflows directly instead of visible-thread dispatch.
