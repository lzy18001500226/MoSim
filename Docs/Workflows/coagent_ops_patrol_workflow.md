# legacy ops patrol Patrol And Recovery Workflow

> Archived legacy workflow. legacy ops patrol patrol, visible-thread recovery,
> bounded dispatch, R1/R2/R3 failover, and dispatch SLOs are not active MoSim
> workflow after the single-thread reset.

The full historical document moved to:

```text
Docs/Cache/agent_legacy/legacy_workflows_20260624/coagent_ops_patrol_workflow.md
```

Current operations start from:

```text
Docs/Workflows/mainline_operations_board.md
Docs/Workflows/single_thread_operating_model.md
```
