# Agent Task Ledger

> Archived legacy ledger. Do not load for ordinary MoSim startup or active
> task selection.

The full historical ledger moved to:

```text
Docs/Cache/agent_legacy/legacy_workflows_20260624/agent_task_ledger.md
```

Use it only for explicit historical trace-back, packet audit, or legacy
cleanup. There is no shared current task state: each conversation's latest
direct user instruction and task-local evidence define its own work.
