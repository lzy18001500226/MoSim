# 模型库交付模板

## 1. 任务概述

- 目标对象：
- 任务类型：
- 目标领域：
- 交付粒度：

## 2. 本轮实际改动

- 关键文件或关键结构改动：
- 是否涉及顶层 package：
- 是否涉及 `Interfaces` / `Utilities` / `Sources` / `Sensors`：
- 是否涉及 `Examples` / `Tests`：

## 3. 关键结构与归位策略

## 4. Examples / Tests 与文档状态

## 5. 验证状态

- 已验证范围：
- 未验证范围：
- 是否达到结构级通过：是 / 否
- 是否达到运行级通过：是 / 否
- 图面可见连线状态：已补齐 `annotation(Line(...))` / 仅完成 `connect(...)` / 不涉及

## 6. 风险与限制

- 是否仍存在仅有语义连接、但图上没有可见连线的连接：

## 7. 下一步建议
