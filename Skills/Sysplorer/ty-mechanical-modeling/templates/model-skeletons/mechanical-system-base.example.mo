within ;

model MechanicalSystemBaseExample
  // Replace the placeholders below with TY library components after
  // completing component selection and parameter-name verification.
  //
  // Example:
  //   TYMultibody.World world(...);
  //   TYMechanics.Rotational.Components.Inertia shaft(...);
  //   TYDriveline.Gears.SomeGear gear(...);

equation
  // Add connect(...) equations only after the minimum closed loop is clear.
  // Every generated connection should include Diagram-view line metadata.
  //
  // Example:
  // connect(revolute1.frame_b, boxBody1.frame_a)
  //   annotation(Line(
  //     points = {{-40, 0}, {-20, 0}},
  //     color = {95, 95, 95},
  //     thickness = 0.5));
  //
  // Query real parameter names from Sysplorer before any parameter assignment.
end MechanicalSystemBaseExample;
