model HydraulicActuationLoopBase
  // Skeleton only. Replace placeholder classes and parameters with the real TY classes selected from component mapping.
  annotation(
    Diagram(coordinateSystem(extent = {{-140, -100}, {140, 100}})),
    Icon(coordinateSystem(extent = {{-140, -100}, {140, 100}})));
end HydraulicActuationLoopBase;
