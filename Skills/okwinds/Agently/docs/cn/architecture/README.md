# Architecture

建议按这个顺序读：

1. [扩展边界](extension-boundaries.md)：判断一个变更应该属于 core、plugin provider、built-in capability，还是 Agent Component。

普通应用开发多数情况下不需要读这个目录。只有在扩展 Agently、编写插件、设计自定义 action，或判断一个新能力应该放在哪一层时才需要读。
