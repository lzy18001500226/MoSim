# Architecture

Read in this order:

1. [Extension Boundaries](extension-boundaries.md): decide whether a change belongs to core, plugin providers, built-in capabilities, or Agent Components.

Most application developers do not need this folder for normal app work. Use it when you are extending Agently itself, writing plugins, designing custom actions, or deciding where a new capability should live.
