# MoSim Non-Frontend Evidence Index

更新时间：2026-07-18

本文是当前非前端竞赛收口的证据入口，不替代各专项 Manifest、运行指标或
最终人工验收。`accepted`、`executed_blocked`、`not_run` 必须以专项权威
JSON 为准。

需求到证据追踪矩阵：
`NON_FRONTEND_REQUIREMENT_EVIDENCE_MATRIX.md` / `.json`。该矩阵只做当前
收尾追踪，不把设计需求自动视为已实现，也不提升任何运行证据的 claim ceiling。

报告数据源表：`NON_FRONTEND_REPORT_SOURCE.md` / `.json`。它只从当前权威
JSON 生成控制器族汇总、14 行 A/B 明细和 P6-P9 状态，不直接生成最终报告
或最终验收结论。

报告图表位于 `figures/`，权威索引为
`figures/REPORT_FIGURE_MANIFEST.json`。当前包含控制器状态分布、七场景 A/B
RMSE 和学习控制相对 Cascade 变化三组 PNG/SVG；图内保留 blocked/not-run
语义，不能脱离 manifest 单独用于扩大结论。

当前复现与演示交付入口为
`NON_FRONTEND_DELIVERY_MANIFEST.md` / `.json`。它绑定本目录的需求矩阵、
报告源表、图表和 P6-P9 专项证据，给出静态重建命令、演示顺序和禁止声明。
它不运行 Gazebo/MWORKS、不导出 PDF、不录制视频，也不代替最终人工验收。

提交包边界入口为
`NON_FRONTEND_SUBMISSION_PACKAGE_MANIFEST.md` / `.json`。它只审计候选
路径、必需文件、排除目录、超大文件和不可访问链接，不复制、删除、暂存或
发布文件。当前白名单选择包含 `3450` 个项目自有控制/运行文件和权威证据，
明确排除前端、UE、References、缓存和历史 Agent 资料。

最终机器 QA 入口为 `NON_FRONTEND_FINAL_QA_AUDIT.md` / `.json`。当前状态为
`passed`：候选文件无缺失、无不可访问路径、无超过 100 MB 文件、无凭据特征
命中，16 条已选/仅参考来源记录的 URL、commit、license 与禁复制边界完整。
该通过不代替 PDF、视频、人工总验收或 Git 远端发布。

## Authority

| Area | Authority | Current state | Claim ceiling |
|---|---|---|---|
| 67-row controller matrix | `Results/control_platform/classic_controller_closeout_20260717/CLASSIC_CONTROLLER_FINAL_MATRIX.json` | 27 accepted, 25 executed-blocked, 15 not-run | Controller-family evidence is closed with blockers |
| Final PID A/B | `Results/control_platform/final_controller_ab_20260718/FINAL_CONTROLLER_SEVEN_SCENARIO_AB.json` | 1 accepted, 11 executed-blocked, 2 not-run | Same-run observed comparison; no general superiority claim |
| Safety | `Results/control_platform/p6_safety_runtime_20260717/P6_SAFETY_RUNTIME_MATRIX.json` | passed | Seven declared safety modes only |
| FTC | `Results/control_platform/p7_ftc_generated_gazebo_r3_20260717/P7_FTC_RUNTIME_CLOSEOUT.json` | passed | Rotor-1 effectiveness 0.65, generated FDI/isolation/takeover/landing; no complete outage or multi-fault claim |
| Formation | `Results/control_platform/p8_formation_mode1_gazebo_r7_20260717/PX4CTRL_SWARM_BASIC_METRICS.json` and the P8 mode directories | passed bounded | Three-UAV formation modes; no autonomous-exploration claim |
| Learning control | `Results/control_platform/p9_learning_gazebo_r4_20260717/P9_LEARNING_RUNTIME_CLOSEOUT.json` | runtime passed, performance blocked | Report-ready experimental routes; not selectable defaults |

## Final A/B

The seven scenarios are `hover`, `step`, `figure8`, `spiral`, `wind`,
`parameter_mismatch`, and `motor_efficiency_fault`. All 14 directories contain
same-run generated-C provenance and landing/disarm evidence. Wind injection
evidence passed for both profiles. The two motor-fault rows remain `not_run`:
the plugin was inserted, but the airborne readiness gate timed out and the
parallel gate cleanup path did not terminate cleanly. This is an orchestration
blocker, not evidence that the FTC controller failed.

## Remaining non-frontend closeout

1. Keep this index, the controller matrices, and the delivery manifest as report sources.
2. Keep both motor-fault A/B rows `not_run` under the versioned infrastructure blocker;
   do not retry them in this closeout.
3. Export and review the user-manual PDF and simulation-report PDF, record the
   reviewed demo video, and write the final human acceptance packet.
4. Complete exact-path Git publication and upstream verification.

The current requirement-to-evidence matrix is generated by
`Scripts/quality/build_non_frontend_requirement_evidence_matrix.py`. It reads
the current C1/C3/P6-P9 authority files and fails closed if the controller
matrix counts drift from `27/25/15`.

The bounded clean single-case motor-fault retry is closed as an infrastructure
blocker at
`Results/control_platform/final_controller_ab_motor_fault_r2_20260718/INFRASTRUCTURE_BLOCKER.json`.
Both C3 motor-fault A/B rows remain `not_run`; no further retry is allowed in
this closeout.

Frontend development, UE/QGC/Flight Console/Model Studio work, and further
FUEL/RACER coverage tuning are outside this index and this closeout goal.
