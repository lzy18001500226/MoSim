task_id: COAGENT-DEVOPS-GIT-SURFACE-UNDER-1K-20260603
status: committed_locally_under_1k_scope
decision: keep_parent_git_surface_isolated_and_hide_large_untracked_trees_in_ide
summary: "Continued the long Git surface recovery goal. Narrow plumbing shows parent repository unignored untracked files are 0 and cached files are 0. A parent working-tree diff count attempt timed out at 60 seconds, so this phase used path-limited diff only. Added git.showUntrackedChanges=false and files.exclude entries to .vscode/settings.json, and mirrored the same setting into the ignored local MoSim.code-workspace so the IDE window opened through that workspace also hides the isolated large trees. The staged batch contained exactly 2 files and no file over 100 MB. Normal git commit timed out at 60 seconds with the same 2-file index still staged and no index.lock, so the verified index was committed locally through git commit-tree/update-ref. This step did not unignore, stage, commit, or push any large batch."
evidence: "parent_unignored_untracked_after_commit=0; parent_cached_changes_after_commit=0; parent_working_tree_diff_attempt=timeout_60s_recorded_as_large_worktree_risk; local_ahead_behind_after_commit=12 0; committed_head=b12855a105; committed_files=.vscode/settings.json,Results/agent_packets/tasks/coagent_devops_git/COAGENT-DEVOPS-GIT-SURFACE-UNDER-1K-20260603.yaml; committed_file_count=2; large_file_check_over_100MB=none; staged_diff_check=pass; normal_git_commit=timeout_60s; index_lock_after_timeout=absent; commit_method=git_commit_tree_update_ref_after_verified_index; vscode_settings_json_valid=true; mosim_code_workspace_json_valid=true; mosim_code_workspace_ignored_local_config=true; ide_settings_added=git.showUntrackedChanges:false,files.exclude_for_References_Agent_References_Blender_References_PX4_Docs_Skills_Unreal_Results_tmp"
risks: "If the IDE still shows 10k+ after settings reload, the source is likely an already cached Source Control model, ignored-files display that is not respecting workspace settings, or another opened repository/root. Push remains pending because earlier push commands timed out under the 60 second rule."
next_action: "Do not open another unignore batch until the IDE surface is rechecked. If 10k+ still appears, capture the exact Source Control repository label/path from the IDE because parent Git plumbing is already at unignored_untracked=0 and cached=0. Push remains pending and should use a longer approved timeout or manual network review."
needs_human: false
blocked_reason: "not_blocked_for_local_git_surface_reduction; push_blocked_by_previous_60s_timeout_policy"
