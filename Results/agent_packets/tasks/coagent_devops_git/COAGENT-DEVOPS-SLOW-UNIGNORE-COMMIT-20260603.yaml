task_id: COAGENT-DEVOPS-SLOW-UNIGNORE-COMMIT-20260603
status: local_ignore_and_ide_surface_isolated_push_pending
decision: distinguish_parent_git_clean_surface_from_ide_ignored_file_display
summary: "Corrected the ignore audit after the user reported the IDE still showed 10k+ files. Parent Git plumbing shows non-ignored untracked files were reduced to 0 after adding the missing Results/unreal_scene_mapping/*probe*.json rule, while ignored untracked files remain large by design under References/PX4, References/Blender, and References/Agent/Gateway. No nested .git repositories were found under those large trees, so the remaining 10k+ display is likely ignored-file display/file watcher pressure rather than parent-repo unignored files. Added .vscode/settings.json to hide ignored Git files and exclude the large isolated trees from watchers/search."
evidence: "parent_unignored_untracked_after_probe_ignore=0; parent_tracked_modified_count=0; parent_cached_diff_count=0; large_ignored_counts=References/PX4:17648,References/Blender:25127,References/Agent/Gateway:15906; nested_git_under_references_px4_blender_gateway=0; nested_unrealscenes_status_clean=true; results_tmp_git_pathspec_behavior_nested_repo_has_1_local_test_modification_but_not_10k_source; added_ignore_rule=Results/unreal_scene_mapping/*probe*.json; added_vscode_settings=git.showIgnoredFiles:false,watcherExclude/search.exclude for References/Agent,References/Blender,References/PX4,Docs/Skills/Unreal"
risks: "Ignored files still exist on disk and can still be shown by some IDE views if they are configured to display ignored files. This action does not delete files and does not submit the ignored trees; later reviewed slices still require git add -f. Local branch remains ahead of origin/main because push is still blocked by previous timeout behavior."
next_action: "Commit the ignore and IDE surface changes locally. Then recover push with a longer approved timeout or manual git push before opening more content batches."
needs_human: true
blocked_reason: "push recovery still pending; local branch is ahead of origin/main"
