task_id: COAGENT-DEVOPS-GIT-DIVIDE-20260531
status: planned
decision: continue_with_next_small_family
summary: >-
  The current checkpoint is now focused on the ROS/FAST-LIO bridge slice.
  Safety probes confirmed `main` is still the active branch, `HEAD...@{upstream}`
  is `1 0`, `.git/index.lock` is absent, and `core.excludesfile` remains
  unset. The bounded current batch is the four-file `Scripts/ros` family:
  `publish_mworks_uav_state_ros2.py`,
  `mosim_dense_lidar_cpp/CMakeLists.txt`,
  `mosim_dense_lidar_cpp/src/dense_lidar_replay_node.cpp`, and
  `mosim_scene_replay/launch/mosim_scene_replay.launch.py`. The slice totals
  about 44.9 KB and `git diff --check` passed. The diff introduces
  Livox-compatible point packing, optional Livox `CustomMsg` publishing,
  and launch wiring for MWORKS state, IMU, and FAST-LIO/RViz2 inputs.
evidence:
  - "branch=main"
  - "upstream_ahead_behind=1 0"
  - "index_lock_present=false"
  - "core_excludesfile=unset"
  - "scripts_ros_batch_total_bytes=44910"
  - "git_diff_check_passed=true"
  - "batch_files=4"
  - "livox_custom_msg_enabled=true"
  - "mworks_fastlio_launch_wiring_enabled=true"
risks:
  - "The worktree still contains many large families, so future batches must remain path-limited."
  - "A broader `git status` has already timed out earlier in the task, so only bounded probes remain safe."
  - "The next step must still recheck cached diff and upstream alignment before commit/push."
ignore_first_strategy: >-
  Keep runtime and generated noise isolated before opening the next slice.
  The existing ignore-first rules should continue suppressing results,
  UE5/runtime outputs, and other large external trees so later inventory
  stays small and reviewable.
proposed_batches:
  - "Batch 1 candidate: `Scripts/ros` bridge slice now under review for commit."
  - "Batch 2 candidate: the next smallest current-HEAD family after the ROS bridge slice, selected only after a path-limited diff and 100MB scan."
  - "Batch 3 candidate: if the next family is noisy, first isolate its generated/runtime subtree instead of forcing it into the next commit."
forbidden_actions:
  - "no broad git status"
  - "no git add -A"
  - "no git clean"
  - "no reset --hard"
  - "no force push"
  - "no staging batches over 1000 files"
  - "no commit of files at or above 100MB"
  - "no unreviewed unignore of runtime/generated trees"
next_action: >-
  Commit the reviewed ROS slice only after a cached diff check and then
  re-evaluate upstream alignment before moving to the next family.
needs_human: false
blocked_reason: ""
