task_id: COAGENT-DEVOPS-GITIGNORE-SPLIT-EXECUTE-20260604
status: in_progress
decision: blender_real_gitignore_throttle_added_but_not_completion
summary: Continued the active long Git split goal after the user reported the local Git surface still showed 10k+ changes. Did not use broad git status, git add -A, git clean, reset --hard, force push, or push. Confirmed that References/Blender is not currently visible as untracked to Git, but it is a huge tracked reference tree and path-limited status probes timeout. Added a real .gitignore throttle at the end of the file so References/Blender new/untracked files are suppressed at the Git layer, not merely hidden in the IDE. This is explicitly temporary and must later be drained or narrowed in reviewed under-1000-file batches; untracked count 0 is not completion.
evidence: active_goal=COAGENT-DEVOPS-GITIGNORE-SPLIT-EXECUTE-20260604; index_lock_absent=true; cached_index_count=0; upstream_ahead_behind=149_0; broad_git_status_used=false; git_add_A_used=false; git_clean_used=false; reset_hard_used=false; force_push_used=false; push_attempted=false; path_limited_status_references_blender_timeout_60s=true; path_limited_status_agent_unreal_windows_mcp_timeout_60s=true; references_blender_disk_files=25127; references_blender_tracked_files=16146; references_blender_visible_untracked_before=0; references_blender_visible_untracked_after=0; global_visible_untracked_after=0; global_visible_untracked_command_duration_about_seconds=30; git_check_ignore_probe=.gitignore:579:References/Blender/** References/Blender/synthetic_probe.tmp; modified_files=.gitignore,Docs/Workflows/agent_task_ledger.md,Results/agent_packets/COAGENT-DEVOPS-GITIGNORE-SPLIT-EXECUTE-20260604.yaml
risks: The user-visible 10k+ surface may be tracked modifications/deletions/renames, which .gitignore cannot hide. References/Blender has 16146 tracked files, so any tracked rename/delete/modify storm must be diagnosed with path-limited tracked-change commands, not additional ignore rules. The new References/Blender/** rule is a temporary throttle and would be wrong as final state unless narrowed to generated/cache/binary/over-100MB classes or replaced by committed content batches. git ls-files --others still took about 30 seconds, showing large-tree pressure remains.
next_action: Gate and commit this small .gitignore plus ledger/result-packet batch. Then continue with path-limited tracked-change diagnostics without broad git status: use git ls-files -m/-d or git diff --name-only on one path family at a time, starting with the path that the UI reports as 10k+ if available. For future untracked mass, add a real .gitignore subtree throttle first, then drain by removing/narrowing it in reviewed under-1000-file batches and committing actual files, not rule-only changes.
needs_human: false
blocked_reason: not_blocked
