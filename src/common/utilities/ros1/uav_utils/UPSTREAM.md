# uav_utils Source Provenance

- Upstream repository: `https://github.com/ZJU-FAST-Lab/Fast-Drone-250`
- Upstream revision: not recoverable from the retained imported snapshot. Do
  not represent this copy as pinned to a Git commit until a source-to-upstream
  comparison supplies one.
- Imported source:
  `References/Lab/planning_local/Fast-Drone-250/src/utils/uav_utils`
- Imported deliverable payload: 11 files, tree SHA-256
  `f00befb146bdb47cfb194d3dfa6a566c2d962f5d003d0704e8e9709a567e4aa8`.
  No build tree, generated Catkin output, cache, or editor-local file was
  present in this component snapshot.
- License evidence: `package.xml` declares `LGPLv3`; the retained component
  does not include a standalone license file. A release-license audit remains
  required before redistribution.
- Python compatibility: `scripts/tf_assist.py` retains Python 2 exception
  syntax and does not compile under the current Python 3 interpreter. It is
  preserved unchanged; a ROS Noetic/Python 3 port is an activation prerequisite,
  not part of this copy-only task.

The 11-file deliverable payload is a byte-for-byte copy of the retained source.
`.gitattributes`, `UPSTREAM.md`, and `PATCHES.md` are migration metadata
outside that payload. Its registry state is `copied_pending_activation`: the
legacy path remains the only active runtime path until the ROS1 workspace,
entrypoints, source revision, and license audit are validated.
