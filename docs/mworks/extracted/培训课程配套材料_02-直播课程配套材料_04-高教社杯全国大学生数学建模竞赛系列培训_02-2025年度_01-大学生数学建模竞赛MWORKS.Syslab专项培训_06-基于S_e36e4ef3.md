# 用遗传算法求解非光滑函数最小值.jl

- Source: `培训课程配套材料/02-直播课程配套材料/04-高教社杯全国大学生数学建模竞赛系列培训/02-2025年度/01-大学生数学建模竞赛MWORKS.Syslab专项培训/06-基于Syslab求解优化问题/code/用遗传算法求解非光滑函数最小值.jl`
- Category: `sysplorer_modeling`
- Score: `80`
- Size: `0.00 MB`
- Extract mode: `text`

## Extracted Text

```text
using TyGlobalOptimization
using TyPlot
using TyBase
xi = range(-6, 2, 300)
yi = range(-4, 4, 300)
X, Y = meshgrid2(xi, yi)
A = [reshape(X, :, 1) reshape(Y, :, 1)]
Z = ps_example(A)
Z = reshape(Z, size(X))
surf(X, Y, Z)
title("ps_example")
ylabel("x(1)")
xlabel("x(2)")
#定义待求解问题
function func(p)
    x1, x2 = p
    return ps_example([x1 x2])[1]
end
lb = [-6.0, -4.0]
ub = [2.0, 4.0]


#执行求解
x, fval, output = ga(func, lb, ub)

#打印优化结果
println("最优解 = $x")
println("最优值 = $fval")
```
