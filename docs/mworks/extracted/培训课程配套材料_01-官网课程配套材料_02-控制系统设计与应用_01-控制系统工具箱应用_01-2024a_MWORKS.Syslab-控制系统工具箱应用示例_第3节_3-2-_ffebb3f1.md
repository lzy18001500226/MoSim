# 3-2-频域分析.jl

- Source: `培训课程配套材料/01-官网课程配套材料/02-控制系统设计与应用/01-控制系统工具箱应用/01-2024a/MWORKS.Syslab-控制系统工具箱应用示例/第3节/3-2-频域分析.jl`
- Category: `syslab_analysis`
- Score: `102`
- Size: `0.00 MB`
- Extract mode: `text`

## Extracted Text

```text
using TyControlSystems
using  TyBase
##【示例27】计算系统频率响应并绘制bode图
G = tf([100], [1, 2, 100])
bode(G)

# 计算不同阻尼比下的系统频率响应
# 定义频率向量 
w = logspace(0, 2, 100)
# 定义不同阻尼比的系统模型 
G1 = tf([100], [1, 2 * 0.1 * 10, 100])
G2 = tf([100], [1, 2 * 0.4 * 10, 100])
G3 = tf([100], [1, 2 * 0.8 * 10, 100])
# bode图绘制 
bode(G1, w, linewidth=1)
bode(G2, w, linewidth=1, ishold=true)
bode(G3, w, linewidth=1, ishold=true)
# bode图网格开启 
bodegrid(true)
# 定义legend 
G1l = raw"$G_1\left(s\right) = \frac{{100}}{{{s^2} + 2s + 100}}$"
G2l = raw"$G_2\left(s\right) = \frac{{100}}{{{s^2} + 8s + 100}}$"
G3l = raw"$G_3\left(s\right) = \frac{{100}}{{{s^2} + 16s + 100}}$"
legend(G1l, G2l, G3l)

#-----------------------------------------#

##【示例28】计算下面MIMO系统频率响应并绘制bode图
# 定义系统矩阵
A = [-1 -1; 6.5 0]
B = [1 1; 1 0]
C = [1 0; 0 1]
D = zeros(2, 2)
# 创建状态空间模型
G = ss(A, B, C, D)
# 绘制系统bode图
bode(G)
bodegrid(true)

# 计算并获取系统频率响应数据
w = logspace(-2, 3, 100)
mag, phase, wout = bode(G, w, fig=false)

#-----------------------------------------#

##【示例29】绘制闭环系统开环传递函数bode图，并确定其增益裕度、相位裕度
s = tf('s')
Gopenloop = 20 * (s + 1) / (s * (s + 5) * (s^2 + 2 * s + 10))
margin(Gopenloop)

# 获取频率响应开环指标
Gm, Pm, Wcg, Wcp = margin(Gopenloop, fig=false)

w = logspace(-2, 3, 100)
mag, phase, wout = bode(G, w, fig=false)

#-----------------------------------------#

##【示例30】考虑如图闭环系统，利用Syslab求取其闭环表达式，并计算其谐振峰值、谐振频率和带宽
s = tf('s')
Gopenloop = 1 / (s * (0.5 * s + 1) * (s + 1))
Gcloseloop = feedback(Gopenloop)
gpeak, fpeak = getPeakGain(Gcloseloop)
fb = bandwidth(Gcloseloop)

## 考虑通过带宽含义计算得到
# 给定频率向量
w = logspace(-1, 1, 500)
# 定义系统模型
s = tf('s')
Gopenloop = 1 / (s * (0.5 * s + 1) * (s + 1))
Gcloseloop = feedback(Gopenloop)
# 计算频率响应
mag, phase, w = bode(Gcloseloop, w, fig=false)
# 计算系统带宽
n = 1
while 20 * log10(mag[n]) >= -3
 global    n = n + 1
end

w[n]

#-----------------------------------------#

##【示例31】计算示例26的MIMO系统频率响应并绘制Nyquist图
# 定义系统矩阵
A = [-1 -1; 6.5 0]
B = [1 1; 1 0]
C = [1 0; 0 1]
D = zeros(2, 2)
# 创建状态空间模型
G = ss(A, B, C, D)
# 绘制系统Nyquist图
nyquist(G)
nyquistgrid(true)

#-----------------------------------------#

##【示例32】绘制系统对数幅-相图
# 定义系统
G = tf([100], [1 2 100])
# 绘制nichols图并开启网格
nichols(G)
nicholsgrid(true)
```
