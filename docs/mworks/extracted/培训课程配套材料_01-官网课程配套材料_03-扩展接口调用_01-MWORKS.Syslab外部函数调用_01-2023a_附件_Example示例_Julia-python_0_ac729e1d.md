# 05 Julia与Python的深度融合.jl

- Source: `培训课程配套材料/01-官网课程配套材料/03-扩展接口调用/01-MWORKS.Syslab外部函数调用/01-2023a/附件/Example示例：Julia-python/05 Julia与Python的深度融合.jl`
- Category: `syslab_analysis`
- Score: `84`
- Size: `0.00 MB`
- Extract mode: `text`

## Extracted Text

```text
using PyCall

# python代码
py"""
import numpy.polynomial
class Doubler(numpy.polynomial.Polynomial):
    def __init__(self, x=10):
        self.x = x
    def my_method(self, arg1): return arg1 + 20
    @property
    def x2(self): return self.x * 2
    @x2.setter
    def x2(self, new_val):
        self.x = new_val / 2
print(Doubler().x2) # 20
"""

# 与上面等价的写法：
# 通过 @pydef 来创建一个python类，其方法是用julia实现的
P = pyimport("numpy.polynomial")
@pydef mutable struct Doubler <: P.Polynomial
    __init__(self, x = 10) = (self.x = x)
    my_method(self, arg1::Number) = arg1 + 20
    x2.get(self) = self.x * 2
    x2.set!(self, new_val) = (self.x = new_val / 2)
end

d = Doubler()
println(d.x2) # 20

d.x2 = 10
println(d.x) # 5

d.x = 15
println(d.x2) # 30
```
