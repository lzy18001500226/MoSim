# 03 控制系统详细设计：PID、NMPC、INDI、L1

## 1. 控制架构总览

```text
reference trajectory
    ↓
task_mode_manager
    ↓
PID / MPC / NMPC outer loop
    ↓
desired acceleration / thrust / attitude
    ↓
disturbance compensation
    ↓
Safety Filter
    ↓
INDI attitude inner loop
    ↓
control allocation
    ↓
motor command
```

---

## 2. 控制器版本

| 版本 | 名称 | 作用 |
|---|---|---|
| C0 | PID baseline | 官方基线，P0 对照组 |
| C1 | Improved PID | P0 保底优化控制器 |
| C2 | AWFF PID | P1 已落地控制器：抗饱和 + 竖直参考前馈 + 导数滤波 |
| C2b | PID + INDI | P1 姿态内环增强 |
| C3 | Linear MPC + INDI | P1 可落地预测控制回退 |
| C4 | NMPC + INDI | P1/P2 高级主控制框架 |
| C5 | NMPC + INDI + L1-inspired | P1/P2 鲁棒增强 |
| C6 | C5 + Safety + Fault Reallocation | P2 高展示度增强 |

推荐报告主线：

```text
C0 官方 PID baseline
→ C1 改进 PID
→ C2 AWFF PID 独立控制器
→ C3/C4 约束预测外环 + INDI 姿态内环
→ C5 加速度残差自适应补偿
→ C6 安全过滤、故障重构和模式切换
```

若平台实时求解限制导致 C4 不稳定，可用 C3 线性 MPC 或 C2 PID-INDI 保持同一接口继续完成消融实验；报告中仍以“统一控制架构”说明高级模块和回退实现。

---

## 3. PID baseline、改进 PID 与 AWFF PID

### 3.1 baseline 分析重点

- Kp、Ki、Kd；
- 积分限幅；
- 电机输出限幅；
- 阶跃超调；
- 8 字跟踪滞后；
- 风扰下稳态误差。

### 3.2 改进项

```text
u = Kp e + Ki ∫e dt + Kd de/dt + Kff a_ref
```

加入：

1. 积分抗饱和；
2. 导数低通滤波；
3. 加速度前馈；
4. 速度阻尼；
5. 输出限幅；
6. 参数配置化。

### 3.3 改进 PID 实现规格

模块名：

```text
controller_pid_improved
```

输入：

```text
state_bus.position
state_bus.velocity
state_bus.attitude
state_bus.angular_rate
reference_bus.position_ref
reference_bus.velocity_ref
reference_bus.acceleration_ref
reference_bus.yaw_ref
controller_params
```

输出：

```text
thrust_ref
attitude_ref
torque_cmd
motor_cmd[4]
debug.error
debug.integral_state
debug.saturation_flag
```

核心函数：

```text
compute_position_pid()
anti_windup()
derivative_lpf()
accel_to_attitude()
compute_attitude_pid()
allocate_motor()
```

抗积分饱和建议：

```pseudo
if output_unsat != output_sat:
    integral = integral + k_aw * (output_sat - output_unsat) * dt
else:
    integral = integral + error * dt
integral = clamp(integral, -integral_max, integral_max)
```

导数滤波：

```pseudo
d_error_raw = (error - error_prev) / dt
d_error_f = alpha_d * d_error_f_prev + (1 - alpha_d) * d_error_raw
```

参数示例：

```yaml
pid_improved:
  dt: 0.01
  kp_pos: [1.2, 1.2, 2.0]
  ki_pos: [0.02, 0.02, 0.05]
  kd_pos: [0.8, 0.8, 1.0]
  kff_acc: [1.0, 1.0, 1.0]
  integral_limit: [0.5, 0.5, 0.5]
  derivative_alpha: 0.8
  max_acc: [4.0, 4.0, 5.0]
  max_tilt_deg: 35
```

验收：

```text
step_nominal: 调节时间或超调量优于 baseline
figure8_nominal: RMSE 低于 baseline
figure8_wind: 稳态误差不高于 baseline
```

### 3.4 AWFF PID 已落地实现规格

当前已在 `models/QuadrotorExperiments/package.mo` 中实现项目自有控制器：

```text
QuadrotorExperiments.AntiWindupFeedforwardController
QuadrotorExperiments.Example1AntiWindupFeedforwardPID
```

Sysblock 控制器仿真路线：

```text
已验证的高度环最小模型：
models/QuadrotorControllerBlocks/AWFF_PID_Sysblock_Demo.mo
AWFF_PID_Sysblock_Demo

正在推进的分层控制器模型：
models/QuadrotorControllerBlocks/AWFF_PositionOuterLoop_Sysblock.mo
AWFF_PositionOuterLoop_Sysblock
models/QuadrotorControllerBlocks/AWFF_AttitudeInnerLoop_Sysblock.mo
AWFF_AttitudeInnerLoop_Sysblock
models/QuadrotorControllerBlocks/AWFF_MotorMixer_Sysblock.mo
AWFF_MotorMixer_Sysblock
```

要求：Sysblock 控制器文件不是单纯的截图支撑材料，而是后续控制器闭环仿真的主实现路线之一。结构截图必须来自 MWORKS.Sysblock/Sysplorer 打开的实际控制器模型窗口，用于证明控制器模块连接、端口和信号流；正式结论必须以 `load_file`、`check_model`、必要时 `simulate_model` 的真实 MWORKS 证据为准，不使用手绘示意图或离线脚本替代。

当前验证状态：三个分层模型已完成文件级结构草案；`AWFF_PositionOuterLoop_Sysblock` 已通过 `load_file`，但 `check_model` 返回失败且 MWORKS 未给出具体错误文本，失败诊断保存在 `results/logs/sysplorer_layered_sysblock_check_failed_20260510_summary.json`。因此分层模型暂不作为已验证仿真证据，下一步应按已通过的 `AWFF_PID_Sysblock_Demo` / 官方 `SimplePID` 导出形态继续收敛。

补充诊断：进一步缩小到单轴位置环 `AWFF_PositionAxis_Sysblock` 后，仍然出现 `load_file` 成功但 `check_model` 失败且无具体错误文本，诊断保存在 `results/logs/sysplorer_position_axis_check_failed_20260510_summary.json`。这说明问题不主要来自多端口复杂度，而更可能来自手写 Sysblock `.mo` 缺少 MWORKS/Sysblock 生成的绑定元数据或内部配置。后续不再盲目手写新的 Sysblock 控制器文件；应优先通过 MWORKS.Sysblock GUI/API 从已验证模型复制、另存或生成，再用 MCP 检查。

当前模型已通过真实 Sysplorer MCP `load_file` 和 `check_model`，证据日志：

```text
results/test_reports/sysplorer_awff_sysblock_demo_check_20260510.jsonl
```

模型替换方式：

```text
QuadrotorExperiments.Example1ProjectControllerBase
  controller3_2: AntiWindupFeedforwardController
```

接口保持与官方 `QuadrotorModel.Blocks.Controller.Controller` 兼容：

```text
inputs:
  position_command[3]  ← climbePath.position_command
  position[3]          ← sensors1_1.PosMea
  angle[3]             ← sensors1_1.AngleMea
outputs:
  y, y1, y2, y3        → actuator1_1.u ... actuator1_4.u
```

控制链路：

```text
position_command - position
→ XY/Z 位置 PID
→ Z 参考速度前馈
→ 条件积分抗饱和
→ roll/pitch 姿态参考限幅
→ roll/pitch/yaw 姿态 PD
→ 控制量限幅
→ 四旋翼混控矩阵
→ motor command y,y1,y2,y3
```

当前 Example1 全时长真实 Sysplorer MCP 结果：

```text
scenario: scenarios/official/example1_awff_pid.yaml
raw: results/raw/official_example1_awff_pid.csv
metrics: results/metrics/official_example1_awff_pid.json
mcp_log: results/test_reports/sysplorer_example1_awff_pid_full_20260510.jsonl
```

验收状态：

```text
check_model: pass
simulate_model: pass
position_rmse_m: 0.259914
RMSE improvement vs official PID: 5.573%
RMSE improvement vs enhanced PID: 2.380%
```

注意：AWFF 当前属于控制器本体仿真，不负责后续 Gazebo 展示。`replay JSON/HTML` 只作为展示素材，不作为控制器仿真证据。

---

## 4. NMPC 外环

NMPC 是高分主线，但不作为初赛唯一可行路线。若实时求解、平台接口或参数整定风险较高，可先实现线性 MPC 或 PID-INDI，确保 P0/P1 闭环稳定，再逐步替换为 NMPC。

### 4.1 简化状态

```text
X = [x, y, z, vx, vy, vz]^T
U = [ax, ay, az]^T
```

### 4.2 离散模型

```text
p(k+1) = p(k) + v(k) dt
v(k+1) = v(k) + a(k) dt
```

### 4.3 目标函数

```text
J = Σ [
    ||p - p_ref||²_Qp
  + ||v - v_ref||²_Qv
  + ||a||²_R
  + ||a_k - a_{k-1}||²_Rd
]
```

### 4.4 约束

```text
||v|| <= v_max
||a|| <= a_max
T_min <= T <= T_max
|roll_ref| <= roll_max
|pitch_ref| <= pitch_max
```

### 4.5 推荐参数

```yaml
dt_nmpc: 0.05
N: 20
Qp: [10, 10, 15]
Qv: [2, 2, 3]
R: [0.2, 0.2, 0.3]
Rd: [0.5, 0.5, 0.8]
```

### 4.6 输出转换

```text
a_cmd → T_ref, roll_ref, pitch_ref
```

近似：

```text
T_ref = m ||g e_z + a_cmd||
tilt ≈ atan(||a_xy|| / (g + a_z))
```

更完整的加速度到姿态转换：

```pseudo
function accel_to_attitude(a_cmd, yaw_ref, mass):
    g_vec = [0, 0, g]
    f_des = mass * (g_vec + a_cmd)
    z_b_des = normalize(f_des)

    x_c = [cos(yaw_ref), sin(yaw_ref), 0]
    y_b_des = normalize(cross(z_b_des, x_c))
    x_b_des = cross(y_b_des, z_b_des)

    R_des = [x_b_des, y_b_des, z_b_des]
    attitude_ref = rotation_to_euler(R_des)
    thrust_ref = dot(f_des, z_b_des)
    return thrust_ref, attitude_ref
```

若姿态转换与平台坐标系不一致，以 `02_模型接口与运行流程.md` 中坐标变换为准。

### 4.7 MPC/NMPC 实现规格

模块名：

```text
controller_mpc_indi
controller_nmpc_indi
```

最低可实现版本：

```text
线性 MPC 使用 6 状态双积分模型，输出期望加速度 a_cmd。
NMPC 使用同一接口，若求解失败，自动回退上一时刻可行解或改进 PID 输出。
```

最低落地参数：

```yaml
mpc_minimum:
  model: double_integrator_6_state
  outer_loop_dt: 0.05
  inner_loop_dt: 0.005
  horizon_N: 15
  max_solve_time_ms: 20
  warm_start: true
  max_consecutive_failures: 3
  fallback_controller: pid_indi
  constraints:
    v_max: [3.0, 3.0, 2.0]
    a_max: [4.0, 4.0, 5.0]
    tilt_max_deg: 35
    thrust_margin: 0.85
```

求解器接口约定：

```text
solve_mpc(state, reference_window, params, last_solution)
  → status
  → a_cmd[3]
  → predicted_state
  → cost
  → solve_time_ms
```

`controller_mpc_indi` 和 `controller_nmpc_indi` 必须保持同一输入输出接口。区别只在预测模型和目标函数内部实现，便于在模型中通过 `controller_id` 替换。

核心函数：

```text
build_prediction_model()
build_cost_function()
apply_constraints()
solve_mpc()
fallback_if_solver_failed()
accel_to_attitude()
```

求解失败回退：

```pseudo
if solver_status != success:
    if last_solution_valid:
        a_cmd = last_solution.a0
    else:
        a_cmd = pid_improved_outer_loop(state, ref)
    debug.solver_fallback = true
```

验收：

```text
figure8_nominal: RMSE 低于 improved PID
spiral_nominal: 最大误差低于 improved PID
solver_failure_ratio < 5%
```

---

## 5. INDI 姿态内环

### 5.1 思路

INDI 不直接依赖完整模型，而使用传感器估计的角加速度和控制增量。

```text
u_k = u_{k-1} + Δu
```

### 5.2 控制律

```text
Δu = G^{-1} (ν - ω_dot_meas)
```

其中：

```text
ν = K_att e_att + K_rate e_rate
```

若直接以力矩为控制：

```text
τ_cmd = τ_prev + J(ν - ω_dot_meas)
```

### 5.3 角加速度估计

```text
ω_dot = (ω_k - ω_{k-1}) / dt
ω_dot_f = α ω_dot_f_prev + (1-α)ω_dot
```

建议：

```text
α = 0.8 ~ 0.95
```

### 5.4 限幅

```text
||Δu|| <= Δu_max
||u|| <= u_max
```

### 5.5 INDI 实现规格

模块名：

```text
attitude_indi_inner_loop
```

输入：

```text
attitude_ref
angular_rate_ref
state_bus.attitude
state_bus.angular_rate
state_bus.angular_acceleration
previous_torque_cmd
```

输出：

```text
torque_cmd
debug.angular_acc_f
debug.delta_torque
```

核心函数：

```text
compute_attitude_error()
estimate_angular_acceleration()
filter_angular_acceleration()
compute_virtual_control()
compute_incremental_torque()
limit_torque_rate()
```

实现伪代码：

```pseudo
e_att = attitude_ref - attitude
e_rate = angular_rate_ref - angular_rate
nu = K_att * e_att + K_rate * e_rate

omega_dot_raw = (angular_rate - angular_rate_prev) / dt
omega_dot_f = alpha * omega_dot_f_prev + (1-alpha) * omega_dot_raw

delta_tau = J * (nu - omega_dot_f)
delta_tau = clamp(delta_tau, -delta_tau_max, delta_tau_max)
tau_cmd = clamp(tau_prev + delta_tau, -tau_max, tau_max)
```

参数示例：

```yaml
indi:
  dt: 0.005
  k_att: [8.0, 8.0, 4.0]
  k_rate: [0.25, 0.25, 0.15]
  angular_acc_alpha: 0.9
  delta_torque_max: [0.03, 0.03, 0.02]
  torque_max: [0.2, 0.2, 0.1]
```

---

## 6. L1-inspired 扰动补偿

本模块采用“L1-inspired”表述，定位为基于加速度残差估计的低通扰动补偿，不宣称实现严格完整的 L1 自适应控制。报告中应强调其工程作用：估计风扰、质量变化和推力效率偏差带来的等效加速度误差，并对外环加速度指令进行补偿。

### 6.1 扰动估计

```text
a_meas = (v_k - v_{k-1}) / dt
a_model = g + (T/m) R e_z
d_hat = a_meas - a_model
```

### 6.2 递推估计

```text
d_hat(k+1) = (1 - λdt)d_hat(k) + γ e_v dt
```

### 6.3 低通滤波

```text
d_comp = LPF(d_hat)
```

### 6.4 补偿方式

```text
a_cmd_corrected = a_cmd - d_comp
```

或：

```text
T_corrected = T_ref + m d_comp_z
```

### 6.5 限幅

```text
||d_comp|| <= d_max
```

### 6.6 扰动补偿实现规格

P1 推荐只实现加速度残差估计：

```pseudo
a_meas = filtered_derivative(velocity)
a_model = [0, 0, -g] + thrust_ref / mass * R * e_z
d_hat_raw = a_meas - a_model
d_hat = alpha_d * d_hat_prev + (1-alpha_d) * d_hat_raw
d_comp = clamp(d_hat, -d_max, d_max)
a_cmd_corrected = a_cmd - d_comp
```

P2 再加入速度误差递推项：

```pseudo
d_hat = (1 - lambda * dt) * d_hat + gamma * (velocity_ref - velocity) * dt
```

参数示例：

```yaml
disturbance_compensation:
  enabled: true
  method: acceleration_residual
  alpha: 0.85
  d_max: [2.0, 2.0, 2.0]
  gamma: [0.1, 0.1, 0.1]
  lambda: 1.0
```

验收：

```text
figure8_wind: RMSE 或 recovery_time 优于 no compensation
spiral_mass_change: steady_error 优于 no compensation
```

---

## 6.7 扰动识别与模式切换

本模块不替代控制器，而是根据残差和场景事件给控制链路提供模式信号。

识别输入：

```text
velocity_error
acceleration_residual
attitude_residual
motor_efficiency eta
sensor_noise_level
event_log
```

模式定义：

| 模式 | 触发条件 | 控制策略 |
|---|---|---|
| NORMAL | 残差低于阈值 | 标准控制 |
| WIND_REJECTION | 水平加速度残差持续偏大 | 启用 L1-inspired 水平补偿 |
| MASS_ADAPTATION | z 向残差或投递事件导致等效质量变化 | 调整质量估计和 z 向补偿 |
| FAULT_TOLERANT | eta 下降或角加速度残差异常 | 启用故障重构和保守限幅 |
| SAFETY_PROTECTION | 高度、倾角、障碍距离接近边界 | 强化 safety_filter |
| EMERGENCY_LAND | 重度故障或持续失稳 | 降级下降或安全终止 |

状态转移表：

| 当前模式 | 触发条件 | 目标模式 | 最小保持时间 | 恢复条件 | 事件 |
|---|---|---|---:|---|---|
| NORMAL | `norm(acc_residual_xy) > wind_threshold` 持续 N 个采样 | WIND_REJECTION | 1.0 s | 残差低于恢复阈值 2.0 s | wind_enter |
| NORMAL/WIND_REJECTION | 投递事件或 `abs(acc_residual_z) > mass_threshold` | MASS_ADAPTATION | 2.0 s | z 向稳态误差恢复 | mass_change |
| 任意非 EMERGENCY | `eta_min < 0.85` 或角加速度残差异常 | FAULT_TOLERANT | 2.0 s | eta 恢复或故障场景结束 | motor_fault |
| 任意 | 高度/倾角/障碍距离接近边界 | SAFETY_PROTECTION | 0.5 s | 安全裕度恢复 | safety_guard |
| FAULT_TOLERANT/SAFETY_PROTECTION | `eta_min < 0.5` 或持续失稳 | EMERGENCY_LAND | 至仿真结束 | 不自动恢复 | emergency_land |
| WIND_REJECTION/MASS_ADAPTATION | 残差恢复且无故障/安全事件 | NORMAL | 1.0 s | - | mode_recover |

冲突优先级：

```text
EMERGENCY_LAND > FAULT_TOLERANT > SAFETY_PROTECTION > MASS_ADAPTATION > WIND_REJECTION > NORMAL
```

伪代码：

```pseudo
function update_mode(state, residual, event, eta, previous_mode):
    if eta_min < 0.5 or severe_instability:
        mode = EMERGENCY_LAND
    elif eta_min < 0.85 or norm(attitude_residual) > fault_threshold:
        mode = FAULT_TOLERANT
    elif safety_margin_low:
        mode = SAFETY_PROTECTION
    elif abs(acc_residual_z) > mass_threshold or event == DELIVERY:
        mode = MASS_ADAPTATION
    elif norm(acc_residual_xy) > wind_threshold:
        mode = WIND_REJECTION
    else:
        mode = NORMAL

    if mode_changed and hold_time_satisfied:
        write_event_log(time, previous_mode, mode, reason)
    return mode
```

验收：

```text
figure8_wind: controller_mode 从 NORMAL 切换到 WIND_REJECTION
delivery_mass_change: 投递后进入 MASS_ADAPTATION，并记录事件
motor_fault_eta70: 故障后进入 FAULT_TOLERANT
heavy_fault: 进入 EMERGENCY_LAND 或安全终止
```

---

## 7. 控制器伪代码

```pseudo
function step(state, ref):
    a_raw = solve_nmpc(state, ref)
    d_hat = estimate_disturbance(state)
    d_comp = low_pass(d_hat)
    a_cmd = a_raw - d_comp

    thrust_ref, attitude_ref = accel_to_attitude(a_cmd, ref.yaw)
    thrust_ref, attitude_ref = safety_filter(thrust_ref, attitude_ref, state)

    torque_cmd = indi(attitude_ref, state.attitude, state.angular_rate)
    motor_cmd = allocate(thrust_ref, torque_cmd, motor_efficiency)

    return motor_cmd
```

通用控制器接口：

```pseudo
function controller_step(state_bus, reference_bus, params, memory):
    state = state_preprocessor(state_bus, memory)
    ref = reference_bus
    mode = task_mode_manager(state, ref, params.mode, memory)

    a_cmd = outer_loop(state, ref, params.outer)
    a_cmd = disturbance_compensator(a_cmd, state, ref, mode, params.disturbance)
    thrust_ref, attitude_ref = accel_to_attitude(a_cmd, ref.yaw, params.mass)
    thrust_ref, attitude_ref = safety_filter(thrust_ref, attitude_ref, state, mode, params.safety)
    torque_cmd = attitude_inner_loop(attitude_ref, state, params.inner)
    motor_cmd = control_allocation(thrust_ref, torque_cmd, mode, params.allocation)

    return motor_cmd, debug, mode, memory
```

---

## 8. 调试顺序

1. PID baseline；
2. 改进 PID；
3. INDI 单轴；
4. INDI 三轴；
5. NMPC 外环单机无扰动；
6. NMPC-INDI 复杂轨迹；
7. L1 风扰；
8. L1 质量变化；
9. 安全过滤；
10. 电机故障；
11. 路径规划轨迹；
12. 编队。
13. 模式切换和事件日志；
14. 控制健康度评分。

---

## 9. 消融实验

| 实验 | 目的 |
|---|---|
| PID vs Improved PID | 工程 PID 改进有效性 |
| PID inner vs INDI inner | INDI 内环有效性 |
| NMPC-INDI vs PID-INDI | NMPC 外环有效性 |
| NMPC-INDI-L1 vs NMPC-INDI | L1 补偿有效性 |
| +Safety vs no Safety | 安全过滤有效性 |
| +Fault Reallocation vs no Reallocation | 容错有效性 |
| +Mode Switch vs fixed mode | 模式自适应有效性 |

---

## 10. 预期结果

```text
复杂轨迹 RMSE 降低
风扰恢复时间缩短
质量变化稳态误差降低
电机故障下最大姿态偏差降低
控制输入饱和比例下降
```

---

## 11. 控制器验收指标

| 控制器 | 必要场景 | 通过标准 |
|---|---|---|
| PID baseline | 悬停、阶跃、8 字、螺旋 | 可稳定运行并导出完整数据 |
| Improved PID | 阶跃、8 字、风扰 | 相比 baseline 至少 1 项核心指标改善 |
| PID-INDI | 姿态阶跃、8 字 | 姿态峰值误差或位置 RMSE 低于 baseline |
| MPC/NMPC-INDI | 8 字、螺旋 | 复杂轨迹 RMSE 低于 improved PID |
| L1-inspired 补偿 | 风扰、质量变化 | 稳态误差、RMSE 或恢复时间至少 1 项下降 |
| Safety/Fault | 限幅、eta=0.7 故障 | 约束违规次数、最大误差或饱和比例下降 |

核心报告口径：

```text
PID baseline → Improved PID → PID-INDI/MPC-INDI → +disturbance compensation
```

每增加一个模块，都必须有消融实验说明其收益；若某高级模块效果不稳定，则在报告中作为探索方案，主结论回退到已稳定通过验收的控制器。
