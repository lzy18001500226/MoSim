# 00 系统总体设计

## 1. 项目定位

项目名称建议：

**面向复杂任务场景的四旋翼无人机自适应鲁棒位姿控制与智能仿真验证系统**

核心口径：

> 以 MWORKS 四旋翼模型为基础，围绕“官方 PID baseline → 改进 PID → PID-INDI/MPC-INDI → 扰动补偿”的逐级消融路线，完成复杂轨迹跟踪、风扰/质量摄动鲁棒性验证和量化评估；在主线稳定的基础上扩展安全过滤、故障注入、路径规划、编队与 Syslab/MCP 自动化评估。

---

## 2. 总体分层架构

```text
L6 业务任务层
    城市巡检 / 物流配送 / 应急搜索 / 编队巡检 / 故障返航

L5.8 交互式实验配置层
    GUI 配置面板 / 算法组合选择 / 参数预览 / 场景生成 / 批量运行入口

L5.5 任务管理与模式决策层
    任务阶段机 / 扰动识别 / 控制模式切换 / 故障降级策略

L5 路径规划与任务轨迹层
    预设航点 / A* / RRT* / Kinodynamic A* / Minimum Snap / B-spline / 可跟踪性检查

L4 外环控制层
    改进 PID / 线性 MPC / NMPC 位置外环 / 速度约束 / 加速度约束

L3 扰动补偿层
    L1-inspired 残差估计 / 风扰补偿 / 质量变化补偿 / 推力效率补偿

L2 姿态内环控制层
    INDI 姿态控制 / 角加速度反馈 / 控制效率矩阵 / 增量控制

L1 安全与容错层
    Safety Filter / 障碍物安全距离 / 最低高度 / 机间距离 / 执行器故障重构

L0 动力学仿真层
    Sunray150 机体 / 电机模型 / 传感器抽象 / 扰动模型 / 结果导出

L-0.5 外部可视化层
    Sysplorer 原生动画审查 / TCP-UDP 状态流 / Unreal 外部渲染 / 视频录制

L-1 自动化验证层
    Syslab 指标计算 / MCP 模型调用 / 批量仿真 / 图表生成
```

---

## 3. 总体架构图

```text
┌─────────────────────────────────────────────┐
│ 任务场景管理器                               │
│ 悬停/阶跃/8字/螺旋/巡检/配送/编队/故障返航   │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 交互式实验配置 GUI                           │
│ 轨迹/规划/控制器/增强模块/扰动/编队/指标选择  │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 任务管理与模式决策                           │
│ 任务阶段、扰动识别、控制模式切换、降级策略     │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 可跟踪性感知路径规划与动态可行轨迹生成         │
│ A*/RRT*/B-spline/Minimum Snap/控制饱和风险检查 │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 外环控制器                                   │
│ 改进 PID / 线性 MPC / NMPC，输出期望加速度/姿态 │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ L1-inspired 扰动补偿                         │
│ 加速度残差估计、低通滤波、风扰/质量变化补偿    │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ Safety Filter 安全过滤器                     │
│ 倾角/推力/速度/高度/障碍距离/机间距离         │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ INDI 姿态内环                                │
│ 增量非线性动态逆、角加速度反馈、快速姿态响应  │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 控制分配与执行器故障重构                     │
│ 总推力/力矩 → 电机指令，效率矩阵修正          │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ MWORKS 四旋翼动力学模型                      │
│ 机体/电机/传感器/扰动/可视化/结果输出         │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ 外部可视化与视频渲染                         │
│ TCP/UDP 状态流、Unreal 场景、跟随相机、雷达区域 │
└───────────────────┬─────────────────────────┘
                    ▼
┌─────────────────────────────────────────────┐
│ Syslab/MCP 自动化仿真评估                    │
│ 结果读取、指标计算、曲线绘制、报告素材导出    │
└─────────────────────────────────────────────┘
```

---

## 4. 关键创新点

### 4.1 自适应鲁棒位姿控制

1. **统一控制链路**：MPC/NMPC 外环、INDI 姿态内环、L1-inspired 加速度残差补偿、安全过滤和故障重构串成一条可替换控制链。
2. **扰动识别与模式切换**：根据速度/加速度残差、姿态残差、电机效率 eta 和传感器噪声指标，切换 `NORMAL`、`WIND_REJECTION`、`MASS_ADAPTATION`、`FAULT_TOLERANT`、`SAFETY_PROTECTION` 和 `EMERGENCY_LAND`。
3. **逐级消融验证**：PID → 改进 PID → PID-INDI/线性 MPC-INDI → NMPC-INDI → +L1-inspired → +Safety/Fault，每个增强模块必须有独立对比指标。

### 4.2 可跟踪性感知规划创新

1. **Trackability-aware Trajectory Planning**：规划轨迹不仅满足几何避障，还检查速度、加速度、jerk、倾角、推力余量和电机饱和风险。
2. **控制反馈修正轨迹**：若仿真结果显示跟踪误差或饱和比例过高，自动放大时间分配、降低参考速度或增加转弯半径。
3. **轻量 EGO-inspired 局部优化**：不强依赖完整 ROS/ESDF，只用局部障碍物几何距离构造碰撞代价和安全引导路径。

### 4.3 故障诊断与安全降级创新

1. **故障链路闭环**：故障注入 → 残差检测 → 故障分类 → 控制分配重构 → 安全降级 → 指标评估。
2. **分级容错**：轻度故障继续任务，中度故障启用重构，重度故障降级返航或安全降落。
3. **事件日志驱动展示**：每次故障、模式切换、安全保护和降级动作都记录到 event_log，支撑报告和视频复现。

### 4.4 多机协同任务创新

1. Leader-Follower 编队；
2. 多队形配置和平滑切换；
3. 狭窄通道自动队形收缩；
4. 机间安全距离约束；
5. Leader 规划绕障 + Follower 可行性检查；
6. Leader 故障或 Follower 异常时的队形降级策略。

### 4.5 智能仿真评估创新

1. 多场景扰动测试矩阵；
2. 风扰、质量摄动、电机效率下降、传感器噪声；
3. Syslab 批量实验与自动指标；
4. 健康度评分：tracking、robustness、safety、energy、smoothness、fault_tolerance；
5. 数据驱动三维回放：读取 MWORKS 导出的 CSV/JSON，生成真实仿真数据对应的视频素材。

### 4.5.0 外部仿真平台对标结论

近期调研对象包括 RflySim、MATLAB UAV Toolbox、Flightmare、RotorS、XTDrone、Pegasus Simulator 和 AirSim。结论不是照搬其中任一平台，而是吸收它们的架构分工：

| 参考平台 | 可借鉴点 | 本项目采用方式 |
|---|---|---|
| RflySim | MBD 主线、PX4/Simulink/DLL/CopterSim、SIL/HIL、UE 场景、UDP/共享内存、多机集群测试 | 只作为机制对标、协议参考和可授权资产迁移来源；最终渲染运行时采用项目自有 UE5 工程 |
| MATLAB UAV Toolbox | Scenario 配置、传感器仿真、Unreal 场景、自定义场景、代码生成出口 | 设计 `Quadrotor Experiment Studio`，统一生成 scenario/controller/planner/render 配置 |
| Flightmare | 物理引擎和渲染引擎解耦，Unity/渲染层只负责场景和传感器输出 | MWORKS 输出真实状态，UE5 读取 raw/native/realtime frame，不回写控制结论 |
| RotorS / XTDrone | ROS/Gazebo 下的传感器、控制器、消息接口和多机组织方式 | 借鉴接口分层，不把 ROS/Gazebo 作为当前比赛主证据链 |
| Pegasus Simulator | PX4/ROS2/Isaac Sim、多机和 GUI 化配置 | 借鉴插件化和场景切换思想，避免引入重型 Isaac 依赖 |
| AirSim | Unreal 视觉、API 控制、PX4/ArduPilot SIL/HIL | 仅作为 UE 视觉桥接参考，不作为唯一方案 |

因此当前技术路线确定为：

```text
MWORKS/Sysplorer/Sysblock:
  dynamics + controller + planner + disturbance + metrics truth

Syslab/Python:
  batch orchestration + metrics + evidence packaging + UDP playback

Unreal/UE5:
  project-owned render-only scene + material + camera + radar sector + video capture

Future export:
  C/C++ controller wrapper + ROS2/Gazebo/PX4 adapter template
```

不能让外部渲染器、ROS 节点或 UE 蓝图成为控制器真实性来源。若 UE 画面和 MWORKS 指标冲突，以 MWORKS raw/native result、metrics 和 event_log 为准，UE 只作为人工审查和视频表达。

### 4.5.0.1 高效实时架构基准：Gazebo/RflySim 对标

Gazebo 和 RflySim 低延迟的共同点不是“渲染更强”，而是把求解、控制、传感器、渲染和 GUI 分成不同进程/模块：

| 机制 | Gazebo 做法 | RflySim 做法 | 本项目采用方式 |
|---|---|---|---|
| 求解与渲染解耦 | `server` 运行物理/传感器系统，`client` 只做 GUI | CopterSim/PX4/外部 DLL 与 RflySim3D 分进程 | MWORKS/Sysplorer 是真值求解端，UE5 是只读渲染端 |
| 高频通信 | Gazebo Transport 发布 pose/sensor/command | 固定 UDP 端口、二进制结构体、共享内存图像/点云 | 使用固定结构状态帧，避免 JSON 大包进入实时闭环 |
| 独立频率 | physics step、sensor update、GUI FPS 分开 | 控制、视觉传感器、RflySim3D FPS 分开 | 控制/建图/规划/渲染分别限频和超时 |
| 轻量批处理 | headless server 可脱离 GUI | `CopterSimNoUI`、低 FPS、大集群独立运行 | 指标批处理不依赖 UE5；视频审查才打开渲染 |
| 可选同步 | lockstep 用于一致性，牺牲实时率 | 通过时间戳/心跳/端口分发维持一致性 | 录制可用 lockstep；日常仿真默认异步丢弃过期帧 |

因此项目实时架构固定为：

```text
MWORKS/Sysplorer authoritative solver
  -> StateBroadcaster @20-50 Hz
  -> SensorMapWorker @20 Hz minimum
  -> PlannerWorker @5-20 Hz
  -> ControllerWorker @20-50 Hz
  -> UE5 RenderClient @30-60 FPS
```

关键约束：

1. 控制器和安全过滤至少按 `20 Hz` 工作，目标 `50 Hz`；局部建图至少 `20 Hz`，传感器/点云输入可 `20-30 Hz`；UE5 渲染目标 `30-60 FPS`。
2. UE5 不进入控制器求解环。渲染卡顿只能影响视频，不允许影响 MWORKS 指标、控制输出或 event_log。
3. 每个实时包必须包含 `seq`、`sim_time`、发送时间戳、pose、velocity、attitude、motor/fault/mode、reference、local plan 摘要和质量标志。
4. 超时策略必须明确：传感器过期则保持上一帧并标记 stale；规划超时则复用上一条仍安全轨迹或减速悬停；控制超时则触发 Safety Filter/Hold；渲染超时直接丢帧。
5. RflySim 不作为最终渲染运行时。它只用于学习 UDP/共享内存/多进程分工、车辆参数数据库、Mid360/点云接口和场景资产组织。经用户授权后，可把可编辑地图、模型、材质、XML 或参数迁移到项目自有 UE5 工程，再重命名、重材质、重碰撞代理和重场景逻辑。
6. 地图真值不能从 RflySim 运行时偷读给规划器。最终数据结构应落到项目自己的 `render_world`、`world_geometry.primitives`、`collision_proxy` 和 `planner_known_map` 中。

### 4.5.1.1 RflySim/UE 场景系统架构

后续视觉展示不再局限于 MWORKS/Sysplorer 原生动画。最终外部场景层采用项目自有 UE5 渲染器，承担高质量地图、材质、相机、雷达点云显示和视频录制；MWORKS 仍承担控制器、动力学、轨迹、扰动和指标真值。RflySim3D 只作为参考运行时和资产迁移来源，不作为最终交付视频的必需依赖。

场景 Profile 分为以下几类：

| Profile | 场景来源 | 典型任务 | 说明 |
|---|---|---|---|
| `dense_forest` | 开源/可用 UE 森林、密林、山地资产或 RflySim 可用场景 | 密集障碍避障、局部感知、路径重规划 | 重点验证未知地图下的规划能力，不把全局地图提前给规划器 |
| `maze_building` | 自建迷宫或 RflySim/UE 建筑室内场景 | 狭窄通道、墙体遮挡、局部地图 | 验证雷达不能穿墙、局部规划和安全距离 |
| `old_factory` | RflySim `OldFactory` 或自有 UE 工厂场景 | 建筑/管道/墙体穿越 | 当前已确认本机 RflySim 能加载该场景并采集 Mid360 点云 |
| `park_patrol` | RflySim `NeighborhoodPark`、UE 公园/社区资产 | 巡检、物流、树木避障 | 适合答辩视频中的真实业务场景 |
| `gate_ring_indoor` | RflySim `VisionRing`、`RobotMissionChallenge`、`MatchScene2025` 或自建室内场景 | 斜穿框、穿环、高速姿态控制 | 重点展示姿态控制、轨迹跟踪和相机视角 |
| `open_grass_wind` | RflySim `Grasslands` / `3DDisplay` / 自建开阔草地 | 风扰、阵风、电机效率退化 | 场景简单，突出控制鲁棒性 |

每个 Profile 必须有两套地图表达：

```text
render_world:
  project-owned UE5 可见真实地图、材质、树木、墙、地形、框、环

planner_known_map:
  起始为空或局部已知；只由 Mid360/抽象传感器按范围、FOV、遮挡逐步更新
```

这样视频中可以显示“真实世界”和“无人机当前已知点云/局部地图”两个视图。真实地图界面用于观众理解环境；点云/局部地图界面用于证明规划器没有偷看未探索区域。若采用单窗口展示，则真实地图保留正常材质，已感知区域用扇形/点云/高亮叠加，未感知区域只能弱化显示，不能作为规划输入。

随机化策略只在仿真开始前执行：

```text
select_profile
  -> choose seed
  -> randomize obstacle/building/gate relative transforms
  -> save scenario_profile.yaml
  -> run simulation with local sensing only
```

随机位置必须落盘并可复现。GUI 可以提供“随机生成建筑物/障碍物位置”按钮，但生成后需要固化 seed、地图对象 ID、包围盒和材质，不允许仿真中途修改真值地图，除非该场景明确为动态障碍物测试。

延迟与卡死保护按外部仿真平台思路设计：

1. 控制器主循环和渲染循环分离，渲染卡顿不影响控制器证据；
2. 规划器设置最大求解时间预算，例如 20 Hz 下单步预算不超过 50 ms，超时则沿用上一条安全局部轨迹或进入 Hold/Safety Filter；
3. 点云处理、占据栅格、路径优化和渲染 actor 更新使用不同频率，不把所有对象放进同一个求解器；
4. 高质量地图使用视觉 mesh，规划使用简化碰撞代理/占据栅格；
5. 如果 NMPC/QP/Safety Filter 超时，event_log 必须记录 `planner_timeout`、`controller_timeout` 或 `safety_hold`。

多机扩展仍使用同一套 Profile，只增加 `uav_count`、队形、机间距离和通信延迟。密林、迷宫、旧工厂、公园、穿环和风扰场景都应支持从 1 架扩展到 3-5 架，但 P1 先完成单机闭环，P2 再扩展 Leader-Follower 编队。

### 4.5.1 外部可视化渲染创新

当前不再把 Gazebo 作为近期主线，也不把 RflySim3D 作为最终渲染主线。近期视频展示采用“MWORKS/Sysplorer 负责真实闭环仿真，项目自有 UE5 工程负责视觉表达”的解耦方案。外部渲染只作为显示层，不参与动力学、控制、路径规划、碰撞验收或指标计算。MATLAB UAV Toolbox / Simulink Coder 的“模型到代码再到 Gazebo/外部仿真”能力作为后续工程化出口保留，当前不阻塞视觉展示路线。

外部渲染必须采用 **asset-first** 路线。此前从零堆盒子、立柱和简单材质只能作为调试场景，不足以支撑最终视频质量。正式 UE5 场景应优先复用成熟场景和模型资产：

1. 本地 `references/Sunray/simulation/sunray_simulator/models` 中已有 Sunray150/Mid360、Gazebo/SDF 场景、AWS RoboMaker residential/retail、树、房屋、窗框、门框、目标区、地形等资产；
2. 本地 `*.world` 文件可作为场景布局参考，例如 planning_test、outdoor、vins_house、yundrone_cup、competition map 等；
3. RflySim/CopterSim 和 RflyExpCode 公开仓库主要提供 Simulink/MAVLink/HIL/实验接口参考；本机 `D:\PX4PSP` 中的 RflySim3D/UE 内容可作为资产迁移候选，但迁移后必须进入项目自有 UE5 工程并重新建立材质、碰撞代理、命名和场景配置；
4. UE Marketplace 或其他开源 UE 场景可作为 P2 视觉升级，但必须先检查许可证、文件大小、贴图依赖、缩放比例、碰撞代理和性能。

因此视觉渲染工作流从“生成几何”调整为：

```text
场景/模型资产盘点
  → SDF/world/mesh 解析
  → scene_asset_registry
  → RflySim 原生体验 / Unreal 导入、材质、碰撞代理、跟随相机
  → MWORKS 状态流驱动
  → 视频审查
```

手工生成的障碍物和栅格只保留为规划真值、碰撞调试和局部感知叠加，不作为最终视觉主体。

RflySim 安装完成后，不能直接把旧 MWORKS 可视化场景搬到 Unreal 并继续堆图形对象。正确顺序是先研究 RflySim 自身的机制：运行 RflySim3D/CopterSim 示例，确认地图切换、车辆模型、相机、地形服务、Mid360/点云、ESDF/Voronoi 路径规划和多机显示的真实效果，然后把可用机制和经授权的资产迁移到项目自有 UE5 工程。若 `RflySim3D` / `RflySimUE` / `RflySimUE5` 只有打包后的可执行文件或 `.pak`，则只能作为机制参考，不能直接把其场景资产纳入仓库。任何 RflySim 资源进入本项目之前，必须先记录许可证、来源路径、文件大小、可编辑性、坐标系、材质依赖和碰撞代理。

当前最可落地的视觉路线是项目自有 UE5 场景。第一步跑通本机 RflySim 原生例程只是为了确认其通信、传感器、场景资产和参数库怎么组织；第二步把可复用部分迁移到 `QuadrotorMworksBridge` 和自有 UE5 场景。资产来源优先检查本地 `D:\PX4PSP` RflySim 安装目录和 `references/Sunray/simulation/sunray_simulator` 已有 Gazebo/SDF/DAE/PNG 资产，把 `*.world`、`model.sdf`、XML、材质和模型引用解析成 `scene_asset_registry`，而不是继续在 UE 中手搓低质量盒子场景。RflySim 的价值不仅是“状态流驱动外部 3D 引擎”，还包括已经组织好的 RflySim3D 场景、UDP/共享内存传感器链路、CopterSim 参数库、地形扫描服务和多机场景流程。

本机 `D:\PX4PSP` 已确认包含可用的 RflySim3D 运行/工程目录和 RflySimSDK。可利用的内容包括：`RflySim3D.exe`、`RflySim3D.uproject`、多张 `.umap` 场景、`FSJ150` 四旋翼模型 XML、障碍物/树/石头/城市/工厂等模型引用、`UE4CtrlAPI.py` 的 UDP 位姿/电机转速推送接口、`VisionCaptureApi.py` 的相机/点云/Mid360 请求接口，以及 `3.RflySim3DUE`、`8.RflySimVision`、`10.RflySimSwarm` 下的场景控制、传感器和多机示例。`CopterSim\ModelData.db` 与 `CopterSim\external\XML\F450.xml` 提供了可读取的真实机体/动力参数来源，例如质量、惯量、机臂半径、推力系数、力矩系数、电机响应时间、悬停转速、电机/桨/电调/电池库。该目录只能作为外部依赖和参考，不整体进入仓库；其中存在多个超过 100 MB 的 UE 资产文件，且重分发许可未确认。

因此 RflySim 的当前定位只有参考/迁移，不作为最终运行时：

```text
route_a_reference_rflysim:
  MWORKS raw/native result
    -> Python/UDP bridge
    -> RflySimSDK UE4CtrlAPI.sendUE4PosFull
    -> inspect timing/sensor/camera/asset behavior

route_b_final_project_owned_unreal:
  MWORKS raw/native result
    -> QuadrotorMworksBridge
    -> project-owned UE scene/assets/camera/radar visualization
```

`route_a` 只能快速验证 RflySim 场景、传感器、相机和通信机制；`route_b` 是可提交、可控、可长期维护的比赛作品路线。两条路线都不改变控制器、规划器、碰撞检查和指标真值。

若 RflySim 下载或授权暂时不可用，备选调研路线不应转回 Gazebo 主线，而是继续保持“外部渲染只读”架构：

| 备选平台 | 采用方式 |
|---|---|
| Cosys-AirSim / Colosseum | 参考 Unreal 插件、API、传感器、相机和状态流组织方式 |
| CARLA-Air / CARLA | 参考高质量 UE 城市场景和道路/建筑资产组织方式；只在资产容易复用时采用 |
| AirSim | 参考成熟 Unreal API 和相机/分割/天气接口；因上游归档，不作为主依赖 |
| Flightmare / FlightGoggles | 参考物理和渲染解耦、敏捷飞行门框场景表达；不直接切换到 Unity 主线 |
| Pegasus Simulator | 参考多机/PX4/传感器架构；Isaac Sim 依赖过重，不作为当前路线 |
| XTDrone / RotorS | 参考 ROS/PX4 多机组织和接口命名；不作为视频渲染路线 |

因此后续不需要一次性下载所有仿真平台。只有当某个平台能提供明确价值时再抓取对应仓库：UE 插件/API 看 Cosys-AirSim 或 Colosseum，城市级 UE 场景看 CARLA-Air/CARLA，门框敏捷飞行表达看 Flightmare/FlightGoggles，ROS/PX4 多机组织看 XTDrone/RotorS。

Gazebo 的高效率值得学习，但不意味着要回到 Gazebo 主线。Gazebo 的关键不是“一个求解器同时承担一切”，而是后端物理、传感器、渲染、GUI 和通信拆成独立插件与进程：详细视觉网格只进入渲染管线，物理求解使用简化碰撞代理，传感器按独立频率更新，GUI 客户端可与仿真服务端分离。当前 Sysplorer 卡顿的根本风险正相反：把大量地形块、障碍物、雷达高亮和视觉效果直接放入模型层，会让显示对象影响模型检查、求解和 GUI 响应。因此本项目只借鉴 Gazebo 的分层架构：

```text
MWORKS/Sysplorer 只负责闭环真值
外部 UE5 渲染器负责视觉质量
TCP/UDP/raw/native result 负责状态传输
scene_asset_registry 负责视觉资产与碰撞真值绑定
```

后续若研究 Gazebo 官方资料，优先看 Gazebo Sim architecture、gz-rendering、gz-sensors、gz-physics 和 sdformat，而不是下载大型 Fuel 场景或把 Gazebo 作为当前比赛主证据链。

外部渲染层需要解决 Sysplorer 原生动画难以稳定处理的问题：

1. **无人机与背景上色**：Sunray150 机体、桨叶、传感器、障碍物、地面、轨迹和安全区域在渲染器内使用独立材质，不再依赖 Sysplorer 大量 `Shape` 组件调色。
2. **雷达扫描表达**：Mid360 抽象扫描范围由状态流驱动，显示前向扇形、近距原色区域、远距弱化区域和已感知障碍高亮；未感知区域不参与规划显示。
3. **跟随相机与视频录制**：外部渲染器提供跟随相机、固定全景相机、俯视相机和局部第一视角，避免人工拖动 Sysplorer 视角导致卡顿。
4. **数据真实性**：所有位置、姿态、参考轨迹、局部计划、障碍显示状态和事件标记均来自 MWORKS raw/native result 或实时 TCP/UDP 状态流，禁止外部渲染器自行补轨迹或改结果。

场景系统采用 `Scenario Profile` 抽象，而不是为每个展示目标单独硬编码一个模型。Profile 由以下对象组成：

```yaml
scenario_profile:
  world_geometry: map / obstacle_field / walls / gates / terrain
  mission_route: start / goal / waypoint / gate_sequence
  planner_profile: waypoint / astar / rrt_star / ego_inspired / min_snap / bspline
  controller_profile: pid / linear_mpc / nmpc / indi / l1 / safety_filter / fault_allocator
  disturbance_profile: wind / gust / mass / inertia / motor_efficiency / sensor_noise
  render_profile: sysplorer_native / unreal_offline / unreal_realtime
  evaluation_spec: metrics / event_log / manual_review / video_clip
```

这样斜框穿越、自主避障、风扰鲁棒、旋翼退化和多机编队都使用同一套配置入口，只替换 Profile，而不是复制模型和脚本。

推荐分两步实现：

```text
step1_offline_playback:
  MWORKS raw.csv / replay.json
    -> UDP/TCP frame stream
    -> Unreal receiver
    -> UAV pose / reference / local plan / radar sector / map material

step2_realtime_bridge:
  Sysplorer running model
    -> state publisher at 20-50 Hz
    -> Unreal receiver
    -> synchronized review and recording
```


#### 4.5.2 模型到外部仿真/代码生成出口

该功能放在 UE5 视频展示之后实施，不作为当前单机闭环与 UE5 渲染的前置条件。目标是学习 MATLAB/Simulink 的工具箱式工作流：用户在实验软件中选择控制器和场景后，可以从 MWORKS.Sysblock/等价控制器生成标准 C/C++ 控制器接口，再生成 ROS2/Gazebo/PX4 或其他外部仿真适配模板。

推荐分阶段验收：

1. **控制器接口冻结**：统一 `QuadState`、`QuadReference`、`QuadCommand`、`QuadEvent` 等结构体，保证 MWORKS、C++、UE5、后续 ROS2/Gazebo 适配器使用同一字段语义。
2. **C/C++ 导出一致性测试**：同一输入序列下，导出控制器输出与 MWORKS raw CSV 控制输出在容差内一致。
3. **外部仿真适配模板**：生成 ROS2 node / Gazebo plugin / PX4 offboard adapter 的骨架代码，但不把 Gazebo 调参作为当前比赛主线。
4. **部署证据分级**：C++ 一致性测试可作为工程化证据；Gazebo/PX4 运行结果只有真实跑通并通过指标检查后，才能进入附加展示，不替代 MWORKS/Sysplorer 主证据。

现阶段优先使用 DeviceDrivers 或项目 UDP bridge 把 MWORKS 状态流送入 UE5。ROS/rosbridge 仅作为无人船资料中的通信模式参考，不引入完整 ROS 导航栈。

### 4.6 交互式实验配置与算法组合创新

最终作品需要提供类似 MATLAB App Designer / UAV Toolbox / Simulink 参数面板风格的交互式实验软件，暂定名为 `Quadrotor Experiment Studio`。该模块不是 QGroundControl/MAVLink 地面站，也不是 Gazebo/PX4 SITL 主线；它只作为本项目的实验配置、仿真编排、日志分析、UE5 渲染控制和证据审查入口。GUI 不替代 MWORKS.Sysplorer/Sysblock 模型本身，而是生成标准 `scenario.yaml`、控制器参数、规划器参数、批量运行任务和渲染配置。若接入 UE5，该 GUI 只负责选择渲染 preset、启动/停止状态流和打开录屏审查，不让 UE5 反向修改控制器或规划器。后续 C++/ROS2/Gazebo 导出功能也必须作为独立导出向导存在，不能混入当前控制证据链。

核心能力：

1. **算法组合选择**：可选择轨迹生成、路径规划、位置控制、姿态控制、增强模块、鲁棒扰动和多机编队配置。
2. **参数可视化**：显示起点、终点、地图模式、扰动强度、控制器频率、传感器频率、约束阈值和当前质量门。
3. **可复现实验生成**：随机障碍物只随机位置，数量固定，使用 seed 固化；GUI 生成的所有配置必须落盘。
4. **仿真编排**：调用现有 MWORKS MCP / Syslab / Python 工作流，运行单场景或批量矩阵，不直接手工改模型源码。
5. **日志分析器**：读取 raw CSV、metrics JSON/CSV、event_log、MCP log、native_result manifest 和人工审核清单，定位模型检查、授权、仿真、质量门和 GUI 审查问题。
6. **指标面板**：读取 metrics JSON/CSV 和人工审核清单，展示 RMSE、最大误差、稳态误差、控制能量、安全距离、编队误差和鲁棒性保持率等指标。
7. **地图渲染审查**：规划地图可以保持高精度栅格，Sysplorer 动画只显示低复杂度合并几何和局部感知高亮；高质量视频渲染交给外部 Unreal 显示层。任何显示方案必须经人工审核；若效果差或拖慢/破坏求解器，立即回退到当前稳定的低复杂度审查方案。

---

## 5. 模块关系

| 模块 | 上游 | 下游 | 可裁剪性 |
|---|---|---|---|
| 场景管理 | 用户配置 | 规划/控制/扰动 | 必做 |
| 交互式配置 GUI | 用户选择/模板 | scenario/controller/planner 配置 | 建议 |
| 任务管理 | 场景/状态/事件 | 规划/控制模式 | 建议 |
| 路径规划 | 场景地图 | 轨迹平滑 | 可裁剪 |
| 轨迹平滑 | 粗路径 | NMPC | 建议 |
| 可跟踪性检查 | 轨迹/控制限制 | 轨迹修正/控制器 | 建议 |
| 外环控制器 | 轨迹/状态 | 扰动补偿/INDI | 核心 |
| Safety | 外环/规划 | INDI/控制分配 | 建议 |
| INDI | 姿态参考 | 控制分配 | 核心 |
| L1-inspired 补偿 | 状态误差 | 外环/INDI | 建议 |
| 故障重构 | 故障估计 | 电机指令 | 可裁剪 |
| 编队 | Leader 轨迹 | 多机参考 | 建议 |
| MCP | 模型/结果 | 自动评估 | 可裁剪 |

---

## 5.1 完整四旋翼系统组成

当前单机主线使用 `sunray150_with_mid360` 作为完整四旋翼系统背景。系统级闭环的核心是机体动力学、电机/旋翼、控制器、扰动、安全状态和结果导出；CUAV、Mid360、ORIN NX 图片只作为图形化仿真和答辩素材参考，不作为新增实物仿真部件。

| 层级 | 模块 | 当前实现 | 后续扩展 |
|---|---|---|---|
| 机体结构 | Sunray150 机架、四电机、四桨 | `QuadrotorModel.Mechanics.QuadChassis`，Sunray150 STL，1.0 kg 等效质量 | 进一步按实物手册细化电池/载荷质量分布 |
| 执行机构 | 电机、桨叶、旋翼升力 | 电机转速输入、升力系数、电机速度传感器 | 电机效率、响应延迟、饱和和温升模型 |
| 飞控/定位背景 | PX6C / CUAV V6X / GPS | 当前不改变官方物理模型；仅在模式管理和安全逻辑中抽象健康状态 | 传感器冗余、Offboard loss、Return/Land 状态机 |
| 环境感知背景 | Livox Mid360 | 当前不改变官方物理模型；规划层按 20 Hz 局部栅格接口建模 | 量程、FOV、噪声、局部障碍更新 |
| 机载计算背景 | ORIN NX 级计算平台 | 当前不改变官方物理模型；用于解释规划、安全监督和多机通信的计算平台 | 后续承载路径规划、局部地图、安全过滤和多机通信 |
| 控制软件 | AWFF / INDI / L1 / MPC / Safety | 已有 Sysblock 控制器和包装实验 | 与规划、模式管理、故障诊断统一接口 |
| 任务软件 | 轨迹、规划、返航、降落 | 已有官方轨迹、返航/降落实验 | 单机避障、多机编队、动态重规划 |

实现边界：

```text
必须真实参与仿真闭环：
  机体动力学、电机/旋翼、传感器状态输出、控制器、电机指令、结果导出

当前作为系统级抽象：
  V6X/PX6C 飞控硬件、GPS/GNSS、Mid360 局部栅格更新节拍、ORIN NX 计算平台

暂不作为核心依赖：
  MAVLink 驱动、真实点云 SLAM、完整 ROS/PX4/Gazebo 通信链路

显示层边界：
  Sysplorer 原生动画用于模型级快速审查和证据兜底；
  Unreal 外部渲染用于视频级视觉表达；
  二者都不能改变 MWORKS 闭环仿真结果。
```

频率约定：

```yaml
system_rates:
  controller_update_rate_hz: 20
  mid360_local_grid_update_rate_hz: 20
  gps_navigation_update_rate_hz: 20
  raw_output_sample_rate_hz: 20
```

CUAV 实物图片位于：

```text
references/CUAV/V6X.png
references/CUAV/GPS.png
references/CUAV/MId360.png
references/CUAV/ORIN NX.png
references/CUAV/Sunray150-正.png
references/CUAV/Sunray150-侧.png
references/CUAV/motor.png
references/CUAV/电池.png
references/CUAV/电调.png
```

这些图片用于完整系统图形化仿真画布、模型图标、报告和答辩解释硬件组成；当前不把图片对应硬件作为新增物理部件接入 `QuadChassis`。`references/CUAV/` 保留无背景原图，模型显示副本统一生成到 `QuadrotorModel/Resources/Images/`。模型显示副本直接使用透明 PNG 原图，保留每张图片自身宽高比例和透明背景。`GPS.png`、`V6X.png`、`MId360.png`、`ORIN NX.png`、`Sunray150-正.png`、`motor.png`、`电池.png`、`电调.png` 分别作为 `QuadrotorExperiments.Sunray150CompleteSystemGraphical_Sysblock` 中感知、飞控、机载计算、机体、电机、电池和电调顶层模块的图标。为避免 Modelica URI 中空格或中文文件名导致解析差异，机载计算平台图片在模型资源目录中保存为 `ORIN_NX.png`，电池和电调分别保存为 `Battery.png`、`ESC.png`，Sunray150 正视图保存为 `Sunray150.png`。`Sunray150-侧.png` 暂不接入模型。三维动画仍使用官方 STL 机体。

---

## 6. 设计边界

### 做

- 位姿控制；
- 路径/轨迹生成；
- 编队轨迹跟踪；
- 仿真评估；
- 多场景验证。

### 不做核心依赖

- 真实硬件；
- 全 ROS 自主导航栈；
- 点云 SLAM；
- 完整 EGO-Planner 移植；
- 端到端 RL 控制；
- 真实视觉感知。
- Gazebo/PX4 SITL/HITL 部署链路。

---

## 7. 最终交付结构

```text
QuadrotorModel/
controllers/
planners/
scenarios/
scripts/
results/
docs/
Design/
workflows/
```

当前仓库不再为空目录预留位置。仅当有真实内容时再创建 `results/{group}/{scene}/{experiment}/raw/`、
`results/{group}/{scene}/{experiment}/metrics/`、`results/{group}/{scene}/{experiment}/figures/`、`docs/figures/` 等输出目录。

核心目录职责：

```text
QuadrotorModel/  官方四旋翼案例模型，P0 baseline 入口
controllers/     控制器参数、控制器实现说明和后续替换模块
planners/        路径规划与轨迹生成配置
scenarios/       场景配置，绑定模型入口、控制器、扰动和结果路径
scripts/         QA、指标计算、结果提取和图表生成脚本
results/         仿真日志、原始结果、指标和展示素材
docs/            用户手册、报告、MWORKS 转换资料和索引
Design/          系统设计、接口、验收和实现路线
workflows/       Codex/MCP 自动化操作流程
```

P0 官方案例复现优先使用以下真实模型入口：

| 场景 | 官方模型入口 | 用途 |
|---|---|---|
| 阶梯爬升 | `QuadrotorModel.Examples.Example1` | baseline smoke test、数据导出、指标链路验证 |
| 螺旋爬升 | `QuadrotorModel.Examples.Example2` | 复杂轨迹 baseline 对比 |
| 8 字形运动 | `QuadrotorModel.Examples.Example3` | 赛题重点轨迹 baseline 对比 |

---

## 8. 参考算法路线

| 方向 | 借鉴点 | 本项目定位 |
|---|---|---|
| Minimum Snap | 四旋翼平滑轨迹、速度/加速度连续 | P2 轨迹平滑或离线参考生成 |
| EGO-Planner / Fast-Planner | ESDF-free、B-spline、局部优化思想 | 只借鉴轻量思想，不完整移植 ROS 栈 |
| INDI | 角加速度反馈、降低模型依赖 | P1 姿态内环增强 |
| MPC/NMPC | 约束跟踪、预测优化 | P1/P2 外环增强，保留线性 MPC 回退 |
| L1 adaptive control | 快速扰动补偿思想 | 采用 L1-inspired 残差估计与低通补偿 |
| CBF / Safety Filter | 安全关键约束保护 | P1/P2 安全过滤，初赛可用裁剪式限幅 |
| MCP | 模型查询、仿真调用、结果读取 | P2 自动化验证辅助，不作为控制算法创新 |
