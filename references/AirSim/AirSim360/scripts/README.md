# Scripts

Small cross-cutting utilities (format conversion, batch checks, packaging helpers) that are **not** tied to a single dataset folder.

Dataset-specific readers live next to their data:

- `data/demo_sample/scripts/`
- `waypoints/scripts/`
