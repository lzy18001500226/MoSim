# 变量 a 和 b 和以前一样被绑定给前两个参数，后面的参数整个做为迭代集合被绑定到变量 x 上，x 为一个元组。
bar(a, b, x...) = (a, b, x)
# bar (generic function with 1 method)

bar(1, 2)
# (1, 2, ())

bar(1, 2, 3)
# (1, 2, (3,))

bar(1, 2, 3, 4)
# (1, 2, (3, 4))

bar(1, 2, 3, 4, 5, 6)
# (1, 2, (3, 4, 5, 6))
