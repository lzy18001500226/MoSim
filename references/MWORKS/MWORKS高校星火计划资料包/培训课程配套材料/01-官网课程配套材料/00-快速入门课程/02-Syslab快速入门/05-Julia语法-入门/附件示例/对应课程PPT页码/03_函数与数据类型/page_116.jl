X = [1, 2, 3, 4]
X[1] = 3; #将X的第1个值改为3
X[2:3] = [0, 0];
X = [1, 2]
Y = X
Y[1] = 5 #X的值是否有变化？
X #X的值也会跟随Y的值变化
pointer(X) == pointer(Y) #地址相同  
X = [1, 2];
Y = copy(X);
pointer(X) == pointer(Y) #地址不同  
X == Y #数值相同
Y[1] = 5 #X的值是否有变化？
X #X的值不会跟随Y的值变化





