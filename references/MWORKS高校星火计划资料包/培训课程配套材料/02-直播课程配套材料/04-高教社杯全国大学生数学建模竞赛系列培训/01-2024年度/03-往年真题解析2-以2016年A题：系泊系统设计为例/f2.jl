##################################
######悬挂重物时悬垂链计算公式######
##################################
function f2(x, x0, y0, a, θ)
    y = a * cosh((x - x0)/a + log((1 + sin(θ)) / cos(θ))) + y0 - a / cos(θ)

    return y
end