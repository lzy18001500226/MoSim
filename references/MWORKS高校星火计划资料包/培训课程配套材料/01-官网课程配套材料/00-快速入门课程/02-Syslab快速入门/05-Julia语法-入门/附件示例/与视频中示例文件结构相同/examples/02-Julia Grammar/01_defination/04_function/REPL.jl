# 使用数学等式定义函数
myfunc1(x) = x^2 + 3 * x + 1
myfunc1(2)

# 使用 function 关键字定义函数
function myfunc2(x)
    x^2 + 3 * x + 1
end
myfunc2(2)

function myfunc3(x)
    return x^2 + 3 * x + 1
    x
end
myfunc3(2)

