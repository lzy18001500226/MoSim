using TyControlSystems
##【示例1】建立传递函数模型 
# 方式一
num = [12, 24, 12, 20]
den = [2, 4, 6, 2, 2]
G = tf(num, den)
# 方式二
G = tf([12, 24, 12, 20], [2, 4, 6, 2, 2])

#-----------------------------------------#

##【示例2】通过Laplace算子定义，通过有理分式方式建立模型
s = tf('s') # 定义Laplace算子
G = 3 * (s^2 + 3) / ((s + 2)^3 * (s^2 + 2 * s + 1) * (s^2 + 5))

#-----------------------------------------#

##【示例3】Syslab中建立离散系统传递函数模型，其采样周期为 T = 0.1s
num = [6 -0.6 -0.12]
den = [1 -1 0.25 0.25 -0.125]
H = tf(num, den, 0.1)

#-----------------------------------------#

##【示例4】构建MIMO系统的传递函数矩阵
s = tf('s')
G11 = tf([1, -1], [1, 1])
G12 = 100 / ((s + 4) * (s + 10.625))
G21 = tf([1, 2], [1, 4, 5])
G22 = tf([1, 2], [1, 0, 4, 20])
# 通过矩阵命令构造传递函数矩阵 
G = [G11 G12; G21 G22]

#-----------------------------------------#

##【示例5】构建MIMO系统的传递函数矩阵,采样时间为ts=0.2s
nums = [[[1]] [[1, 0]]; [[-1, 2]] [[3]]]
dens = [[[1, 0.3]] [[1, 0.3]]; [[1, 0.3]] [[1, 0.3]]]
ts = 0.2
H = tf(nums, dens, ts)

#-----------------------------------------#

#【示例6】构建双输入双输出的系统状态空间模型
A = [-12 -17.2 -16.8 -11.9; 6 8.5 9.5 8; 5 8.7 3.5 6; -6 -6.5 -9.7 -5]
B = [2.5 0.1; 2 0.5; 3 1; 0 0.5]
C = [2 3 0.8 0; 0.5 0.56 0.3 -1]
D = zeros(2, 2)
G = ss(A, B, C, D)

#-----------------------------------------#

##【示例7】构建零极点增益模型
z = [0]
p = [1 - 1im, 1 + 1im, 2]
k = -2
G = zpk(z, p, k)

#-----------------------------------------#

##【示例8】构建离散时间零极点增益模型，采样时间为 𝑡𝑠=0.125𝑠
z = [[[0]]; [[-0.5]]]
p = [[[0.3]]; [[0.1 - 1im, 0.1 + 1im]]]
k = [[1]; [2]]
G = zpk(z, p, k, 0.125)
