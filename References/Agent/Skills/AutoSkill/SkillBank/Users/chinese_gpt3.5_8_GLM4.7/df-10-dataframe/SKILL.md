---
id: "b106e278-06d1-44ea-afc5-287c9556cf37"
name: "df / 10 / dataframe"
description: "General SOP for common requests related to df, 10, dataframe."
version: "0.1.0"
tags:
  - "df"
  - "10"
  - "dataframe"
triggers:
  - "Use when the user asks for a process or checklist."
  - "Use when you want to reuse a previously mentioned method/SOP."
examples:
  - input: "Break this into best-practice, executable steps."
---

# df / 10 / dataframe

General SOP for common requests related to df, 10, dataframe.

## Prompt

Follow this SOP (replace specifics with placeholders like <PROJECT>/<ENV>/<VERSION>):
1) Offline OpenAI-format conversation source.
2) Title: 0dae0cc83a35fd99d7797bed42de735b.json#conv_1
3) Use the user questions below as the PRIMARY extraction evidence.
4) Use the full conversation below as SECONDARY context reference.
5) In the full conversation section, assistant/model replies are reference-only and not skill evidence.
6) Primary User Questions (main evidence):
7) python中dataframe怎么选择2022年的所有数据行
8) python中dataframe有列A和列B，按如下规则生成列C：如果列A的值大于列B的值，那么列C取值为1；否则列C取值为0
9) 遍历DatetimeIndex对象中每一个日期的for循环语句怎么写
10) 怎么对dataframe切片出某一行某一列的值

For each step, include: action, checks, and failure rollback/fallback plan.
Output format: for each step number, provide status/result and what to do next.

## Triggers

- Use when the user asks for a process or checklist.
- Use when you want to reuse a previously mentioned method/SOP.

## Examples

### Example 1

Input:

  Break this into best-practice, executable steps.
