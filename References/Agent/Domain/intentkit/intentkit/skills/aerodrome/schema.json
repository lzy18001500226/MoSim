{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Aerodrome",
  "description": "Swap tokens and manage Slipstream CL liquidity positions on Aerodrome DEX (Base)",
  "x-icon": "/skills/aerodrome/aerodrome.svg",
  "x-tags": [
    "DeFi"
  ],
  "properties": {
    "enabled": {
      "type": "boolean",
      "title": "Enabled",
      "description": "Whether this skill is enabled",
      "default": false
    },
    "states": {
      "type": "object",
      "properties": {
        "quote": {
          "type": "string",
          "title": "Swap Quote",
          "enum": [
            "disabled",
            "public",
            "private"
          ],
          "x-enum-title": [
            "Disabled",
            "Agent Owner + All Users",
            "Agent Owner Only"
          ],
          "default": "disabled",
          "description": "Get swap quotes from Aerodrome Slipstream"
        },
        "swap": {
          "type": "string",
          "title": "Execute Swap",
          "enum": [
            "disabled",
            "public",
            "private"
          ],
          "x-enum-title": [
            "Disabled",
            "Agent Owner + All Users",
            "Agent Owner Only"
          ],
          "default": "disabled",
          "description": "Execute token swaps on Aerodrome Slipstream (requires wallet)"
        },
        "get_positions": {
          "type": "string",
          "title": "View Positions",
          "enum": [
            "disabled",
            "public",
            "private"
          ],
          "x-enum-title": [
            "Disabled",
            "Agent Owner + All Users",
            "Agent Owner Only"
          ],
          "default": "disabled",
          "description": "View Aerodrome Slipstream liquidity positions"
        },
        "add_liquidity": {
          "type": "string",
          "title": "Add Liquidity",
          "enum": [
            "disabled",
            "public",
            "private"
          ],
          "x-enum-title": [
            "Disabled",
            "Agent Owner + All Users",
            "Agent Owner Only"
          ],
          "default": "disabled",
          "description": "Add liquidity to Aerodrome Slipstream pools (requires wallet)"
        },
        "remove_liquidity": {
          "type": "string",
          "title": "Remove Liquidity",
          "enum": [
            "disabled",
            "public",
            "private"
          ],
          "x-enum-title": [
            "Disabled",
            "Agent Owner + All Users",
            "Agent Owner Only"
          ],
          "default": "disabled",
          "description": "Remove liquidity from Aerodrome Slipstream positions (requires wallet)"
        }
      },
      "required": [
        "quote",
        "swap",
        "get_positions",
        "add_liquidity",
        "remove_liquidity"
      ],
      "description": "States for each Aerodrome skill"
    }
  },
  "required": [
    "states",
    "enabled"
  ],
  "additionalProperties": true
}
