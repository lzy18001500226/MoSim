{
  "template_type": "visible_thread_dispatch_packet",
  "template_version": 1,
  "request_id": "PMO-REQUEST-YYYYMMDD-001",
  "task_id": "PMO-TASK-YYYYMMDD-001",
  "origin_thread": "MoSim｜主线 PMO",
  "origin_thread_id": "019e9868-83ea-70f0-92c5-a3a408bd78c6",
  "target_thread": "MoSim｜示例可见部门",
  "target_thread_id": "replace-with-active-visible-thread-id",
  "responsible_department": "replace-with-owner",
  "task_class": "complicated_task",
  "objective": "Replace this with one bounded dispatch objective.",
  "read_scope": [
    "Replace with concrete read scope"
  ],
  "write_scope": [
    "Replace with concrete write scope, or [] for read-only"
  ],
  "allowed_actions": [
    "Work only inside the declared task scope"
  ],
  "forbidden_actions": [
    "Do not change visible-thread lifecycle"
  ],
  "native_surface_gate": {
    "selected_native_surface": [
      "visible_thread",
      "coagent_packet_glue"
    ],
    "surface_selection_reason": "Durable visible-department context plus packet return is required.",
    "rejected_surfaces": {
      "subagent": "Disposable context is insufficient for this reusable department assignment.",
      "codex_exec": "Hidden formal dispatch is not accepted without visible delivery."
    },
    "worktree_required": false,
    "worktree_decision": "No isolated worktree is required for this template instance unless the task packet declares one."
  },
  "semantic_boundary": {
    "decision_scope": "visible_thread",
    "state_class": "routable",
    "evidence_minimum": [
      "Target thread is status=active_visible in the dispatch registry"
    ],
    "allowed_actions": [
      "Send one bounded task packet and wait for the expected return/blocker packet"
    ],
    "forbidden_actions": [
      "Declare engineering completion from chat or packet metadata alone"
    ],
    "stop_triggers": [
      "Approval, provider, dispatch-surface, or agent-loop failure appears"
    ],
    "next_owner": "current_department"
  },
  "department_local_goal_required": true,
  "subagent_plan_required": true,
  "required_planning_fields": [
    "department_local_goal",
    "critical_path_steps",
    "parallelizable_slices",
    "subagent_plan",
    "subagent_plan_reason",
    "subagents_used",
    "verification_gates",
    "manual_review_or_blocker_triggers"
  ],
  "expected_engineering_outputs": [
    "Replace with concrete engineering evidence expected from this task"
  ],
  "acceptance": "Expected return packet passes the relevant contract checker and includes declared evidence.",
  "definition_of_done": "The target department writes the expected return packet or a blocker packet with semantic_boundary.",
  "stop_condition": "Stop on approval/review/provider surface, dispatch failure, or out-of-scope live/runtime requirement.",
  "checkpoint_plan": "Return a blocker or checkpoint if the critical path cannot finish in the current turn.",
  "expected_return_path": "Results/agent_packets/returns/PMO-REQUEST-YYYYMMDD-001.json",
  "blocker_return_path": "Results/agent_packets/blockers/PMO-REQUEST-YYYYMMDD-001.json"
}
